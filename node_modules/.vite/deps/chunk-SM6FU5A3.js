import {
  l
} from "./chunk-MQPF3LCI.js";
import {
  f,
  r as r2,
  s
} from "./chunk-WZSGCEUK.js";
import {
  r
} from "./chunk-SCZRL2WP.js";
import {
  m
} from "./chunk-HITYQXMG.js";
import {
  a2 as a
} from "./chunk-4XNJLQ7P.js";

// node_modules/@arcgis/core/widgets/smartMapping/SmartMappingSliderViewModel.js
var l2 = class extends l {
  constructor(t) {
    super(t), this._initialStopValues = [], this._max = null, this._min = null, this.hasTimeData = false, this.inputFormatFunction = (t2, s2) => "max" === s2 ? this.getUnzoomedMax().toString() : "min" === s2 ? this.getUnzoomedMin().toString() : t2.toString(), this.inputParseFunction = null, this.labelFormatFunction = (t2) => {
      if (this.hasTimeData) return r2(t2);
      const { max: s2, min: e, precision: i } = this, a2 = s2 - e > 10 ? 2 : i;
      return s(parseFloat(t2.toFixed(a2)));
    }, this.thumbsConstrained = false, this.zoomingEnabled = true;
  }
  set breaks(t) {
    this._set("breaks", t), this.notifyChange("max"), this.notifyChange("min");
  }
  set stops(t) {
    if (t == null ? void 0 : t.length) {
      const { max: s2, min: e } = this, i = this.getValuesFromStops(t), o = {};
      null != e && i.some((t2) => t2 < e) && (o.min = Math.min(...i)), null != s2 && i.some((t2) => t2 > s2) && (o.max = Math.max(...i)), this.set({ ...o }), this._initialStopValues = i;
    } else this._initialStopValues = null;
    this._set("stops", t || null), this.notifyChange("values");
  }
  get labels() {
    const { values: t } = this, s2 = t && t.length ? t.map((t2, s3) => this.getLabelForValue(t2, "value", s3)) : [], e = this.getUnzoomedMax(), i = this.getUnzoomedMin();
    return { max: this.getLabelForValue(e, "max"), min: this.getLabelForValue(i, "min"), values: s2 };
  }
  get max() {
    const { breaks: t } = this;
    return (t == null ? void 0 : t.length) ? t[t.length - 1].max : this.max;
  }
  set max(t) {
    const { zoomOptions: s2 } = this;
    if (null != (s2 == null ? void 0 : s2.max)) {
      const e = this.values;
      let i = s2.max;
      if (t < i && (i = t, s2.max = t), e && e.length) for (let s3 = 0; s3 < e.length; s3++) t < e[s3] && this.setValue(s3, t);
      this._storeZoomMax(i, t);
    } else this.updateBreakMax(t), this.setMax(t);
    this.notifyChange("labels");
  }
  get min() {
    const { breaks: t } = this;
    return (t == null ? void 0 : t.length) ? t[0].min : this.min;
  }
  set min(t) {
    const { zoomOptions: s2 } = this;
    if (null != (s2 == null ? void 0 : s2.min)) {
      const e = this.values;
      let i = s2.min;
      if (t > i && (i = t, s2.min = t), e && e.length) for (let s3 = 0; s3 < e.length; s3++) t > e[s3] && this.setValue(s3, t);
      this._storeZoomMin(i, t);
    } else this.updateBreakMin(t), this.setMin(t);
    this.notifyChange("labels");
  }
  get state() {
    const { max: t, min: s2, values: e } = this;
    return null != t && null != s2 && e.length > 0 ? "ready" : "disabled";
  }
  get values() {
    const { breaks: t, stops: s2 } = this;
    if (!t && !s2) return [];
    if (t == null ? void 0 : t.length) {
      const s3 = t.map((t2) => t2.max);
      return s3.pop(), s3;
    }
    if ((s2 == null ? void 0 : s2.length) < 2) return [];
    const e = this.getValuesFromStops();
    return [e[0], e[e.length - 1]];
  }
  set zoomOptions(t) {
    const { zoomingEnabled: s2, zoomOptions: e } = this;
    if (s2) {
      if (e) {
        const s3 = null != this._max ? this._max : this.max, e2 = null != this._min ? this._min : this.min;
        if (t) {
          const { max: i, min: o } = t, n = null != i, a2 = null != o, l3 = a2 ? t.min : e2, r4 = a2 ? e2 : null, h = n ? t.max : s3, m2 = n ? s3 : null;
          this._storeZoomMin(l3, r4), this._storeZoomMax(h, m2);
        } else this.setMax(s3), this.setMin(e2), this._min = null, this._max = null;
      } else if (t) {
        const { max: s3, min: e2 } = t;
        null != e2 && this._storeZoomMin(t.min, this.min), null != s3 && this._storeZoomMax(t.max, this.max);
      }
      this._set("zoomOptions", t), this.notifyChange("max"), this.notifyChange("min");
    }
  }
  getStopIndexFromValueIndex(t) {
    const { stops: s2 } = this, e = this.values[t];
    if (0 === t) {
      return e <= this.values[1] ? 0 : s2.length - 1;
    }
    if (1 === t) {
      return this.values[0] >= e ? 0 : s2.length - 1;
    }
    return null;
  }
  getStopChanges(t, s2) {
    const e = this.stops, i = this.getStopIndexFromValueIndex(t), o = this.getValuesFromStops();
    o[i] = s2;
    const [n, a2] = [o[0], o[o.length - 1]].sort((t2, s3) => t2 > s3 ? 1 : -1), l3 = a2 - n, r4 = e.length - 1;
    return o.map((t2, s3) => ({ index: s3, value: this.toPrecision(n + s3 * l3 / r4) }));
  }
  setValue(t, s2) {
    const { max: e, min: i, values: o } = this, n = o[t];
    if (isNaN(s2) || n === s2 || s2 > e || s2 < i) return;
    o[t] = this.toPrecision(s2), this._set("values", [...o]);
    const l3 = o.slice().sort((t2, s3) => t2 > s3 ? 1 : -1), r4 = l3[0], h = l3[l3.length - 1], m2 = f(r4, h, this._initialStopValues ?? []);
    this.updateStops(m2), this.updateBreaks(), this.notifyChange("labels");
  }
  getValuesFromStops(t) {
    const s2 = t || this.stops;
    return s2 == null ? void 0 : s2.map((t2) => t2.hasOwnProperty("ratio") ? t2.ratio : t2.value);
  }
  updateBreaks() {
    const { breaks: t, values: s2 } = this;
    (t == null ? void 0 : t.length) && s2.slice().sort((t2, s3) => t2 > s3 ? 1 : -1).forEach((s3, e) => {
      t[e].max = s3, null != t[e + 1] && (t[e + 1].min = s3);
    });
  }
  updateBreakMax(t) {
    const { breaks: s2, max: e, min: i } = this;
    !isNaN(t) && e !== t && null != i && t > i && (s2 == null ? void 0 : s2.length) && (s2[s2.length - 1].max = t);
  }
  updateBreakMin(t) {
    const { breaks: s2, max: e, min: i } = this;
    !isNaN(t) && i !== t && null != e && t < e && (s2 == null ? void 0 : s2.length) && (s2[0].min = t);
  }
  updateStops(t) {
    const { stops: s2 } = this, e = this.getValuesFromStops();
    if (e == null ? void 0 : e.length) {
      for (const s3 of t) e[s3.index] = s3.value;
      s2.forEach((t2, s3) => {
        t2.hasOwnProperty("ratio") ? t2.ratio = e[s3] : t2.value = e[s3];
      });
    }
  }
  getUnzoomedMax() {
    return this.zoomOptions && null != this._max ? this._max : this.max;
  }
  getUnzoomedMin() {
    return this.zoomOptions && null != this._min ? this._min : this.min;
  }
  _storeZoomMax(t, s2) {
    this._max = s2, this.setMax(t);
  }
  _storeZoomMin(t, s2) {
    this._min = s2, this.setMin(t);
  }
};
r([m()], l2.prototype, "breaks", null), r([m()], l2.prototype, "stops", null), r([m()], l2.prototype, "hasTimeData", void 0), r([m()], l2.prototype, "inputFormatFunction", void 0), r([m()], l2.prototype, "inputParseFunction", void 0), r([m()], l2.prototype, "labelFormatFunction", void 0), r([m({ readOnly: true })], l2.prototype, "labels", null), r([m()], l2.prototype, "max", null), r([m()], l2.prototype, "min", null), r([m({ readOnly: true })], l2.prototype, "state", null), r([m()], l2.prototype, "thumbsConstrained", void 0), r([m({ readOnly: true })], l2.prototype, "values", null), r([m()], l2.prototype, "zoomingEnabled", void 0), r([m()], l2.prototype, "zoomOptions", null), l2 = r([a("esri.widgets.smartMapping.SmartMappingSliderViewModel")], l2);
var r3 = l2;

export {
  r3 as r
};
//# sourceMappingURL=chunk-SM6FU5A3.js.map
