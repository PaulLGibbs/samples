import {
  n
} from "./chunk-55SEMSMA.js";
import {
  r
} from "./chunk-WTKIQJGU.js";

// node_modules/@arcgis/core/views/3d/layers/support/symbolColorUtils.js
var n2;
function e(t) {
  switch (t) {
    case "multiply":
    default:
      return n2.Multiply;
    case "ignore":
      return n2.Ignore;
    case "replace":
      return n2.Replace;
    case "tint":
      return n2.Tint;
  }
}
function r2(e2, r3, a2) {
  if (null == e2 || r3 === n2.Ignore) return a2[0] = 255, a2[1] = 255, a2[2] = 255, void (a2[3] = 255);
  const c = r(Math.round(e2[3] * i), 0, i), p = 0 === c || r3 === n2.Tint ? 0 : r3 === n2.Replace ? u : l;
  a2[0] = r(Math.round(e2[0] * o), 0, o), a2[1] = r(Math.round(e2[1] * o), 0, o), a2[2] = r(Math.round(e2[2] * o), 0, o), a2[3] = c + p;
}
!function(t) {
  t[t.Multiply = 1] = "Multiply", t[t.Ignore = 2] = "Ignore", t[t.Replace = 3] = "Replace", t[t.Tint = 4] = "Tint";
}(n2 || (n2 = {}));
var o = 255;
var i = 85;
var u = i;
var l = 2 * i;

// node_modules/@arcgis/core/views/3d/webgl-engine/collections/Component/Material/shader/DecodeSymbolColor.glsl.js
function a(a2) {
  a2.vertex.code.add(n`
    vec4 decodeSymbolColor(vec4 symbolColor, out int colorMixMode) {
      float symbolAlpha = 0.0;

      const float maxTint = 85.0;
      const float maxReplace = 170.0;
      const float scaleAlpha = 3.0;

      if (symbolColor.a > maxReplace) {
        colorMixMode = ${n.int(n2.Multiply)};
        symbolAlpha = scaleAlpha * (symbolColor.a - maxReplace);
      } else if (symbolColor.a > maxTint) {
        colorMixMode = ${n.int(n2.Replace)};
        symbolAlpha = scaleAlpha * (symbolColor.a - maxTint);
      } else if (symbolColor.a > 0.0) {
        colorMixMode = ${n.int(n2.Tint)};
        symbolAlpha = scaleAlpha * symbolColor.a;
      } else {
        colorMixMode = ${n.int(n2.Multiply)};
        symbolAlpha = 0.0;
      }

      return vec4(symbolColor.r, symbolColor.g, symbolColor.b, symbolAlpha);
    }
  `);
}

export {
  n2 as n,
  e,
  r2 as r,
  a
};
//# sourceMappingURL=chunk-RMGNMGKE.js.map
