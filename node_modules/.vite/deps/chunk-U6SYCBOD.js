import {
  b,
  l,
  m
} from "./chunk-REYN6SOX.js";
import {
  E,
  T
} from "./chunk-S5TBEJZU.js";
import {
  U
} from "./chunk-3QGNA5CV.js";
import {
  e
} from "./chunk-YJTIKWYS.js";
import {
  ue
} from "./chunk-W56LNAEB.js";
import {
  s
} from "./chunk-CAZ5FG6J.js";

// node_modules/@arcgis/core/smartMapping/statistics/heatmapStatistics.js
var p = ue;
async function u(a) {
  if (!(a == null ? void 0 : a.layer) || !a.view) throw new s("heatmap-statistics:missing-parameters", "'layer' and 'view' parameters are required");
  const u2 = [l.CSVLayer, l.FeatureLayer, l.GeoJSONLayer, l.KnowledgeGraphSublayer, l.OGCFeatureLayer, l.OrientedImageryLayer, l.ParquetLayer, l.StreamLayer, l.WFSLayer], { layer: m3, ...d } = a, y = m(m3, u2);
  if (!y) throw new s("heatmap-statistics:invalid-parameters", "'layer' must be one of these types: " + b(u2).join(", "));
  const f = { layerAdapter: y, ...d };
  f.radius = null == f.radius ? null == f.blurRadius ? 18 : e(f.blurRadius) : f.radius;
  const c = null != f.signal ? { signal: f.signal } : null;
  await y.load(c);
  const h = f.field, w = h ? y.getField(h) : null, L = await U({ field: h }), g = T(y, L, "heatmap-statistics:invalid-parameters");
  if (g) throw g;
  if (w) {
    const r = E(y, w, "heatmap-statistics:invalid-parameters", p);
    if (r) throw r;
  }
  return f;
}
async function m2(r) {
  const { layerAdapter: a, ...t } = await u(r);
  return a.heatmapStatistics(t);
}

export {
  m2 as m
};
//# sourceMappingURL=chunk-U6SYCBOD.js.map
