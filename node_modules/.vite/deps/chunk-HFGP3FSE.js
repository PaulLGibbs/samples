import {
  f
} from "./chunk-35USVNJT.js";
import {
  e as e2
} from "./chunk-C2LO2UDO.js";
import {
  s
} from "./chunk-6WRMI32T.js";
import {
  i
} from "./chunk-XORINZPE.js";
import {
  o
} from "./chunk-DHOEKN2U.js";
import {
  n
} from "./chunk-55SEMSMA.js";
import {
  e
} from "./chunk-HYT6FDCG.js";

// node_modules/@arcgis/core/chunks/MeasurementArrow.glsl.js
function d(d2) {
  const a2 = new i(), { vertex: l, fragment: c } = a2;
  f(l, d2), l.uniforms.add(new s("width", (e3) => e3.width)), a2.attributes.add(e.POSITION, "vec3"), a2.attributes.add(e.NORMAL, "vec3"), a2.attributes.add(e.UV0, "vec2"), a2.attributes.add(e.LENGTH, "float"), a2.varyings.add("vtc", "vec2"), a2.varyings.add("vlength", "float"), a2.varyings.add("vradius", "float"), l.main.add(n`vec3 bitangent = normal;
vtc = uv0;
vlength = length;
vradius = 0.5 * width;
vec4 pos = view * vec4(position + vradius * bitangent * uv0.y, 1.0);
gl_Position = proj * pos;`), c.uniforms.add(new s("outlineSize", (e3) => e3.outlineSize), new e2("outlineColor", (e3) => e3.outlineColor), new s("stripeLength", (e3) => e3.stripeLength), new e2("stripeEvenColor", (e3) => e3.stripeEvenColor), new e2("stripeOddColor", (e3) => e3.stripeOddColor));
  const v = 1 / Math.sqrt(2);
  return c.code.add(n`
    const float INV_SQRT2 = ${n.float(v)};

    vec4 arrowColor(vec2 tc, float len) {
      float d = INV_SQRT2 * (tc.x - abs(tc.y));
      d = min(d, INV_SQRT2 * (len - tc.x - abs(tc.y)));
      d = min(d, 1.0 - abs(tc.y));

      if (d < 0.0) {
        return vec4(0.0);
      }
      if (d < outlineSize) {
        return outlineColor;
      }
      return fract(0.5 / stripeLength * tc.x * vradius) >= 0.5 ? stripeOddColor : stripeEvenColor;
    }`), c.main.add(n`
    vec2 ntc = vec2(vtc.x / vradius, vtc.y);
    vec4 color = arrowColor(ntc, vlength / vradius);
    if (color.a < ${n.float(o)}) {
      discard;
    }
    fragColor = color;`), a2;
}
var a = Object.freeze(Object.defineProperty({ __proto__: null, build: d }, Symbol.toStringTag, { value: "Module" }));

export {
  d,
  a
};
//# sourceMappingURL=chunk-HFGP3FSE.js.map
