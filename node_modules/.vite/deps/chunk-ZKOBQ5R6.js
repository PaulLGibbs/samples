import {
  n
} from "./chunk-55SEMSMA.js";
import {
  t
} from "./chunk-EYUJRAXJ.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/shading/Gamma.glsl.js
function e(e2) {
  e2.fragment.code.add(n`
    const float GAMMA = ${n.float(t)};
    const float INV_GAMMA = ${n.float(1 / t)};

    vec4 delinearizeGamma(vec4 color) {
      return vec4(pow(color.rgb, vec3(INV_GAMMA)), color.a);
    }

    vec3 linearizeGamma(vec3 color) {
      return pow(color, vec3(GAMMA));
    }
  `);
}

export {
  e
};
//# sourceMappingURL=chunk-ZKOBQ5R6.js.map
