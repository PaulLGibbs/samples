import {
  d
} from "./chunk-JUR5IDCC.js";
import {
  C,
  a
} from "./chunk-NFP5DC4E.js";
import {
  n
} from "./chunk-ADK5FOLG.js";
import {
  m
} from "./chunk-5OHMIWSA.js";

// node_modules/@arcgis/core/smartMapping/labels/support/utils.js
function r(e, n2) {
  const o = n2.featureReduction;
  if ("fields" in o) {
    for (const t of o.fields) if (t.name.toLowerCase() === e.toLowerCase()) return t;
  }
  return null;
}
function a2(e) {
  return `
  $feature["${e}"];
  var value = $feature["${e}"];
  var num = Count(Text(Round(value)));
  var label = When(
    num < 4, Text(value, "#.#"),
    num == 4, Text(value / Pow(10, 3), "#.0k"),
    num <= 6, Text(value / Pow(10, 3), "#k"),
    num == 7, Text(value / Pow(10, 6), "#.0m"),
    num > 7, Text(value / Pow(10, 6), "#m"),
    Text(value, "#,###")
  );
  return label;
  `;
}
function u(e, n2, l, r2) {
  const u2 = l.name;
  if ("mode" === l.statisticType) {
    const a3 = "getFieldDomain" in e ? e.getFieldDomain(l.onStatisticField) : null;
    let s2 = null;
    return "coded-value" === (a3 == null ? void 0 : a3.type) ? s2 = a3.codedValues.map((e2) => new n({ value: e2.code, label: e2.name })) : "unique-value" === (n2 == null ? void 0 : n2.type) && null != n2.field && n2.field.toLowerCase() === u2.toLowerCase() && (s2 = (n2.uniqueValueInfos ?? []).map((e2) => e2.clone())), s2 ? d(s2, u2, r2) : `Text($feature["${u2}"])`;
  }
  return a2(u2);
}
function s(o, t = false) {
  const r2 = new m({ haloColor: "#373837", haloSize: "1px", color: "#f0f0f0", font: { family: "Noto Sans", size: "12px", weight: "bold" } });
  return new C({ symbol: r2, deconflictionStrategy: "none", labelPlacement: t ? "always-horizontal" : "center-center", labelExpressionInfo: new a({ expression: o }) });
}

export {
  r,
  a2 as a,
  u,
  s
};
//# sourceMappingURL=chunk-DYZMHZVM.js.map
