import {
  h,
  i
} from "./chunk-IELVVG4J.js";
import {
  v
} from "./chunk-CEEENLG2.js";

// node_modules/@arcgis/core/smartMapping/statistics/spatialStatistics.js
function o(n, e = false) {
  let o2 = [];
  if (e) {
    for (const t of n) if (t.geometry) {
      const n2 = t.geometry;
      o2.push.apply(o2, n2.points);
    }
  } else o2 = n.map((t) => t.geometry);
  const s2 = [], i3 = [];
  let l2 = 1 / 0, r = -1 / 0, a = 0, c = 0, f = 0, u = 0;
  for (const m of o2) {
    if (!m) continue;
    e ? (s2[0] = m[0], s2[1] = m[1]) : (s2[0] = m.x, s2[1] = m.y);
    let n2 = 1 / 0, p = -1 / 0;
    for (const a2 of o2) {
      if (a2 === m) continue;
      if (!a2) continue;
      e ? (i3[0] = a2[0], i3[1] = a2[1]) : (i3[0] = a2.x, i3[1] = a2.y);
      const o3 = i(s2, i3);
      o3 > 0 && (o3 < n2 && (n2 = o3), o3 < l2 && (l2 = o3), o3 > p && (p = o3), o3 > r && (r = o3));
    }
    n2 !== 1 / 0 && (++f, a += n2), p !== -1 / 0 && (++u, c += p);
  }
  return { minDistance: l2 !== 1 / 0 ? l2 : null, maxDistance: r !== -1 / 0 ? r : null, avgMinDistance: f ? a / f : null, avgMaxDistance: u ? c / u : null };
}
function s(t) {
  let e = 0, o2 = 0, s2 = 1 / 0, i3 = -1 / 0;
  for (const l2 of t) {
    const t2 = l2.geometry;
    if (t2) {
      let l3 = 0;
      for (const e2 of t2.paths) {
        const t3 = h(e2);
        t3 > 0 && (l3 += t3);
      }
      l3 > 0 && (++e, o2 += l3, l3 < s2 && (s2 = l3), l3 > i3 && (i3 = l3));
    }
  }
  return { minLength: s2 !== 1 / 0 ? s2 : null, maxLength: i3 !== -1 / 0 ? i3 : null, avgLength: e ? o2 / e : null };
}
function i2(t) {
  let n = 0, o2 = 0, s2 = 1 / 0, i3 = -1 / 0;
  for (const l2 of t) {
    const t2 = l2.geometry;
    if (t2) {
      const l3 = v(t2);
      if (l3) {
        const t3 = Math.abs(l3.xmax - l3.xmin), e = Math.abs(l3.ymax - l3.ymin), r = Math.sqrt(t3 * e);
        r > 0 && (++n, o2 += r, r < s2 && (s2 = r), r > i3 && (i3 = r));
      }
    }
  }
  return { minSize: s2 !== 1 / 0 ? s2 : null, maxSize: i3 !== -1 / 0 ? i3 : null, avgSize: n ? o2 / n : null };
}
function l(t) {
  const { features: n } = t;
  let e = null;
  switch (t.geometryType) {
    case "point":
      e = o(n);
      break;
    case "multipoint":
      e = o(n, true);
      break;
    case "polyline":
      e = s(n);
      break;
    case "polygon":
      e = i2(n);
  }
  return Promise.resolve(e);
}

export {
  l
};
//# sourceMappingURL=chunk-4LSJIQF3.js.map
