import {
  g
} from "./chunk-IT4SSBCW.js";
import {
  p
} from "./chunk-PJ4EDUAZ.js";
import {
  s
} from "./chunk-CAZ5FG6J.js";

// node_modules/@arcgis/core/layers/support/RasterJobHandler.js
var t = class {
  constructor() {
    this._workerThread = null, this._destroyed = false;
  }
  async initialize() {
    const e = await p("RasterWorker");
    this._destroyed ? e.close() : this._workerThread = e;
  }
  destroy() {
    this._destroyed = true, this._workerThread && (this._workerThread.close(), this._workerThread = null);
  }
  async convertVectorFieldData(r, t2) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    const n = await this._workerThread.invoke("convertVectorFieldData", { pixelBlock: r.pixelBlock.toJSON(), type: r.dataType }, t2);
    return n ? new g(n) : null;
  }
  computeStatisticsHistograms(r, o) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    return this._workerThread.invoke("computeStatisticsHistograms", { pixelBlock: r.pixelBlock.toJSON() }, o);
  }
  async decode(r, t2) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    const n = await this._workerThread.invoke("decode", r, t2);
    return n ? new g(n) : null;
  }
  async symbolize(r, t2) {
    var _a, _b;
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    const n = { extent: (_a = r.extent) == null ? void 0 : _a.toJSON(), pixelBlock: (_b = r.pixelBlock) == null ? void 0 : _b.toJSON(), simpleStretchParams: r.simpleStretchParams, bandIds: r.bandIds }, a = await this._workerThread.invoke("symbolize", n, t2);
    return a ? new g(a) : null;
  }
  async updateSymbolizer(r, o) {
    var _a;
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    const t2 = (_a = r == null ? void 0 : r.rendererJSON) == null ? void 0 : _a.histograms;
    await Promise.all(this._workerThread.broadcast("updateSymbolizer", { symbolizerJSON: r.toJSON(), histograms: t2 }, o));
  }
  async updateRasterFunction(r, o) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    await Promise.all(this._workerThread.broadcast("updateRasterFunction", { rasterFunctionJSON: r.toJSON() }, o));
  }
  async process(r, t2) {
    var _a, _b;
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    const n = await this._workerThread.invoke("process", { extent: (_a = r.extent) == null ? void 0 : _a.toJSON(), primaryPixelSizes: (_b = r.primaryPixelSizes) == null ? void 0 : _b.map((e) => null != e ? e.toJSON() : null), primaryPixelBlocks: r.primaryPixelBlocks.map((e) => null != e ? e.toJSON() : null), primaryRasterIds: r.primaryRasterIds }, t2);
    return n ? new g(n) : null;
  }
  async stretch(r, t2) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    if (!(r == null ? void 0 : r.pixelBlock)) return null;
    const n = { srcPixelBlock: r.pixelBlock.toJSON(), stretchParams: r.stretchParams }, a = await this._workerThread.invoke("stretch", n, t2);
    return a ? new g(a) : null;
  }
  async split(r, t2) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    if (!(r == null ? void 0 : r.pixelBlock)) return null;
    const n = { srcPixelBlock: r.pixelBlock.toJSON(), tileSize: r.tileSize, maximumPyramidLevel: r.maximumPyramidLevel, useBilinear: r.useBilinear }, a = await this._workerThread.invoke("split", n, t2);
    return a && a.forEach((e, r2) => {
      a.set(r2, e ? g.fromJSON(e) : null);
    }), a;
  }
  async clipTile(r, t2) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    if (!(r == null ? void 0 : r.pixelBlock)) return null;
    const n = { ...r, pixelBlock: r.pixelBlock.toJSON() }, a = await this._workerThread.invoke("clipTile", n, t2);
    return a ? g.fromJSON(a) : null;
  }
  async estimateStatisticsHistograms(r, o) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    if (!(r == null ? void 0 : r.pixelBlock)) return null;
    const t2 = { srcPixelBlock: r.pixelBlock.toJSON() };
    return await this._workerThread.invoke("estimateStatisticsHistograms", t2, o);
  }
  async mosaicAndTransform(r, t2) {
    var _a;
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    if (!((_a = r == null ? void 0 : r.srcPixelBlocks) == null ? void 0 : _a.length)) return { pixelBlock: null };
    const n = { ...r, srcPixelBlocks: r.srcPixelBlocks.map((e) => null != e ? e.toJSON() : null) }, a = await this._workerThread.invoke("mosaicAndTransform", n, t2);
    return { pixelBlock: a.pixelBlock ? new g(a.pixelBlock) : null, localNorthDirections: a.localNorthDirections };
  }
  async createFlowMesh(r, o) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    const t2 = { buffer: r.flowData.data.buffer, maskBuffer: r.flowData.mask.buffer, width: r.flowData.width, height: r.flowData.height }, { meshType: n, simulationSettings: a } = r, i = await this._workerThread.invoke("createFlowMesh", { meshType: n, flowData: t2, simulationSettings: a }, { ...o, transferList: [t2.buffer, t2.maskBuffer] });
    return { vertexData: new Float32Array(i.vertexBuffer), indexData: new Uint32Array(i.indexBuffer) };
  }
  getProjectionOffsetGrid(r, o) {
    if (!this._workerThread) throw new s("raster-jobhandler:no-connection", "no available worker connection");
    const t2 = null != r.datumTransformation ? r.datumTransformation.steps.map((e) => ({ wkid: e.wkid, wkt: e.wkt, isInverse: e.isInverse })) : null, n = null != r.rasterTransform ? r.rasterTransform.toJSON() : null, a = { projectedExtent: r.projectedExtent.toJSON(), srcBufferExtent: r.srcBufferExtent.toJSON(), pixelSize: r.pixelSize, hasWrapAround: r.hasWrapAround, spacing: r.spacing, datumTransformationSteps: t2, rasterTransform: n, isAdaptive: r.isAdaptive, includeGCSGrid: r.includeGCSGrid };
    return this._workerThread.invoke("getProjectionOffsetGrid", a, o);
  }
};

export {
  t
};
//# sourceMappingURL=chunk-PBGRHHZ4.js.map
