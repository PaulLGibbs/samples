import {
  o
} from "./chunk-XX6OB2JQ.js";
import {
  M,
  q,
  v
} from "./chunk-VHYEPDZ3.js";
import {
  Gu,
  I,
  Nc,
  Ol,
  Vl,
  Xo,
  Yl,
  eh,
  fs,
  ig,
  kh,
  kl,
  ls,
  nu,
  qg,
  us,
  vc,
  yc
} from "./chunk-3LTZKV7D.js";
import {
  x as x2
} from "./chunk-AJBMH4BL.js";
import {
  Bt,
  X
} from "./chunk-2I7JQ4NT.js";
import {
  n as n3
} from "./chunk-BKBHJYAN.js";
import {
  Mt,
  P,
  Qt,
  a,
  b,
  ct,
  ei,
  h,
  n as n2,
  w,
  x2 as x
} from "./chunk-ODBTW2MK.js";
import {
  n,
  s
} from "./chunk-SCZRL2WP.js";

// node_modules/@arcgis/core/chunks/GeodeticDistanceCalculator-D4iQ-_NX.js
function X2() {
  return new n3();
}
var k = class {
  [Symbol.dispose]() {
    this.m_peR1.destroy(), this.m_peR2.destroy(), this.m_peR3.destroy(), this.m_peR1 = null, this.m_peR2 = null, this.m_peR3 = null;
  }
  constructor(t, e, i) {
    this.m_peR1 = new yc(), this.m_peR2 = new yc(), this.m_peR3 = new yc(), void 0 !== t ? this.reset(t, e, i) : this.reset(1, 0, 1);
  }
  reset(t, e, i) {
    this.m_a = t, this.m_e2 = e, this.m_b = this.m_a * Math.sqrt(1 - this.m_e2), this.m_unitToDegree = i, this.m_unitToRad = 0.017453292519943295 * i, this.m_u180 = 180 / this.m_unitToDegree, this.m_u360 = 360 / this.m_unitToDegree;
  }
  calculateBbox(t, e, i, s2) {
    const n4 = this.m_peR1, o2 = this.m_peR2, a2 = this.m_peR3;
    vc.geodeticDistance(this.m_a, this.m_e2, t * this.m_unitToRad, e * this.m_unitToRad, i * this.m_unitToRad, s2 * this.m_unitToRad, a2, n4, o2, 0);
    const m = Math.cos(n4.val), r = Math.cos(o2.val + 3.141592653589793), u = e * this.m_unitToRad, c = Nc.phiToEta(this.m_e2, u), h2 = Math.sin(n4.val) * Math.cos(c);
    let l = e;
    if (m * r < 0) {
      const t2 = m < 0 ? -1 : 1, e2 = Math.acos(Math.abs(h2)) * t2;
      l = Nc.etaToPhi(this.m_e2, e2) / this.m_unitToRad;
    }
    let p = ct((i - t) * this.m_unitToRad, 2 * Math.PI);
    Math.abs(p) > Math.PI && (p = p > 0 ? p - 2 * Math.PI : p + 2 * Math.PI), p /= this.m_unitToRad, i = t + p;
    const d = X2();
    return d.xmin = Math.min(t, i), d.xmax = Math.max(t, i), d.ymin = Math.min(e, s2), d.ymin = Math.min(d.ymin, l), d.ymax = Math.max(e, s2), d.ymax = Math.max(d.ymax, l), d;
  }
  minDistanceGeodesic(t, e) {
    const i = O(t), s2 = O(e);
    if (i && s2) return this.lowerDistanceGeodetic_(t.xmin, t.ymin, e.xmin, e.ymin);
    const n4 = this.normalizeGeodesic_(e, t);
    return U(t, n4) ? t.ymin >= n4.ymax ? this.minDistanceGeodesicUpDown_(t, n4) : n4.ymin >= t.ymax ? this.minDistanceGeodesicUpDown_(n4, t) : t.xmin > n4.xmax ? this.minDistanceGeodesicLeftRight_(n4, t) : this.minDistanceGeodesicLeftRight_(t, n4) : 0;
  }
  normalizeGeodesic_(t, e) {
    const i = t.clone(), s2 = t.clone();
    let n4 = V(i, e);
    if (0 === n4) return i;
    for (; s2.xmax > e.xmin; ) {
      s2.xmin -= this.m_u360, s2.xmax -= this.m_u360;
      const t2 = V(s2, e);
      if (t2 < n4 && (i.assign(s2), n4 = t2, 0 === n4)) return i;
    }
    for (s2.assign(t); s2.xmin < e.xmax; ) {
      s2.xmin += this.m_u360, s2.xmax += this.m_u360;
      const t2 = V(s2, e);
      if (t2 < n4 && (i.assign(s2), n4 = t2, 0 === n4)) return i;
    }
    return i;
  }
  lowerDistanceGeodetic_(t, e, i, s2) {
    const n4 = this.m_peR1;
    {
      const o2 = this.m_b * this.m_b / this.m_a, a2 = Nc.phiToPhig(this.m_e2, this.m_unitToRad * e), m = Nc.phiToPhig(this.m_e2, this.m_unitToRad * s2);
      vc.geodeticDistance(o2, 0, this.m_unitToRad * t, a2, this.m_unitToRad * i, m, n4, null, null, 0);
    }
    return n4.val;
  }
  minDistanceGeodesicUpDown_(t, e) {
    if (e.xmin <= t.xmin ? e.xmax >= t.xmin : t.xmax >= e.xmin) return this.lowerDistanceGeodetic_(0, t.ymin, 0, e.ymax);
    if (e.xmin > t.xmax) {
      const i = this.lowerDistanceGeodeticSegMeridional_(t.xmax, t.ymin, t.ymax, e.xmin, e.ymin, e.ymax);
      if (e.xmax - t.xmin < this.m_u180) return i;
      const s2 = this.lowerDistanceGeodeticSegMeridional_(t.xmin, t.ymin, t.ymax, e.xmax, e.ymin, e.ymax);
      return Math.min(i, s2);
    }
    {
      const i = this.lowerDistanceGeodeticSegMeridional_(t.xmin, t.ymin, t.ymax, e.xmax, e.ymin, e.ymax);
      if (t.xmax - e.xmin < this.m_u180) return i;
      const s2 = this.lowerDistanceGeodeticSegMeridional_(t.xmax, t.ymin, t.ymax, e.xmin, e.ymin, e.ymax);
      return Math.min(i, s2);
    }
  }
  minDistanceGeodesicLeftRight_(t, e) {
    let i = Math.abs(t.xmax - e.xmin);
    for (; i > this.m_u180; ) i -= this.m_u360;
    let s2 = Math.abs(t.xmin - e.xmax);
    for (; s2 > this.m_u180; ) s2 -= this.m_u360;
    return Math.abs(i) <= Math.abs(s2) ? this.minDistanceGeodesicLeftRightNormalized_(t, e) : this.minDistanceGeodesicLeftRightNormalized_(e, t);
  }
  minDistanceGeodesicLeftRightNormalized_(t, e) {
    let i, s2;
    return i = this.lowerDistanceGeodetic_(t.xmax, t.ymin, e.xmin, e.ymin), t.ymin >= e.ymin && t.ymin <= e.ymax && (s2 = this.lowerDistanceGeodeticPtMeridional_(t.xmax, t.ymin, e.xmin, e.ymin, e.ymax), i = Math.min(i, s2)), e.ymin >= t.ymin && e.ymin <= t.ymax && (s2 = this.lowerDistanceGeodeticPtMeridional_(e.xmin, e.ymin, t.xmax, t.ymin, t.ymax), i = Math.min(i, s2)), s2 = this.lowerDistanceGeodetic_(t.xmax, t.ymax, e.xmin, e.ymax), i = Math.min(i, s2), t.ymax >= e.ymin && t.ymax <= e.ymax && (s2 = this.lowerDistanceGeodeticPtMeridional_(t.xmax, t.ymax, e.xmin, e.ymin, e.ymax), i = Math.min(i, s2)), e.ymax >= t.ymin && e.ymax <= t.ymax && (s2 = this.lowerDistanceGeodeticPtMeridional_(e.xmin, e.ymax, t.xmax, t.ymin, t.ymax), i = Math.min(i, s2)), i;
  }
  lowerDistanceGeodeticSegMeridional_(t, e, i, s2, n4, o2) {
    let a2 = this.lowerDistanceGeodeticPtMeridional_(t, e, s2, n4, o2);
    const m = this.lowerDistanceGeodeticPtMeridional_(t, i, s2, n4, o2);
    let r = this.lowerDistanceGeodeticPtMeridional_(s2, n4, t, e, i);
    const u = this.lowerDistanceGeodeticPtMeridional_(s2, o2, t, e, i);
    return a2 = Math.min(a2, m), r = Math.min(r, u), Math.min(a2, r);
  }
  lowerDistanceGeodeticPtMeridional_(t, e, i, s2, n4) {
    const o2 = this.m_b * this.m_b / this.m_a, a2 = Nc.phiToPhig(this.m_e2, this.m_unitToRad * e), m = Nc.phiToPhig(this.m_e2, this.m_unitToRad * s2), r = Nc.phiToPhig(this.m_e2, this.m_unitToRad * n4), u = this.m_peR1, c = this.m_peR2;
    vc.geodeticDistance(o2, 0, this.m_unitToRad * i, m, this.m_unitToRad * t, a2, u, null, null, 0), vc.geodeticDistance(o2, 0, this.m_unitToRad * i, r, this.m_unitToRad * t, a2, c, null, null, 0);
    const h2 = us(1, 0, new ei(this.m_unitToRad * t, a2)), l = us(1, 0, new ei(this.m_unitToRad * i, m)), d = us(1, 0, new ei(this.m_unitToRad * i, r));
    let g = new X();
    g.setCrossProductVector(l, d);
    const x3 = l.dotProduct(d);
    if (g.length() < 1568e-13) return x3 >= 0 ? Math.min(u.val, c.val) : 0;
    g = g.getUnitVector();
    let y = new X();
    if (y.setCrossProductVector(g, h2), y.length() < 1568e-13) {
      return Math.min(u.val, c.val) - 1e-5 * o2;
    }
    y = y.getUnitVector();
    let P2 = h2.sub(g.mul(h2.dotProduct(g)));
    {
      const e2 = new X();
      e2.setCrossProductVector(l, P2);
      const i2 = new X();
      i2.setCrossProductVector(P2, d);
      let s3 = g.dotProduct(e2) > 0 && g.dotProduct(i2) > 0;
      if (s3 || (P2.negateThis(), e2.setCrossProductVector(l, P2), i2.setCrossProductVector(P2, d), s3 = g.dotProduct(e2) > 0 && g.dotProduct(i2) > 0), s3) {
        P2 = P2.getUnitVector();
        const e3 = fs(1, 0, P2), i3 = this.m_peR3;
        vc.geodeticDistance(o2, 0, this.m_unitToRad * t, a2, e3.x, e3.y, i3, null, null, 0);
        const s4 = Math.min(u.val, c.val);
        return Math.min(s4, i3.val);
      }
    }
    return Math.min(u.val, c.val);
  }
};
function O(t) {
  return t.xmin === t.xmax && t.ymin === t.ymax;
}
function U(t, e) {
  return e.xmax < t.xmin || e.xmin > t.xmax || e.ymax < t.ymin || e.ymin > t.ymax;
}
function V(t, e) {
  let i, s2 = 0;
  return i = t.xmin - e.xmax, i > s2 && (s2 = i), i = e.xmin - t.xmax, i > s2 && (s2 = i), s2;
}
var j = class {
};
function L(t) {
  return { outPoint: void 0 === t ? new ei() : t.clone() };
}
function z(t, e) {
  t.outPoint.assign(e.outPoint);
}
function H() {
  return new n3();
}
var Q = class {
  constructor(t, e, i, s2, n4 = 0, o2 = 4) {
    this.m_ptDistFrom = new ei(), this.m_segStartPt = new ei(), this.m_segEndPt = new ei(), this.m_geodeticLength = new yc(), this.m_az12 = new yc(), this.m_minGeodeticDist = new yc(), this.m_segStartPt3d = new X(), this.m_segEndPt3d = new X(), this.m_sr = s2, this.m_distCurveType = n4, this.m_segCurveType = o2, this.m_inputGCS = this.m_sr.getGCS(), this.m_rpu = this.m_inputGCS.getUnit().getUnitToBaseFactor(), this.m_bIsPannablePcs = 2 === this.m_sr.getCoordinateSystemType() && this.m_sr.isPannable();
    const a2 = Gu();
    this.m_inputGCS.querySpheroidData(a2), this.m_a = a2.majorSemiAxis, this.m_eSquared = a2.e2, this.setPointDistFrom(t), this.setSegmentEndPoints(e, i);
  }
  setSegmentEndPoints(t, e) {
    this.m_segStartPt.assign(t), this.m_segEndPt.assign(e), this.m_bIsPannablePcs || (this.m_segStartPt.mulThis(this.m_rpu), this.m_segEndPt.mulThis(this.m_rpu)), 2 === this.m_segCurveType && (this.m_segStartPt3d.assign(us(this.m_a, this.m_eSquared, this.m_segStartPt)), this.m_segEndPt3d.assign(us(this.m_a, this.m_eSquared, this.m_segEndPt))), this.calculateAndUpdateSegmentLength();
  }
  setPointDistFrom(t) {
    this.m_ptDistFrom.assign(t), this.m_ptDistFrom.scale(this.m_rpu);
  }
  setSegmentCurveType(t) {
    this.m_segCurveType = t;
  }
  setDistanceCurveType(t) {
    this.m_distCurveType = t;
  }
  makeFunctor() {
    return (i) => {
      let s2;
      switch (this.m_segCurveType) {
        case 0:
        case 1:
        case 3: {
          const o2 = { stack: [], error: void 0, hasError: false };
          try {
            const e = n(o2, new yc(), false), n4 = n(o2, new yc(), false);
            vc.geodeticCoordinate(this.m_a, this.m_eSquared, this.m_segStartPt.x, this.m_segStartPt.y, this.m_geodeticLength.val * i, this.m_az12.val, e, n4, this.m_segCurveType), s2 = new ei(e.val, n4.val);
            break;
          } catch (n4) {
            o2.error = n4, o2.hasError = true;
          } finally {
            s(o2);
          }
        }
        case 2: {
          const t = X.lerp(this.m_segStartPt3d, this.m_segEndPt3d, i);
          s2 = fs(this.m_a, this.m_eSquared, t);
          break;
        }
        case 4:
          s2 = ei.lerp(this.m_segStartPt, this.m_segEndPt, i), this.m_bIsPannablePcs && (ig(this.m_sr, 0, [s2], 1), s2.mulThis(this.m_rpu));
          break;
        default:
          b("Invalid curve type");
      }
      return vc.geodeticDistance(this.m_a, this.m_eSquared, this.m_ptDistFrom.x, this.m_ptDistFrom.y, s2.x, s2.y, this.m_minGeodeticDist, null, null, this.m_distCurveType), this.m_minGeodeticDist.val;
    };
  }
  calculateAndUpdateSegmentLength() {
    switch (this.m_segCurveType) {
      case 0:
      case 2:
      case 1:
      case 3:
        vc.geodeticDistance(this.m_a, this.m_eSquared, this.m_segStartPt.x, this.m_segStartPt.y, this.m_segEndPt.x, this.m_segEndPt.y, this.m_geodeticLength, this.m_az12, null, this.m_segCurveType);
        break;
      case 4:
        this.m_geodeticLength.val = ei.distance(this.m_segStartPt, this.m_segEndPt);
        break;
      default:
        b("Invalid curve type");
    }
  }
  [Symbol.dispose]() {
    this.m_geodeticLength[Symbol.dispose](), this.m_az12[Symbol.dispose](), this.m_minGeodeticDist[Symbol.dispose]();
  }
};
function W() {
  return new j();
}
var J = class {
  [Symbol.dispose]() {
    this.m_envHelper[Symbol.dispose]();
  }
  constructor(t, e, i, s2, n4) {
    this.m_boxGeomA = H(), this.m_boxGeomB = H(), this.m_envGeomA = new n3(), this.m_envGeomB = new n3(), this.m_progressCounter = 0, this.m_transformPCS2GCS = null, this.m_segmentBoxesA = /* @__PURE__ */ new Map(), this.m_segmentBoxesB = /* @__PURE__ */ new Map(), this.m_boundaryPtsA = [], this.m_boundaryPtsB = [], this.m_scaleToRadians = new x2(), this.m_scaleToDegrees = new x2(), this.m_inputSR = t, this.m_distCurveType = e, this.m_progressTracker = i, this.m_maxDistance = s2, this.m_maxDeviation = n4, this.m_inputGCS = t.getGCS(), this.m_peGeogcs = this.m_inputGCS.getPECoordSys(), this.m_tolerance = 0;
    const o2 = Gu();
    this.m_inputGCS.querySpheroidData(o2), this.m_a = o2.majorSemiAxis, this.m_eSquared = o2.e2, this.m_rpu = this.m_inputGCS.getUnit().getUnitToBaseFactor(), this.m_envHelper = new k(this.m_a, this.m_eSquared, 1), 4 === this.m_distCurveType && (this.m_distCurveType = 0), this.m_boxGeomA.setEmpty(), this.m_boxGeomB.setEmpty(), this.m_envGeomA.setEmpty(), this.m_envGeomB.setEmpty(), this.m_bIsPannablePcs = 2 === this.m_inputSR.getCoordinateSystemType() && this.m_inputSR.isPannable();
  }
  progress(t = false) {
  }
  calculate(t, e, i, s2) {
    const n4 = L(), o2 = L();
    if (t.queryEnvelope(this.m_envGeomA), e.queryEnvelope(this.m_envGeomB), this.checkGeometriesIntersect(t, e, n4, o2)) return i && z(i, n4), s2 && z(s2, o2), 0;
    const a2 = this.prepareGeometry(t);
    if (a2.isEmpty()) return Number.NaN;
    const m = this.prepareGeometry(e);
    if (m.isEmpty()) return Number.NaN;
    this.m_tolerance = this.computeTolerance(a2, m);
    const r = this._ExecuteBruteForce(a2, m, n4, o2);
    return r >= this.m_maxDistance ? Number.NaN : (i && (this.prepareOutput(n4), z(i, n4)), s2 && (this.prepareOutput(o2), z(s2, o2)), r);
  }
  createDistanceFunctor(t, e, i) {
    const s2 = new Q(t, e, i, this.m_inputSR, this.m_distCurveType);
    return this.m_bIsPannablePcs || 1 === this.m_inputSR.getCoordinateSystemType() || s2.setSegmentCurveType(2), s2;
  }
  _NormalizeAndProject(t) {
    if (this.m_inputSR.isPannable() && (t = new kh().foldInto360Range(t, this.m_inputSR)), 2 === this.m_inputSR.getCoordinateSystemType()) {
      const e = qg(this.m_inputSR, this.m_inputGCS, null);
      t = new kh().execute(t, e, this.m_progressTracker);
    }
    return t;
  }
  _ExecuteBruteForce(t, e, n4, o2) {
    switch (t.getGeometryType()) {
      case a.enumPoint:
        return this.calculateDistanceGeodeticPointGeometry(t, e, n4, o2);
      case a.enumMultiPoint:
        return this.calculateDistanceGeodeticMultipointGeometry(t, e, n4, o2);
      case a.enumPolyline:
      case a.enumPolygon:
      case a.enumEnvelope:
        return this.calculateDistanceGeodeticMultipathGeometry(t, e, n4, o2);
      default:
        P("");
    }
  }
  calculateDistanceGeodeticPointGeometry(t, e, n4, o2) {
    switch (e.getGeometryType()) {
      case a.enumPoint:
        return this.calculateDistanceGeodeticPointPoint(t, e, n4, o2);
      case a.enumMultiPoint:
        return this.calculateDistanceGeodeticPointMultipoint(t, e, n4, o2);
      case a.enumPolyline:
      case a.enumPolygon:
        return this.calculateDistanceGeodeticPointMultipath(t, e, n4, o2);
      default:
        P("");
    }
  }
  calculateDistanceGeodeticPointPoint(i, s2, n4, o2) {
    const a2 = { stack: [], error: void 0, hasError: false };
    try {
      const e = i.getXY();
      e.scale(this.m_rpu);
      const m = s2.getXY();
      m.scale(this.m_rpu), n4.outPoint.assign(e), o2.outPoint.assign(m);
      const r = n(a2, new yc(), false);
      return vc.geodeticDistance(this.m_a, this.m_eSquared, e.x, e.y, m.x, m.y, r, null, null, this.m_distCurveType), r.val;
    } catch (m) {
      a2.error = m, a2.hasError = true;
    } finally {
      s(a2);
    }
  }
  calculateDistanceGeodeticPointMultipoint(i, s2, n4, o2) {
    const a2 = { stack: [], error: void 0, hasError: false };
    try {
      this.computeEnvelopesAndBoxes(i, s2);
      const e = this.estimateMinimumDistance(n4, o2);
      let m = e < this.m_maxDistance ? e : this.m_maxDistance;
      const r = i.getXY();
      r.scale(this.m_rpu), n4.outPoint.assign(r);
      const u = us(this.m_a, this.m_eSquared, r), c = new ei(), h2 = s2.getImpl(), d = n(a2, new yc(), false), g = h2.getPointCount();
      for (let t = 0; t < g; ++t) {
        h2.queryXY(t, c), c.scale(this.m_rpu);
        const e2 = us(this.m_a, this.m_eSquared, c);
        if (!(X.distance(u, e2) > m) && (!(this.m_envHelper.minDistanceGeodesic(this.m_boxGeomA, this.m_boxGeomB) > m) && (vc.geodeticDistance(this.m_a, this.m_eSquared, r.x, r.y, c.x, c.y, d, null, null, this.m_distCurveType), d.val < m && (m = d.val, o2.outPoint = c, 0 === m)))) return m;
      }
      return m === this.m_maxDistance ? Number.POSITIVE_INFINITY : m;
    } catch (m) {
      a2.error = m, a2.hasError = true;
    } finally {
      s(a2);
    }
  }
  calculateDistanceGeodeticPointMultipath(i, s2, o2, a2) {
    const m = { stack: [], error: void 0, hasError: false };
    try {
      const e = new ei(), r = new ei();
      e.assign(i.getXY()), r.assign(i.getXY()), o2.outPoint.assign(r.mul(this.m_rpu)), this.m_bIsPannablePcs && Yl(this.m_inputSR, [e], 1, false), this.computeEnvelopesAndBoxes(i, s2);
      const u = this.estimateMinimumDistance(o2, a2);
      let c = u < this.m_maxDistance ? u : this.m_maxDistance;
      const h2 = s2.getImpl(), l = new ei(0, 0), _ = new ei(0, 0), p = [1], g = this.canUseSpatialTree(i, s2), x3 = W();
      g && this.buildSpatialTree(x3, s2);
      const y = H();
      y.setCoords({ xmin: r.x, ymin: r.y, xmax: r.x, ymax: r.y });
      const P2 = n(m, this.createDistanceFunctor(r, l, _), false), S = h2.querySegmentIterator();
      if (S.stripAttributes(), g) n2(0);
      else for (; S.nextPath(); ) for (; S.hasNextSegment(); ) {
        const t = S.nextSegment(), i2 = this.findOrComputeBoxSegment(S.getStartPointIndex(), t, this.m_segmentBoxesB);
        if (this.m_envHelper.minDistanceGeodesic(y, i2) > c) continue;
        const s3 = this.calculateDistanceGeodeticPointSegment(e, r, t, p, P2);
        if (s3.second < c && (this.updateOutputSegment(a2, t, s3.first), c = s3.second, 0 === c)) return 0;
      }
      return c === this.m_maxDistance ? Number.POSITIVE_INFINITY : c;
    } catch (r) {
      m.error = r, m.hasError = true;
    } finally {
      s(m);
    }
  }
  calculateDistanceGeodeticMultipointGeometry(t, e, n4, o2) {
    switch (e.getGeometryType()) {
      case a.enumPoint:
        return this.calculateDistanceGeodeticPointMultipoint(e, t, o2, n4);
      case a.enumMultiPoint:
        return this.calculateDistanceGeodeticMultipointMultipoint(t, e, n4, o2);
      case a.enumPolyline:
      case a.enumPolygon:
      case a.enumEnvelope:
        return this.calculateDistanceGeodeticMultipointMultipath(t, e, n4, o2);
      default:
        P("");
    }
  }
  calculateDistanceGeodeticMultipointMultipoint(i, s2, n4, o2) {
    const a2 = { stack: [], error: void 0, hasError: false };
    try {
      const e = n(a2, new yc(), false);
      this.computeEnvelopesAndBoxes(i, s2);
      const m = this.estimateMinimumDistance(n4, o2);
      let r = m < this.m_maxDistance ? m : this.m_maxDistance;
      const u = new ei(), c = new ei(), h2 = i.getImpl(), d = s2.getImpl(), g = H(), x3 = h2.getPointCount(), y = d.getPointCount();
      for (let t = 0; t < x3; ++t) {
        if (u.assign(h2.getXY(t)), g.setCoords({ xmin: u.x, ymin: u.y, xmax: u.x, ymax: u.y }), this.m_envHelper.minDistanceGeodesic(g, this.m_boxGeomB) > r) continue;
        u.scale(this.m_rpu);
        const i2 = us(this.m_a, this.m_eSquared, u);
        for (let t2 = 0; t2 < y; ++t2) {
          c.assign(d.getXY(t2)), c.scale(this.m_rpu);
          const s3 = us(this.m_a, this.m_eSquared, c);
          if (!(X.distance(i2, s3) >= r) && (vc.geodeticDistance(this.m_a, this.m_eSquared, u.x, u.y, c.x, c.y, e, null, null, this.m_distCurveType), e.val < r && (n4.outPoint.assign(u), o2.outPoint.assign(c), r = e.val, 0 === r))) return r;
        }
      }
      return r === this.m_maxDistance ? Number.POSITIVE_INFINITY : r;
    } catch (m) {
      a2.error = m, a2.hasError = true;
    } finally {
      s(a2);
    }
  }
  calculateDistanceGeodeticMultipointMultipath(i, s2, o2, a2) {
    const m = { stack: [], error: void 0, hasError: false };
    try {
      const e = s2.getImpl().querySegmentIterator();
      e.stripAttributes(), this.computeEnvelopesAndBoxes(i, s2);
      const r = this.estimateMinimumDistance(o2, a2);
      let u = r < this.m_maxDistance ? r : this.m_maxDistance;
      const c = new ei(), h2 = new ei(0, 0), l = new ei(0, 0), _ = new ei(0, 0), p = [0], g = H(), x3 = n(m, this.createDistanceFunctor(h2, l, _), false), y = W(), P2 = this.canUseSpatialTree(i, s2);
      P2 && this.buildSpatialTree(y, s2);
      const S = i.getPointCount();
      for (let t = 0; t < S; ++t) if (c.assign(i.getXY(t)), h2.assign(i.getXY(t)), this.m_bIsPannablePcs && Yl(this.m_inputSR, [c], 1, false), g.setCoords({ xmin: h2.x, ymin: h2.y, xmax: h2.x, ymax: h2.y }), !(this.m_envHelper.minDistanceGeodesic(g, this.m_boxGeomB) > u)) if (x3.setPointDistFrom(h2), P2) n2(0);
      else {
        for (; e.nextPath(); ) for (; e.hasNextSegment(); ) {
          const t2 = e.nextSegment();
          if (this.m_envHelper.minDistanceGeodesic(g, this.findOrComputeBoxSegment(e.getStartPointIndex(), t2, this.m_segmentBoxesB)) > u) continue;
          const i2 = this.calculateDistanceGeodeticPointSegment(c, h2, t2, p, x3);
          if (i2.second < u && (o2.outPoint.assign(h2.mul(this.m_rpu)), this.updateOutputSegment(a2, t2, i2.first), u = i2.second, 0 === u)) return 0;
        }
        e.resetToFirstPath();
      }
      return u === this.m_maxDistance ? Number.POSITIVE_INFINITY : u;
    } catch (r) {
      m.error = r, m.hasError = true;
    } finally {
      s(m);
    }
  }
  calculateDistanceGeodeticMultipathGeometry(t, e, n4, o2) {
    switch (e.getGeometryType()) {
      case a.enumPoint:
        return this.calculateDistanceGeodeticPointMultipath(e, t, o2, n4);
      case a.enumMultiPoint:
        return this.calculateDistanceGeodeticMultipointMultipath(e, t, o2, n4);
      case a.enumPolyline:
      case a.enumPolygon:
      case a.enumEnvelope:
        return this.calculateDistanceGeodeticMultipathMultipath(t, e, n4, o2);
      default:
        P("");
    }
  }
  calculateDistanceGeodeticMultipathMultipath(t, e, i, s2) {
    const n4 = [t], o2 = [e], a2 = this.swapGeometries(n4, o2), m = n4[0], r = o2[0];
    this.computeEnvelopesAndBoxes(n4[0], o2[0]);
    const u = this.estimateMinimumDistance(i, s2), c = this.calculateDistanceGeodeticMultipathMultipath_(m, r, i, s2, u);
    return a2 && (s2.outPoint = Mt(i.outPoint, i.outPoint = s2.outPoint)), c;
  }
  calculateDistanceGeodeticMultipathMultipath_(i, s2, o2, a2, m = Number.MAX_VALUE) {
    const u = { stack: [], error: void 0, hasError: false };
    try {
      let e = m;
      const c = new ei(0, 0), h2 = new ei(), l = new ei(0, 0), _ = new ei(), p = new ei(0, 0), d = new ei(), y = new ei(), P2 = new ei(), S = [new ei()], f = [0], G = [0], D = H(), v2 = H(), T = n(u, this.createDistanceFunctor(p, c, l), false), b2 = W(), w2 = this.canUseSpatialTree(i, s2);
      w2 && this.buildSpatialTree(b2, s2);
      const E = s2.querySegmentIterator();
      E.stripAttributes();
      const R = new nu(), C = M(i);
      for (let t = 0; t < C.length; ++t) {
        const s3 = C[t];
        i.getSegmentBuffer(s3, R, true);
        const m2 = R.get();
        if (D.assign(this.findOrComputeBoxSegment(s3, m2, this.m_segmentBoxesA)), w2) n2(0);
        else {
          if (p.assign(m2.getStartXY()), d.assign(m2.getStartXY()), y.assign(m2.getEndXY()), P2.assign(m2.getEndXY()), this.m_bIsPannablePcs && (ig(this.m_inputSR, 0, [d], 1), ig(this.m_inputSR, 0, [P2], 1)), this.m_envHelper.minDistanceGeodesic(D, this.m_boxGeomB) > e) continue;
          for (; E.nextPath(); ) {
            for (; E.hasNextSegment(); ) {
              const t2 = E.nextSegment();
              if (c.assign(t2.getStartXY()), h2.assign(t2.getStartXY()), l.assign(t2.getEndXY()), _.assign(t2.getEndXY()), m2.intersect(t2, S, f, G, this.m_tolerance)) return this.updateOutputSegment(o2, m2, f[0]), this.updateOutputSegment(a2, t2, G[0]), 0;
              if (this.m_bIsPannablePcs && (ig(this.m_inputSR, 0, [h2], 1), ig(this.m_inputSR, 0, [_], 1)), v2.assign(this.findOrComputeBoxPoint(E.getStartPointIndex(), h2, _, this.m_segmentBoxesB)), this.m_envHelper.minDistanceGeodesic(D, v2) > e) continue;
              T.setSegmentEndPoints(c, l), T.setPointDistFrom(d);
              let i2 = ls(T.makeFunctor(), 0, 1, 1e-10);
              if (i2.second < e && (o2.outPoint.assign(d.mul(this.m_rpu)), this.updateOutputSegment(a2, t2, i2.first), e = i2.second, 0 === e)) break;
              if (T.setPointDistFrom(P2), i2 = ls(T.makeFunctor(), 0, 1, 1e-10), i2.second < e && (o2.outPoint.assign(P2.mul(this.m_rpu)), this.updateOutputSegment(a2, t2, i2.first), e = i2.second, 0 === e)) break;
              if (T.setSegmentEndPoints(p, y), T.setPointDistFrom(h2), i2 = ls(T.makeFunctor(), 0, 1, 1e-10), i2.second < e && (this.updateOutputSegment(o2, m2, i2.first), a2.outPoint.assign(h2.mul(this.m_rpu)), e = i2.second, 0 === e)) break;
              if (T.setPointDistFrom(_), i2 = ls(T.makeFunctor(), 0, 1, 1e-10), i2.second < e && (this.updateOutputSegment(o2, m2, i2.first), a2.outPoint.assign(_.mul(this.m_rpu)), e = i2.second, 0 === e)) break;
            }
            if (0 === e) return 0;
          }
          E.resetToFirstPath();
        }
      }
      return e === this.m_maxDistance ? Number.POSITIVE_INFINITY : e;
    } catch (c) {
      u.error = c, u.hasError = true;
    } finally {
      s(u);
    }
  }
  calculateDistanceGeodeticPointSegment(t, e, i, s2, n4) {
    const o2 = i.getStartXY(), a2 = i.getEndXY();
    return i.intersectPoint(t, s2, this.m_tolerance) ? Qt(s2[0], 0) : (n4.setPointDistFrom(e), n4.setSegmentEndPoints(o2, a2), ls(n4.makeFunctor(), 0, 1, 1e-10));
  }
  prepareGeometry(t) {
    let e = t.clone();
    const i = t.getGeometryType();
    if (i === a.enumPoint || i === a.enumMultiPoint) return this._NormalizeAndProject(e);
    if (i === a.enumEnvelope && (e = this.envelopeToPolygon(e)), this.m_inputSR.isPannable()) return this.prepareGeometryPannable(e, 4);
    if (2 === this.m_inputSR.getCoordinateSystemType()) {
      if (e = Ol(e, this.m_inputSR, 0, null), e.isEmpty()) return e;
      e = new o().execute(e, this.m_inputSR, 5e4, this.m_maxDeviation / 2, 0, null);
      const t2 = this.m_inputSR.getSRToGCSTransform();
      return new kh().execute(e, t2, this.m_progressTracker);
    }
    const n4 = new n3();
    e.queryLooseEnvelope(n4);
    const o2 = this.m_inputSR.getPannableExtent();
    if (n4.ymin < o2.ymin || n4.ymax > o2.ymax) {
      const t2 = new n3(n4.xmin - 1, o2.ymin, n4.xmax + 1, o2.ymax);
      if (e = new I().execute(e, t2, this.m_inputSR, this.m_progressTracker), e.isEmpty()) return e;
    }
    return n4.width() > 2 * o2.width() ? (e = kl(e, -2 * o2.width(), 2 * o2.width(), this.m_inputSR, true, 0, true, this.m_progressTracker), e) : e;
  }
  prepareGeometryPannable(t, e) {
    const i = new n3();
    t.queryEnvelope(i);
    const s2 = this.m_inputSR.getPannableExtent();
    if (s2.containsEnvelope(i)) return t;
    const n4 = new x();
    s2.queryIntervalX(n4);
    const o2 = new x();
    return i.queryIntervalX(o2), t = n4.contains(o2) ? Vl(t, this.m_inputSR) : new kh().foldInto360RangeGeodetic(t, this.m_inputSR, e);
  }
  prepareOutput(t) {
    t.outPoint.scale(1 / this.m_rpu), 2 === this.m_inputSR.getCoordinateSystemType() && Yl(this.m_inputSR, [t.outPoint], 1, false);
  }
  updateOutputSegment(t, e, i) {
    e.queryCoord2D(i, t.outPoint), this.m_bIsPannablePcs && ig(this.m_inputSR, 0, [t.outPoint], 1), t.outPoint.mulThis(this.m_rpu);
  }
  computeTolerance(t, e) {
    t.queryEnvelope(this.m_envGeomA), e.queryEnvelope(this.m_envGeomB);
    const i = new n3(this.m_envGeomA);
    return i.mergeEnvelope2D(this.m_envGeomB), Bt(null, i);
  }
  envelopeToPolygon(t) {
    const e = new eh();
    return e.addEnvelope(t, false), e;
  }
  computeEnvelopesAndBoxes(t, e) {
    t.queryEnvelope(this.m_envGeomA), e.queryEnvelope(this.m_envGeomB), this.m_boxGeomA = this.computeBoxGeometry(t, this.m_boundaryPtsA, this.m_segmentBoxesA), this.m_boxGeomB = this.computeBoxGeometry(e, this.m_boundaryPtsB, this.m_segmentBoxesB);
  }
  computeBoxGeometry(t, e, i) {
    switch (t.getGeometryType()) {
      case a.enumPoint:
        return this.computeBoxPoint(t, e);
      case a.enumMultiPoint:
        return this.computeBoxMultiPoint(t, e);
      case a.enumPolyline:
      case a.enumPolygon:
        return this.computeBoxMultiPath(t, e, i);
      default:
        b("Invalid geometry type");
    }
  }
  computeBoxPoint(t, e) {
    const i = t.getXY();
    e.length = 0, e.push(i.mul(this.m_rpu));
    const s2 = H();
    return s2.setCoords({ xmin: i.x, ymin: i.y, xmax: i.x, ymax: i.y }), s2;
  }
  computeBoxMultiPoint(t, e) {
    let i = true;
    for (let n4 = 0; n4 < t.getPointCount(); ++n4) {
      const s3 = t.getXY(n4);
      if (i) {
        e.length = 0;
        for (let t2 = 0; t2 < 4; ++t2) e.push(s3.clone());
        i = false;
      }
      s3.x < e[0].x && e[0].assign(s3), s3.y > e[1].y && e[1].assign(s3), s3.x > e[2].x && e[2].assign(s3), s3.y < e[3].y && e[3].assign(s3);
    }
    for (let n4 = 0; n4 < e.length; ++n4) e[n4].mulThis(this.m_rpu);
    const s2 = H();
    return t.queryEnvelope(s2), s2;
  }
  computeBoxMultiPath(t, e, i) {
    let s2 = true;
    const n4 = H();
    n4.setEmpty();
    const o2 = t.querySegmentIterator();
    for (o2.stripAttributes(); o2.nextPath(); ) for (; o2.hasNextSegment(); ) {
      const t2 = o2.nextSegment(), a2 = t2.getStartXY(), m = t2.getEndXY();
      if (this.m_bIsPannablePcs && (ig(this.m_inputSR, 0, [a2], 1), ig(this.m_inputSR, 0, [m], 1)), s2) {
        for (let t3 = 0; t3 < 4; ++t3) e.push(a2.clone());
        s2 = false;
      }
      a2.x < e[0].x && e[0].assign(a2), a2.y > e[1].y && e[1].assign(a2), a2.x > e[2].x && e[2].assign(a2), a2.y < e[3].y && e[3].assign(a2), m.x < e[0].x && e[0].assign(m), m.y > e[1].y && e[1].assign(m), m.x > e[2].x && e[2].assign(m), m.y < e[3].y && e[3].assign(m);
      const r = this.findOrComputeBoxPoint(o2.getStartPointIndex(), a2, m, i);
      n4.mergeEnvelope2D(r);
    }
    for (let a2 = 0; a2 < e.length; ++a2) e[a2].mulThis(this.m_rpu);
    return n4;
  }
  findOrComputeBoxSegment(t, e, i) {
    if (!i.has(t)) {
      let s2 = H();
      if (e.queryEnvelope(s2), this.m_bIsPannablePcs) {
        const t2 = ei.construct(s2.xmin, s2.ymin), e2 = ei.construct(s2.xmax, s2.ymax);
        ig(this.m_inputSR, 0, [t2], 1), ig(this.m_inputSR, 0, [e2], 1), s2 = this.m_envHelper.calculateBbox(t2.x, t2.y, e2.x, e2.y);
      }
      return i.set(t, s2), s2;
    }
    return i.get(t);
  }
  findOrComputeBoxPoint(t, e, i, s2) {
    if (!s2.has(t)) {
      let n4 = H();
      return n4.setCoords({ xmin: e.x, ymin: e.y, xmax: i.x, ymax: i.y }), this.m_bIsPannablePcs && (n4 = this.m_envHelper.calculateBbox(n4.xmin, n4.ymin, n4.xmax, n4.ymax)), s2.set(t, n4), n4;
    }
    return s2.get(t);
  }
  estimateMinimumDistance(i, s2) {
    const n4 = { stack: [], error: void 0, hasError: false };
    try {
      const e = (t) => us(this.m_a, this.m_eSquared, t), o2 = this.m_boundaryPtsA.map(e), a2 = this.m_boundaryPtsB.map(e);
      let m = Number.MAX_VALUE, r = 0, u = 0;
      for (let t = 0; t < o2.length; ++t) for (let e2 = 0; e2 < a2.length; ++e2) {
        const i2 = X.distance(o2[t], a2[e2]);
        i2 < m && (m = i2, r = t, u = e2);
      }
      i.outPoint.assign(this.m_boundaryPtsA[r]), s2.outPoint.assign(this.m_boundaryPtsB[u]);
      const c = n(n4, new yc(), false);
      return vc.geodeticDistance(this.m_a, this.m_eSquared, this.m_boundaryPtsA[r].x, this.m_boundaryPtsA[r].y, this.m_boundaryPtsB[u].x, this.m_boundaryPtsB[u].y, c, null, null, this.m_distCurveType), c.val;
    } catch (o2) {
      n4.error = o2, n4.hasError = true;
    } finally {
      s(n4);
    }
  }
  swapGeometries(t, e) {
    return w(t[0]) > w(e[0]) && (e[0] = Mt(t[0], t[0] = e[0]), this.m_envGeomB = Mt(this.m_envGeomA, this.m_envGeomA = this.m_envGeomB), true);
  }
  canUseSpatialTree(t, e) {
    return false;
  }
  checkGeometriesIntersect(t, e, i, n4) {
    let o2 = t.getGeometryType(), a2 = e.getGeometryType();
    if (this.m_envGeomA.isIntersecting(this.m_envGeomB)) {
      if (o2 === a.enumPoint && 2 === e.getDimension()) {
        const s2 = [0], o3 = [t.getXY()];
        if (Xo(e, o3, 1, this.m_tolerance, s2), 0 !== s2[0]) return i.outPoint.assign(o3[0]), n4.outPoint.assign(o3[0]), true;
      } else if (a2 === a.enumPoint && 2 === t.getDimension()) {
        const s2 = [2], o3 = [e.getXY()];
        if (Xo(t, o3, 1, this.m_tolerance, s2), 0 !== s2[0]) return i.outPoint.assign(o3[0]), n4.outPoint.assign(o3[0]), true;
      }
      if (o2 === a.enumMultiPoint && 2 === e.getDimension()) return this.multipointIntersectsArea(t, e, i, n4);
      if (a2 === a.enumMultiPoint && 2 === t.getDimension()) return this.multipointIntersectsArea(e, t, n4, i);
      let r, u, c = t, h2 = e;
      if (o2 === a.enumEnvelope && (r = this.envelopeToPolygon(t), c = r, o2 = a.enumPolygon), a2 === a.enumEnvelope && (u = this.envelopeToPolygon(e), h2 = u, a2 = a.enumPolygon), h(o2) && h(a2)) {
        const t2 = c.querySegmentIterator(), e2 = h2.querySegmentIterator(), s2 = v(), o3 = v();
        if (q(c, h2, t2, e2, s2, o3)) return i.outPoint.assign(s2.outPoint), n4.outPoint.assign(s2.outPoint), true;
      }
    }
    return false;
  }
  multipointIntersectsArea(t, e, i, s2) {
    const n4 = t.getPointCount();
    for (let o2 = 0; o2 < n4; ++o2) {
      const n5 = [2], a2 = t.getXY(o2);
      if (Xo(e, [a2], 1, this.m_tolerance, n5), 0 !== n5[0]) return i.outPoint.assign(a2), s2.outPoint.assign(a2), true;
    }
    return false;
  }
  getNearestNeighbourVisitor(t, e, i, s2, o2, a2, m) {
    return n2(0), {};
  }
  buildSpatialTree(t, e) {
    n2(0);
  }
};

export {
  L,
  Q,
  J
};
//# sourceMappingURL=chunk-RHVBZJH7.js.map
