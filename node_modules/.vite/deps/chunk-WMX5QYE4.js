import {
  r
} from "./chunk-SCZRL2WP.js";
import {
  g
} from "./chunk-YZLMZUTQ.js";
import {
  m
} from "./chunk-HITYQXMG.js";
import {
  a2 as a
} from "./chunk-4XNJLQ7P.js";

// node_modules/@arcgis/core/widgets/ScaleRangeSlider/ScaleRanges.js
var i;
var c = 147914382;
var l = [{ id: "room", minScale: 100 }, { id: "rooms", minScale: 400 }, { id: "smallBuilding", minScale: 800 }, { id: "building", minScale: 1999 }, { id: "buildings", minScale: 3999 }, { id: "street", minScale: 7499 }, { id: "streets", minScale: 14999 }, { id: "neighborhood", minScale: 29999 }, { id: "town", minScale: 59999 }, { id: "city", minScale: 119999 }, { id: "cities", minScale: 249999 }, { id: "metropolitanArea", minScale: 499999 }, { id: "county", minScale: 999999 }, { id: "counties", minScale: 1999999 }, { id: "stateProvince", minScale: 3999999 }, { id: "statesProvinces", minScale: 6999999 }, { id: "countriesSmall", minScale: 14999999 }, { id: "countriesBig", minScale: 34999999 }, { id: "continent", minScale: 99999999 }, { id: "world", minScale: c }];
function r2(e, n) {
  const t = l, a2 = t.length, i2 = [];
  let c2, r3 = e;
  e = e >= 0 ? e : 0, n = n > 0 ? n : t[a2 - 1].minScale;
  for (let l2 = 0; l2 < a2; l2++) {
    const a3 = Math.min(t[l2].minScale, n);
    c2 = Math.min(a3, n), e <= a3 && r3 < n && i2.push({ id: t[l2].id, maxScale: Math.max(r3, e), minScale: c2 }), r3 = c2 + 1;
  }
  return i2.reverse(), s(i2);
}
function s(e) {
  if (0 === e.length) return e;
  const [n] = e, t = e[e.length - 1];
  return n.minScale === n.maxScale && (e.shift(), n.minScale += 1), t.minScale === t.maxScale && (e.pop(), t.maxScale -= 1), e;
}
var o = i = class extends g {
  constructor() {
    super(...arguments), this.ranges = [];
  }
  static fromScaleRange({ maxScale: e, minScale: n }) {
    return new i({ ranges: r2(e, n) });
  }
  static async fromLayer(e) {
    var _a;
    await e.when();
    const n = "tileInfo" in e ? (_a = e.tileInfo) == null ? void 0 : _a.lods : void 0;
    let t;
    if (n) {
      t = r2(n[n.length - 1].scale, n[0].scale);
    } else t = r2(0, 0);
    return new i({ ranges: t });
  }
  get firstRange() {
    return this.ranges[0];
  }
  get lastRange() {
    const e = this.ranges;
    return e[e.length - 1];
  }
  get length() {
    return this.ranges.length;
  }
  get maxScale() {
    return this.lastRange.maxScale;
  }
  get minScale() {
    return this.firstRange.minScale;
  }
  isMinScaleEdge(e) {
    const { firstRange: n } = this, t = n.minScale, a2 = i.RecommendedScales[n.id] || n.maxScale;
    return e <= t && e >= a2;
  }
  isMaxScaleEdge(e) {
    const { lastRange: n } = this, t = n.maxScale;
    return e <= (i.RecommendedScales[n.id] || n.minScale) && e >= t;
  }
  findScaleRange(e) {
    const n = this.ranges;
    let t;
    if (e >= n[0].maxScale) return n[0];
    if (e <= n[n.length - 1].minScale) return n[n.length - 1];
    for (let a2 = 0; a2 < n.length; a2++) {
      const i2 = n[a2];
      if (e >= i2.maxScale && e <= i2.minScale) {
        t = i2;
        break;
      }
    }
    return t;
  }
  findScaleRangeByIndex(e) {
    return this.ranges[this._clampScaleRangeIndex(e)];
  }
  scaleToRangeIndex(e) {
    return this.ranges.indexOf(this.findScaleRange(e));
  }
  clampScale(e) {
    return Math.min(this.minScale, Math.max(this.maxScale, e));
  }
  clampMinScale(e) {
    return 0 === e ? this.minScale : this.clampScale(e);
  }
  clampMaxScale(e) {
    return this.clampScale(e);
  }
  contains(e) {
    const n = this.ranges;
    let t = false;
    for (let a2 = 0; a2 < n.length; a2++) {
      const { maxScale: i2, minScale: c2 } = n[a2];
      if (e >= i2 && e <= c2) {
        t = true;
        break;
      }
    }
    return t;
  }
  _clampScaleRangeIndex(e) {
    const n = 0;
    if (e <= n) return n;
    const t = this.ranges.length - 1;
    return e > t ? t : Math.floor(e);
  }
};
o.RecommendedScales = Object.freeze({ world: c, continent: 5e7, countriesBig: 25e6, countriesSmall: 12e6, statesProvinces: 6e6, stateProvince: 3e6, counties: 15e5, county: 75e4, metropolitanArea: 32e4, cities: 16e4, city: 8e4, town: 4e4, neighborhood: 2e4, streets: 1e4, street: 5e3, buildings: 2500, building: 1250, smallBuilding: 800, rooms: 400, room: 100 }), r([m()], o.prototype, "firstRange", null), r([m()], o.prototype, "lastRange", null), r([m()], o.prototype, "length", null), r([m()], o.prototype, "maxScale", null), r([m()], o.prototype, "minScale", null), r([m({ constructOnly: true })], o.prototype, "ranges", void 0), o = i = r([a("esri.widgets.ScaleRangeSlider.ScaleRanges")], o);
var m2 = o;

export {
  m2 as m
};
//# sourceMappingURL=chunk-WMX5QYE4.js.map
