import {
  m as m2
} from "./chunk-WMX5QYE4.js";
import {
  l
} from "./chunk-MQPF3LCI.js";
import {
  P,
  d
} from "./chunk-QXWM4LT3.js";
import {
  r
} from "./chunk-SCZRL2WP.js";
import {
  g
} from "./chunk-YZLMZUTQ.js";
import {
  m
} from "./chunk-HITYQXMG.js";
import {
  a2 as a
} from "./chunk-4XNJLQ7P.js";

// node_modules/@arcgis/core/widgets/ScaleRangeSlider/ScaleRangeSliderViewModel.js
var c = class extends g {
  constructor(e) {
    super(e), this.layer = null, this.mode = "range", this.scaleRanges = m2.fromScaleRange({ minScale: 0, maxScale: 0 }), this.sliderViewModel = (() => {
      const { max: e2 } = this._getSliderIndexRange(this.scaleRanges.length - 1);
      return new l({ precision: 10, min: 0, max: e2, values: [0, e2] });
    })();
  }
  initialize() {
    this.addHandles([d(() => this.layer, (e) => e == null ? void 0 : e.load().catch(() => {
    })), d(() => this.mode, (e) => {
      var _a, _b;
      "range" === e && 1 === ((_a = this.sliderViewModel.values) == null ? void 0 : _a.length) ? this.sliderViewModel.values = [this.sliderViewModel.min, this.sliderViewModel.values[0]] : 2 === ((_b = this.sliderViewModel.values) == null ? void 0 : _b.length) && ("min-scale-only" === e ? this.sliderViewModel.values = [this.sliderViewModel.values[0]] : "max-scale-only" === e && (this.sliderViewModel.values = [this.sliderViewModel.values[1]]));
    }), d(() => {
      var _a, _b;
      return { loaded: (_a = this.layer) == null ? void 0 : _a.loaded, ready: (_b = this.view) == null ? void 0 : _b.ready };
    }, ({ loaded: e, ready: i }) => {
      if (!e || !i) return;
      if (this._hasTiledLayer()) {
        const e2 = this._getLayerResampling() ? void 0 : this.mapScaleToSlider(this._getTiledLayerMaxScale()), i2 = this.mapScaleToSlider(this._getTiledLayerMinScale());
        this.sliderViewModel.effectiveMax = e2, this.sliderViewModel.effectiveMin = i2;
      } else {
        const e2 = this.layer && "maxScaleRange" in this.layer ? this.layer.maxScaleRange : null, { minScale: i2 = 0, maxScale: a3 = 0 } = e2 ?? {};
        this.sliderViewModel.effectiveMax = 0 === a3 ? void 0 : this.mapScaleToSlider(a3), this.sliderViewModel.effectiveMin = 0 === i2 ? void 0 : this.mapScaleToSlider(i2);
      }
      const a2 = this.layer;
      a2 && "minScale" in a2 && "maxScale" in a2 ? (this.minScale = a2.minScale, this.maxScale = a2.maxScale) : (this.minScale = void 0, this.maxScale = void 0);
    }, P)]);
  }
  get effectiveMaxScale() {
    return 0 === this.maxScale ? this.maxScaleLimit : this.maxScale;
  }
  get effectiveMinScale() {
    return 0 === this.minScale ? this.minScaleLimit : this.minScale;
  }
  get effectiveMaxScaleLimit() {
    return this.mapSliderToScale(this.sliderViewModel.effectiveMax ?? this.sliderViewModel.max);
  }
  get effectiveMinScaleLimit() {
    return this.mapSliderToScale(this.sliderViewModel.effectiveMin ?? this.sliderViewModel.min);
  }
  get maxScale() {
    return this._getScale("max");
  }
  set maxScale(e) {
    this._setMaxScaleOnSlider(e);
  }
  get maxScaleLimit() {
    return this.mapSliderToScale(this.sliderViewModel.max);
  }
  set maxScaleLimit(e) {
    this._setSliderRange({ maxScale: e, minScale: this.minScaleLimit });
  }
  get minScale() {
    return this._getScale("min");
  }
  set minScale(e) {
    this._setMinScaleOnSlider(e);
  }
  get minScaleLimit() {
    return this.mapSliderToScale(this.sliderViewModel.min);
  }
  set minScaleLimit(e) {
    this._setSliderRange({ maxScale: this.maxScaleLimit, minScale: e });
  }
  get state() {
    const { view: e, layer: i } = this;
    return !e && !i || !e && (i == null ? void 0 : i.loaded) || !i && (e == null ? void 0 : e.ready) || e && e.ready && (i == null ? void 0 : i.loaded) ? "ready" : "disabled";
  }
  set view(e) {
    this._set("view", e);
  }
  mapScaleToSlider(e) {
    const i = this.scaleRanges.scaleToRangeIndex(e), a2 = this.scaleRanges.findScaleRangeByIndex(i), { maxScale: l2, minScale: t } = a2, { max: s, min: r2 } = this._getSliderIndexRange(i);
    return this._mapToRange(e, t, l2, r2, s);
  }
  mapSliderToScale(e) {
    const i = this.scaleRanges.findScaleRangeByIndex(e), { maxScale: a2, minScale: l2 } = i, { max: t, min: s } = this._getSliderIndexRange(e);
    return this._mapToRange(e, s, t, l2, a2);
  }
  _setSliderRange(e) {
    this.scaleRanges = m2.fromScaleRange(e);
    const { max: i } = this._getSliderIndexRange(this.scaleRanges.length - 1);
    this.sliderViewModel.max = i, this.sliderViewModel.min = 0, this.notifyChange("maxScaleLimit"), this.notifyChange("minScaleLimit");
  }
  _getSliderIndexRange(e) {
    const i = Math.floor(e);
    return { min: i, max: i + 0.99999 };
  }
  _mapToRange(e, i, a2, l2, t) {
    return l2 + (e - i) * (t - l2) / (a2 - i);
  }
  _getSliderValue(e) {
    const { min: i, max: a2, values: l2 } = this.sliderViewModel, [t, s] = l2;
    switch (this.mode) {
      case "max-scale-only":
        return "min" === e ? i : t;
      case "min-scale-only":
        return "min" === e ? t : a2;
      default:
        return "min" === e ? t ?? i : s ?? a2;
    }
  }
  _getScale(e) {
    const i = this.mapSliderToScale(this._getSliderValue(e));
    return this._normalizeScale(e, i);
  }
  _setMaxScaleOnSlider(e) {
    const { scaleRanges: i, sliderViewModel: a2 } = this;
    if (void 0 !== e) {
      const l2 = this.mapScaleToSlider(this._constrainMaxScaleToLayer(i.clampMaxScale(e)));
      switch (this.mode) {
        case "range":
          a2.values = [a2.values[0], l2];
          break;
        case "max-scale-only":
          a2.values = [l2];
      }
    }
  }
  _setMinScaleOnSlider(e) {
    const { scaleRanges: i, sliderViewModel: a2 } = this;
    if (void 0 !== e) {
      const l2 = this.mapScaleToSlider(this._constrainMinScaleToLayer(i.clampMinScale(e)));
      switch (this.mode) {
        case "range":
          a2.values = [l2, a2.values[1]];
          break;
        case "min-scale-only":
          a2.values = [l2];
      }
    }
  }
  _constrainMinScaleToLayer(e) {
    const { scaleRanges: i } = this;
    if (this._hasTiledLayer()) {
      const { firstRange: a2 } = i, l2 = this._getTiledLayerMinScale(), t = 0.85;
      e = this._mapToRange(e, a2.maxScale, a2.minScale, 0, 1) > t || e > l2 ? l2 : e;
    }
    return e;
  }
  _constrainMaxScaleToLayer(e) {
    if (this._hasTiledLayer() && !this._getLayerResampling()) {
      const i = this._getTiledLayerMaxScale();
      e = e < i ? i : e;
    }
    return e;
  }
  _normalizeScale(e, i) {
    const a2 = "max" === e ? "maxScale" : "minScale", l2 = this._hasTiledLayer() ? "min" === e ? this._getTiledLayerMinScale() : this._getTiledLayerMaxScale() : this.scaleRanges[a2], t = this._hasTiledLayer() ? "min" === e ? this._getTiledLayerRealMinScale() : this._getTiledLayerRealMaxScale() : l2, s = 1e-6, r2 = 0 === i || t === i || Math.abs(t - i) <= s;
    return Number((r2 ? 0 : i).toFixed(6));
  }
  _getLayerLODS() {
    var _a, _b;
    const { layer: e } = this;
    if (!(e == null ? void 0 : e.loaded) || !("tileInfo" in e)) return null;
    return "wcs" === e.type || "imagery-tile" === e.type && "Raster" === ((_a = e.raster) == null ? void 0 : _a.tileType) ? null : (_b = e.tileInfo) == null ? void 0 : _b.lods;
  }
  _getLayerResampling() {
    var _a;
    return !!((_a = this.layer) == null ? void 0 : _a.loaded) && (!("resampling" in this.layer) || this.layer.resampling);
  }
  _getTiledLayerMinScale() {
    const e = this._getLayerLODS();
    return this.scaleRanges.clampMinScale(e[0].scale);
  }
  _getTiledLayerMaxScale() {
    const e = this._getLayerLODS();
    return e[e.length - 1].scale;
  }
  _getTiledLayerRealMinScale() {
    var _a, _b;
    const e = this._getLayerLODS(), i = this.layer, a2 = i && "sourceJSON" in i ? ((_b = (_a = i.sourceJSON) == null ? void 0 : _a.tileInfo) == null ? void 0 : _b.lods) ?? e : e;
    return this.scaleRanges.clampMinScale(a2[0].scale);
  }
  _getTiledLayerRealMaxScale() {
    var _a, _b;
    const e = this._getLayerLODS(), i = this.layer, a2 = i && "sourceJSON" in i ? ((_b = (_a = i.sourceJSON) == null ? void 0 : _a.tileInfo) == null ? void 0 : _b.lods) ?? e : e;
    return a2[a2.length - 1].scale;
  }
  _hasTiledLayer() {
    return !!this._getLayerLODS();
  }
};
r([m({ readOnly: true })], c.prototype, "effectiveMaxScale", null), r([m({ readOnly: true })], c.prototype, "effectiveMinScale", null), r([m()], c.prototype, "effectiveMaxScaleLimit", null), r([m()], c.prototype, "effectiveMinScaleLimit", null), r([m()], c.prototype, "layer", void 0), r([m()], c.prototype, "maxScale", null), r([m()], c.prototype, "maxScaleLimit", null), r([m()], c.prototype, "mode", void 0), r([m()], c.prototype, "minScale", null), r([m()], c.prototype, "minScaleLimit", null), r([m()], c.prototype, "scaleRanges", void 0), r([m({ type: l })], c.prototype, "sliderViewModel", void 0), r([m({ readOnly: true })], c.prototype, "state", null), r([m()], c.prototype, "view", null), c = r([a("esri.widgets.ScaleRangeSlider.ScaleRangeSliderViewModel")], c);
var o = c;

export {
  o
};
//# sourceMappingURL=chunk-7XINCNCX.js.map
