import {
  l
} from "./chunk-L47X4E4Y.js";
import {
  e,
  i,
  r as r2,
  s
} from "./chunk-TJJJEFNQ.js";
import {
  V
} from "./chunk-6UTUOXT5.js";
import {
  c,
  e as e2
} from "./chunk-EMTJUW7P.js";
import {
  p
} from "./chunk-VPARD4XG.js";
import {
  _
} from "./chunk-2QY274QD.js";
import {
  n
} from "./chunk-5AW3ASC5.js";
import {
  j
} from "./chunk-DJTR6MPM.js";
import {
  r
} from "./chunk-SCZRL2WP.js";
import {
  m
} from "./chunk-HITYQXMG.js";
import {
  a2 as a
} from "./chunk-4XNJLQ7P.js";

// node_modules/@arcgis/core/views/draw/PolygonDrawAction.js
var u = class extends V {
  constructor(e3) {
    super(e3), this._panEnabled = false, this._popVertexOnPointerMove = false, this._addVertexOnPointerUp = false, this._drawTool = null, this.mode = e2;
  }
  initialize() {
    this._addViewHandles(), "3d" === this.view.type && this._addDrawTool();
  }
  destroy() {
    this._removeDrawTool(), this.emit("destroy");
  }
  get _dragEnabled() {
    return "freehand" === this.mode || "hybrid" === this.mode;
  }
  get _clickEnabled() {
    return "click" === this.mode || "hybrid" === this.mode;
  }
  undo() {
    this._drawTool ? this._drawTool.undo() : (super.undo(), this.notifyChange("vertices"));
  }
  redo() {
    this._drawTool ? this._drawTool.redo() : (super.redo(), this.notifyChange("vertices"));
  }
  canUndo() {
    var _a;
    return ((_a = this._drawTool) == null ? void 0 : _a.canUndo()) ?? super.canUndo();
  }
  canRedo() {
    var _a;
    return ((_a = this._drawTool) == null ? void 0 : _a.canRedo()) ?? super.canRedo();
  }
  complete() {
    this._completeDrawing();
  }
  _getGeometryZValue() {
    return this.hasZ && this.vertices.length > 0 ? this.vertices[0][2] : this.defaultZ;
  }
  _addViewHandles() {
    this._addViewHandles2DOnly(), this.addHandles(this.view.on("key-down", (e3) => {
      const { key: t, repeat: i2 } = e3;
      t !== p.vertexAdd || i2 ? t !== p.complete || i2 ? t !== p.undo || this.interactiveUndoDisabled || i2 ? t !== p.redo || this.interactiveUndoDisabled || i2 || (e3.stopPropagation(), this.redo()) : (e3.stopPropagation(), this.undo()) : (e3.stopPropagation(), this._drawCompleteHandler(e3)) : (e3.stopPropagation(), this._handleVertexAddKey(e3));
    }, _.TOOL));
  }
  _addViewHandles2DOnly() {
    "2d" === this.view.type && (this.addHandles([this.view.on("click", (e3) => {
      e3.stopPropagation();
    }, _.TOOL), this.view.on("pointer-down", (e3) => {
      this._shouldHandlePointerEvent(e3) && (this._abortSnapping(), this._activePointerId = e3.pointerId, this._addVertexOnPointerUp = true, this._cursorScreenPoint = n(e3), "touch" === e3.pointerType && this._updateCursor());
    }, _.TOOL), this.view.on("pointer-move", (e3) => {
      this._abortSnapping(), this._popVertexOnPointerMove && (this.undo(), this._popVertexOnPointerMove = false), this._cursorScreenPoint = n(e3), "touch" !== e3.pointerType && this._updateCursor();
    }, _.TOOL), this.view.on("pointer-drag", (e3) => {
      this._shouldHandlePointerEvent(e3) && (this._abortSnapping(), this._cursorScreenPoint = n(e3), this._dragEnabled && !this._panEnabled ? this._vertexAddHandler(e3) : this._addVertexOnPointerUp = false);
    }, _.TOOL), this.view.on("pointer-up", (e3) => {
      if (this._shouldHandlePointerEvent(e3)) if (this._abortSnapping(), this._activePointerId = null, this._addVertexOnPointerUp) {
        if (!this._clickEnabled) return 1 === this.vertices.length && this.vertices.pop(), void this._drawCompleteHandler(e3);
        this._vertexAddHandler(e3);
      } else {
        const t = "touch" === e3.pointerType;
        this._updateCursor(t);
      }
    }, _.TOOL), this.view.on("drag", (e3) => {
      this._dragEnabled && null != this._activePointerId && !this._panEnabled && e3.stopPropagation();
    }, _.TOOL), this.view.on("drag", ["Shift"], (e3) => {
      e3.stopPropagation();
    }, _.TOOL), this.view.on("double-click", (e3) => {
      e3.stopPropagation(), this._drawCompleteHandler(e3);
    }, _.TOOL), this.view.on("double-click", ["Control"], (e3) => {
      e3.stopPropagation(), this._drawCompleteHandler(e3);
    }, _.TOOL), this.view.on("key-down", (e3) => {
      const { key: t, repeat: i2 } = e3;
      t !== p.pan || i2 || (e3.stopPropagation(), this._panEnabled = true);
    }, _.TOOL), this.view.on("key-up", (e3) => {
      e3.key === p.pan && (e3.stopPropagation(), this._panEnabled = false);
    }, _.TOOL)]), this._addUndoRedoHandles());
  }
  _handleVertexAddKey(e3) {
    this._drawTool ? this._drawTool.drawOperation.commitStagedVertex() : this._cursorScreenPoint && (this._abortSnapping(), this._vertexAddHandler(e3));
  }
  _addUndoRedoHandles() {
    this.addHandles([this._editGeometryOperations.on("vertex-remove", (e3) => {
      if (this.notifyChange("vertices"), "undo" === e3.operation) {
        const t = [...this._committedVertices];
        null != this._stagedVertex && t.push(this._coordinateHelper.pointToArray(this._stagedVertex)), this.emit("undo", new s(this.view, e3.vertices[0].index, t));
      }
    }), this._editGeometryOperations.on("vertex-add", (e3) => {
      if (this.notifyChange("vertices"), "apply" === e3.operation) {
        const e4 = this._committedVertices.length - 1, t = new e(this.view, e4, this.vertices);
        this.emit("vertex-add", t), t.defaultPrevented && (this._popVertexOnPointerMove = true);
      } else if ("redo" === e3.operation) {
        const t = [...this._committedVertices];
        null != this._stagedVertex && t.push(this._coordinateHelper.pointToArray(this._stagedVertex)), this.emit("redo", new e(this.view, e3.vertices[0].index, t));
      }
    })]);
  }
  _addDrawTool() {
    const e3 = new l({ view: this.view, elevationInfo: this.elevationInfo, hasZ: this.hasZ, geometryType: "polygon", mode: this.mode });
    this._drawTool = e3, this.view.addAndActivateTool(e3), e3.on("vertex-add", (t) => {
      if (1 === t.vertices.length) {
        const { view: i2 } = this, o = t.vertices[0].vertexIndex, r3 = e3.getVertexCoords();
        this.emit("vertex-add", new e(i2, o, r3)), "undo" !== t.operation && "redo" !== t.operation || this.emit(t.operation, new e(i2, o, r3));
      }
    }), e3.on("vertex-remove", (t) => {
      if (1 === t.vertices.length) {
        const { view: i2 } = this, o = t.vertices[0].vertexIndex, r3 = e3.getVertexCoords();
        this.emit("vertex-remove", new s(i2, o, r3)), "undo" !== t.operation && "redo" !== t.operation || this.emit(t.operation, new s(i2, o, r3));
      }
    }), e3.on("cursor-update", (t) => {
      1 === t.vertices.length && this.emit("cursor-update", new i(this.view, t.vertices[0].vertexIndex, e3.getVertexCoords()));
    }), e3.on("complete", (t) => {
      this.emit("draw-complete", new r2(e3.getVertexCoords())), this._removeDrawTool(), this.removeAllHandles();
    });
  }
  _removeDrawTool() {
    this._drawTool && (this.view.tools.remove(this._drawTool), this._drawTool = null);
  }
  _addVertex(e3) {
    const t = this._coordinateHelper.arrayToVector(e3);
    this._isDuplicateOfLastVertex(t) || (this._lastVertexUnsnapped = this._stagedVertexUnsnapped, this._popCursorVertex(), this._editGeometryOperations.appendVertex(t));
  }
  _updateCursor(e3 = false) {
    if (this._popCursorVertex(), !this._cursorScreenPoint) return;
    const t = this.getCoordsAndPointFromScreenPoint(this._cursorScreenPoint);
    null == t || e3 || this._pushCursorVertex(t.vertex, () => this.emit("cursor-update", new i(this.view, this._activeComponent.vertices.length, this.vertices, null != this._stagedVertex ? new j(this._stagedVertex) : null)));
  }
  _completeDrawing() {
    if (this._drawTool) return void this._drawTool.completeCreateOperation();
    if (this._activePointerId = null, this._popCursorVertex(), this._committedVertices.length < 3) return;
    this._abortSnapping(), null != this._snappingManager && this._snappingManager.doneSnapping();
    const e3 = new r2(this.vertices);
    this.emit("draw-complete", e3), e3.defaultPrevented || this.removeAllHandles();
  }
};
r([m()], u.prototype, "_dragEnabled", null), r([m()], u.prototype, "_clickEnabled", null), r([m({ type: c })], u.prototype, "mode", void 0), u = r([a("esri.views.draw.PolygonDrawAction")], u);
var m2 = u;

export {
  m2 as m
};
//# sourceMappingURL=chunk-WMBE7FVB.js.map
