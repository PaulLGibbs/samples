import {
  be,
  ce,
  fe,
  ge,
  le,
  ne,
  ue,
  xe
} from "./chunk-KLTQ32S3.js";
import {
  n
} from "./chunk-P2ND47GM.js";
import "./chunk-27ISRESK.js";
import "./chunk-7FMDFQ4Q.js";
import "./chunk-V67AJJBP.js";
import {
  Fe,
  P,
  Ut,
  _,
  ae,
  e,
  ei,
  o as o2,
  pe,
  ze
} from "./chunk-GIUO3FC5.js";
import {
  o
} from "./chunk-SCZRL2WP.js";
import "./chunk-XSCQBFM2.js";

// node_modules/@arcgis/core/widgets/Feature/FeatureMedia/xyChart.js
var x = class _x extends ce {
  constructor() {
    super(...arguments), Object.defineProperty(this, "_ph", { enumerable: true, configurable: true, writable: true, value: 0 }), Object.defineProperty(this, "_pw", { enumerable: true, configurable: true, writable: true, value: 0 });
  }
  _makeGraphics(t, e2) {
    return this.makeColumn(e2, t);
  }
  _makeFieldNames() {
    super._makeFieldNames();
    const t = this.get("xAxis"), e2 = this.get("yAxis"), i = "CategoryAxis", s = "ValueAxis";
    t.isType(i) && (this.get("openCategoryXField") || (this._xOpenField = this._xField)), t.isType(s) && (this.get("openValueXField") || (this._xOpenField = this._xField)), e2.isType(i) && (this.get("openCategoryYField") || (this._yOpenField = this._yField)), e2.isType(s) && (this.get("openValueYField") || (this._yOpenField = this._yField));
  }
  _prepareChildren() {
    super._prepareChildren();
    const t = this.get("xAxis"), e2 = this.get("yAxis"), i = this.dataItems.length, s = Math.max(0, this.startIndex() - 2), a = Math.min(this.endIndex() + 2, i - 1);
    if (t.inited && e2.inited) for (let o3 = s; o3 <= a; o3++) {
      let t2 = this.dataItems[o3];
      this._createGraphics(t2);
    }
  }
  _updateChildren() {
    const t = this.chart;
    t && (this._ph = t.plotContainer.height(), this._pw = t.plotContainer.width());
    const e2 = this.get("xAxis"), i = this.get("yAxis"), a = this.get("baseAxis"), o3 = this.columns.template;
    this.isDirty("fill") && null == o3.get("fill") && o3.set("fill", this.get("fill")), this.isDirty("fillPattern") && null == o3.get("fillPattern") && o3.set("fillPattern", this.get("fillPattern")), this.isDirty("stroke") && null == o3.get("stroke") && o3.set("stroke", this.get("stroke"));
    let n2 = 0, r = 0, l = 0;
    P(a.series, (t2) => {
      if (t2 instanceof _x) {
        const e3 = t2.get("stacked");
        e3 && 0 == l && r++, !e3 && t2.get("clustered") && r++;
      }
      t2 === this && (n2 = r - 1), l++;
    }), this.get("clustered") || (n2 = 0, r = 1), 0 === r && (r = 1, n2 = 0);
    const h = e2.get("renderer"), g = i.get("renderer"), d = "cellStartLocation", c = "cellEndLocation", m = h.get(d, 0), p = h.get(c, 1), u = g.get(d, 0), _3 = g.get(c, 1);
    if (this._aLocationX0 = m + n2 / r * (p - m), this._aLocationX1 = m + (n2 + 1) / r * (p - m), this._aLocationY0 = u + n2 / r * (_3 - u), this._aLocationY1 = u + (n2 + 1) / r * (_3 - u), e2.inited && i.inited) {
      if (this._axesDirty || this._valuesDirty || this._stackDirty || this.isDirty("vcx") || this.isDirty("vcy") || this._sizeDirty) {
        const t2 = this.dataItems.length;
        let e3 = Math.max(0, this.startIndex() - 2), i2 = Math.min(this.endIndex() + 2, t2 - 1);
        for (let a2 = 0; a2 < e3; a2++) this._toggleColumn(this.dataItems[a2], false);
        let s = this.dataItems[e3];
        for (let a2 = e3; a2 <= i2; a2++) {
          let t3 = this.dataItems[a2];
          if (null != t3.get("valueX") && null != t3.get("valueY")) {
            if (s = t3, a2 > 0 && e3 > 0) for (let t4 = a2 - 1; t4 >= 0; t4--) {
              let e4 = this.dataItems[t4];
              if (null != e4.get("valueX") && null != e4.get("valueY")) {
                s = e4;
                break;
              }
            }
            break;
          }
          this._toggleColumn(t3, false);
        }
        for (let a2 = e3; a2 <= i2; a2++) {
          let t3 = this.dataItems[a2];
          this._updateGraphics(t3, s), null != t3.get("valueX") && null != t3.get("valueY") && (s = t3);
        }
        for (let a2 = i2 + 1; a2 < t2; a2++) this._toggleColumn(this.dataItems[a2], false);
      }
    } else this._skipped = true;
    this.updateLegendMarker(this.get("tooltipDataItem")), super._updateChildren();
  }
  _createGraphics(t) {
    let e2 = t.get("graphics");
    if (!e2) {
      e2 = this._makeGraphics(this.columns, t), t.set("graphics", e2), e2._setDataItem(t);
      const i = t.get("legendDataItem");
      if (i) {
        const t2 = i.get("markerRectangle");
        if (t2) {
          const i2 = t2.states.lookup("default");
          P(ze, (s) => {
            const a2 = e2.get(s, this.get(s));
            t2.set(s, a2), i2.set(s, a2);
          });
        }
      }
      let a = t.get("rangeGraphics");
      a && P(a, (t2) => {
        t2.dispose();
      }), a = [], t.setRaw("rangeGraphics", a), this.axisRanges.each((e3) => {
        const i2 = e3.container, s = this._makeGraphics(e3.columns, t);
        a && a.push(s), s.setPrivate("list", e3.columns), i2.children.push(s);
      });
    }
  }
  createAxisRange(t) {
    return P(this.dataItems, (t2) => {
      const e2 = t2.get("graphics");
      e2 && (e2.dispose(), t2.set("graphics", void 0));
    }), super.createAxisRange(t);
  }
  _updateGraphics(t, e2) {
    let i = t.get("graphics");
    const a = this._xField, o3 = this._yField, l = t.get(a), h = t.get(o3);
    if (null != l && null != h) {
      const l2 = this._xOpenField, h2 = this._yOpenField, g = this.get("locationX", t.get("locationX", 0.5)), d = this.get("locationY", t.get("locationY", 0.5)), c = this.get("openLocationX", t.get("openLocationX", g)), m = this.get("openLocationY", t.get("openLocationY", d)), p = i.get("width"), u = i.get("height"), x2 = this.get("stacked"), _3 = this.get("xAxis"), I2 = this.get("yAxis"), f = this.get("baseAxis"), y = _3.get("start"), P2 = _3.get("end"), b = I2.get("start"), v = I2.get("end");
      let D, T, C, w, A = this.get("vcy", 1), k = this.get("vcx", 1), F = false, M = false;
      if (I2.isType("CategoryAxis") && _3.isType("CategoryAxis")) {
        let e3 = this._aLocationX0 + c - 0.5, i2 = this._aLocationX1 + g - 0.5;
        if (p instanceof e) {
          let t2 = (i2 - e3) * (1 - p.value) / 2;
          e3 += t2, i2 -= t2;
        }
        if (D = _3.getDataItemPositionX(t, l2, e3, k), T = _3.getDataItemPositionX(t, a, i2, k), e3 = this._aLocationY0 + m - 0.5, i2 = this._aLocationY1 + d - 0.5, u instanceof e) {
          let t2 = (i2 - e3) * (1 - u.value) / 2;
          e3 += t2, i2 -= t2;
        }
        C = I2.getDataItemPositionY(t, h2, e3, A), w = I2.getDataItemPositionY(t, o3, i2, A), t.setRaw("point", { x: D + (T - D) / 2, y: C + (w - C) / 2 });
      } else if (_3 === f) {
        let e3 = this._aLocationX0 + c - 0.5, i2 = this._aLocationX1 + g - 0.5;
        if (p instanceof e) {
          let t2 = (i2 - e3) * (1 - p.value) / 2;
          e3 += t2, i2 -= t2;
        }
        if (D = _3.getDataItemPositionX(t, l2, e3, k), T = _3.getDataItemPositionX(t, a, i2, k), C = I2.getDataItemPositionY(t, o3, d, A), this._yOpenField !== this._yField) w = I2.getDataItemPositionY(t, h2, m, A);
        else if (x2) {
          let e4 = t.get("stackToItemY");
          w = e4 ? I2.getDataItemPositionY(e4, o3, m, e4.component.get("vcy")) : I2.basePosition();
        } else w = I2.basePosition();
        t.setRaw("point", { x: D + (T - D) / 2, y: C }), M = true;
      } else if (I2 === f) {
        let e3 = this._aLocationY0 + m - 0.5, i2 = this._aLocationY1 + d - 0.5;
        if (u instanceof e) {
          let t2 = (i2 - e3) * (1 - u.value) / 2;
          e3 += t2, i2 -= t2;
        }
        if (C = I2.getDataItemPositionY(t, h2, e3, A), w = I2.getDataItemPositionY(t, o3, i2, A), T = _3.getDataItemPositionX(t, a, g, k), this._xOpenField !== this._xField) D = _3.getDataItemPositionX(t, l2, c, k);
        else if (x2) {
          let e4 = t.get("stackToItemX");
          D = e4 ? _3.getDataItemPositionX(e4, a, c, e4.component.get("vcx")) : _3.basePosition();
        } else D = _3.basePosition();
        F = true, t.setRaw("point", { x: T, y: C + (w - C) / 2 });
      }
      this._updateSeriesGraphics(t, i, D, T, C, w, F, M), D < y && T < y || D > P2 && T > P2 || C < b && w <= b || C >= v && w > v || o2(D) || o2(C) ? this._toggleColumn(t, false) : this._toggleColumn(t, true);
      let X = t.get("rangeGraphics");
      X && P(X, (e3) => {
        this._updateSeriesGraphics(t, e3, D, T, C, w, F, M);
      }), this._applyGraphicsStates(t, e2);
    } else this._toggleColumn(t, false);
  }
  _updateSeriesGraphics(t, e2, i, s, a, o3, n2, r) {
    const h = e2.get("width"), g = e2.get("height"), d = e2.get("maxWidth"), c = e2.get("maxHeight"), m = this.getPoint(i, a), p = this.getPoint(s, o3), u = t.get("point");
    if (u) {
      const t2 = this.getPoint(u.x, u.y);
      u.x = t2.x + this._x, u.y = t2.y + this._y;
    }
    if (i = m.x, s = p.x, a = m.y, o3 = p.y, _(h)) {
      const t2 = (s - i - h) / 2;
      i += t2, s -= t2;
    }
    if (_(d) && d < Math.abs(s - i)) {
      const t2 = (s - i - d) / 2;
      i += t2, s -= t2;
    }
    if (_(g)) {
      const t2 = (o3 - a - g) / 2;
      a += t2, o3 -= t2;
    }
    if (_(c) && c < Math.abs(o3 - a)) {
      const t2 = (o3 - a - c) / 2;
      a += t2, o3 -= t2;
    }
    this.get("adjustBulletPosition") && (n2 && (s = Math.min(Math.max(0, s), this._pw), i = Math.min(Math.max(0, i), this._pw)), r && (a = Math.min(Math.max(0, a), this._ph), o3 = Math.min(Math.max(0, o3), this._ph))), t.setRaw("left", i), t.setRaw("right", s), t.setRaw("top", a), t.setRaw("bottom", o3), e2.setPrivate("width", s - i), e2.setPrivate("height", o3 - a), e2.set("x", i), e2.set("y", o3 - (o3 - a));
  }
  _handleDataSetChange() {
    super._handleDataSetChange(), P(this._dataItems, (t) => {
      this._toggleColumn(t, false);
    });
  }
  _applyGraphicsStates(t, e2) {
    const i = t.get("graphics"), s = i.states.lookup("dropFromOpen"), a = i.states.lookup("riseFromOpen"), o3 = i.states.lookup("dropFromPrevious"), n2 = i.states.lookup("riseFromPrevious");
    if (s || o3 || a || n2) {
      const i2 = this.get("xAxis"), r = this.get("yAxis"), h = this.get("baseAxis");
      let g, d, c;
      h === i2 && r.isType("ValueAxis") ? (g = t.get(this._yOpenField), d = t.get(this._yField), c = e2.get(this._yField)) : h === r && i2.isType("ValueAxis") && (g = t.get(this._xOpenField), d = t.get(this._xField), c = e2.get(this._xField)), _(g) && _(d) && (d < g ? s && s.apply() : a && a.apply(), _(c) && (d < c ? o3 && o3.apply() : n2 && n2.apply()));
    }
  }
  disposeDataItem(t) {
    super.disposeDataItem(t);
    const e2 = t.get("graphics");
    e2 && (this.columns.removeValue(e2), e2.dispose());
    const i = t.get("rangeGraphics");
    i && P(i, (t2) => {
      const e3 = t2.getPrivate("list");
      e3 && e3.removeValue(t2), t2.dispose();
    });
  }
  hideDataItem(t, e2) {
    const i = Object.create(null, { hideDataItem: { get: () => super.hideDataItem } });
    return o(this, void 0, void 0, function* () {
      const a = [i.hideDataItem.call(this, t, e2)], o3 = t.get("graphics");
      o3 && a.push(o3.hide(e2));
      const n2 = t.get("rangeGraphics");
      n2 && P(n2, (t2) => {
        a.push(t2.hide(e2));
      }), yield Promise.all(a);
    });
  }
  _toggleColumn(t, e2) {
    const i = t.get("graphics");
    i && i.setPrivate("visible", e2);
    const a = t.get("rangeGraphics");
    a && P(a, (t2) => {
      t2.setPrivate("visible", e2);
    });
    const o3 = t.bullets;
    o3 && P(o3, (t2) => {
      t2.setPrivate("hidden", !e2);
    });
  }
  showDataItem(t, e2) {
    const i = Object.create(null, { showDataItem: { get: () => super.showDataItem } });
    return o(this, void 0, void 0, function* () {
      const a = [i.showDataItem.call(this, t, e2)], o3 = t.get("graphics");
      o3 && a.push(o3.show(e2));
      const n2 = t.get("rangeGraphics");
      n2 && P(n2, (t2) => {
        a.push(t2.show(e2));
      }), yield Promise.all(a);
    });
  }
  updateLegendMarker(t) {
    let e2 = this.get("legendDataItem");
    if (this.get("useLastColorForLegendMarker") && !t) {
      const e3 = this.dataItems[this.endIndex() - 1];
      e3 && (t = e3);
    }
    if (e2) {
      let i = this.columns.template;
      if (t) {
        let e3 = t.get("graphics");
        e3 && (i = e3);
      }
      const a = e2.get("markerRectangle");
      if (a && !e2.get("itemContainer").get("disabled")) {
        const t2 = a.states.lookup("default");
        P(ze, (e3) => {
          const s = i.get(e3, this.get(e3));
          a.set(e3, s), t2.set(e3, s);
        });
      }
    }
  }
  _getTooltipTarget(t) {
    if ("bullet" == this.get("seriesTooltipTarget")) return super._getTooltipTarget(t);
    let e2 = t.get("graphics");
    return e2 || this;
  }
};
Object.defineProperty(x, "className", { enumerable: true, configurable: true, writable: true, value: "BaseColumnSeries" }), Object.defineProperty(x, "classNames", { enumerable: true, configurable: true, writable: true, value: ce.classNames.concat([x.className]) });
var _2 = class __ extends ue {
  constructor() {
    super(...arguments), Object.defineProperty(this, "_frequency", { enumerable: true, configurable: true, writable: true, value: 1 }), Object.defineProperty(this, "_itemMap", { enumerable: true, configurable: true, writable: true, value: {} });
  }
  _afterNew() {
    this._settings.themeTags = Ut(this._settings.themeTags, ["axis"]), this.fields.push("category"), this.setPrivateRaw("name", "category"), this.addTag("category"), super._afterNew();
  }
  _prepareChildren() {
    super._prepareChildren();
    const t = this.dataItems.length;
    let e2 = 0;
    this._valuesDirty && (this._itemMap = {}, P(this.dataItems, (t2) => {
      t2.setRaw("index", e2), this._itemMap[t2.get("category")] = t2, e2++;
    }), this.setPrivateRaw("maxZoomFactor", t)), this.setPrivateRaw("startIndex", Math.max(Math.round(this.get("start", 0) * t), 0)), this.setPrivateRaw("endIndex", Math.min(Math.round(this.get("end", 1) * t), t)), (this._sizeDirty || this._valuesDirty || this.isDirty("start") || this.isDirty("end") || this.isPrivateDirty("endIndex") || this.isPrivateDirty("startIndex") || this.isPrivateDirty("width") || this.isPrivateDirty("height")) && this.dataItems.length > 0 && (this._handleRangeChange(), this._prepareAxisItems(), this._updateAxisRanges());
  }
  _handleRangeChange() {
    P(this.series, (t) => {
      let e2 = this.dataItems[this.startIndex()].get("category"), a = this.dataItems[this.endIndex() - 1].get("category"), o3 = t.get("baseAxis"), n2 = t.get("xAxis"), r = t.get("yAxis");
      if (n2 instanceof __ && r instanceof __) t._markDirtyAxes();
      else if (o3 === this) {
        let l, h, g = r;
        if (n2 === o3 ? (t.get("categoryXField") && (l = "categoryX"), t.get("openCategoryXField") && (h = "openCategoryX")) : r === o3 && (t.get("categoryYField") && (l = "categoryY"), t.get("openCategoryYField") && (h = "openCategoryY"), g = n2), g instanceof ge && (l || h)) {
          let i, o4;
          for (let s = 0, a2 = t.dataItems.length; s < a2; s++) {
            let a3 = t.dataItems[s];
            if (l && a3.get(l) === e2) {
              i = a3;
              break;
            }
            if (h && a3.get(h) === e2) {
              i = a3;
              break;
            }
          }
          for (let e3 = t.dataItems.length - 1; e3 >= 0; e3--) {
            let i2 = t.dataItems[e3];
            if (l && i2.get(l) === a) {
              o4 = i2;
              break;
            }
            if (h && i2.get(h) === a) {
              o4 = i2;
              break;
            }
          }
          let n3 = 0, r2 = t.dataItems.length;
          i && (n3 = t.dataItems.indexOf(i)), o4 && (r2 = t.dataItems.indexOf(o4) + 1), t.setPrivate("startIndex", n3), t.setPrivate("endIndex", r2);
          let g2 = false;
          for (let e3 = n3; e3 < r2; e3++) {
            const i2 = t.dataItems[e3];
            if (P(t.__valueXShowFields, (t2) => {
              null != i2.get(t2) && (g2 = true);
            }), P(t.__valueYShowFields, (t2) => {
              null != i2.get(t2) && (g2 = true);
            }), g2) break;
          }
          t.setPrivate("outOfSelection", !g2);
        }
        t._markDirtyAxes();
      }
    });
  }
  _prepareAxisItems() {
    var t;
    const e2 = this.get("renderer"), i = this.dataItems.length;
    let s = this.startIndex();
    s > 0 && s--;
    let a = this.endIndex();
    a < i && a++;
    const o3 = e2.get("minorLabelsEnabled"), n2 = e2.get("minorGridEnabled", o3);
    let r = e2.axisLength() / Math.max(e2.get("minGridDistance"), 1), l = Math.max(1, Math.min(i, Math.ceil((a - s) / r)));
    s = Math.floor(s / l) * l, this._frequency = l;
    for (let g = 0; g < i; g++) this._toggleDataItem(this.dataItems[g], false);
    let h = this.dataItems[s].get("index", 0);
    for (let g = s; g < a; g += l) {
      let t2 = this.dataItems[g];
      this._createAssets(t2, []), this._toggleDataItem(t2, true);
      let e3 = l;
      n2 && (e3 = 1), this._prepareDataItem(t2, h, e3), h++;
    }
    if (e2.get("minorGridEnabled")) for (let g = s; g < a; g++) {
      let e3 = this.dataItems[g];
      g % l != 0 && (this._createAssets(e3, ["minor"], true), this._toggleDataItem(e3, true), this._prepareDataItem(e3, 0, 1), o3 || null === (t = e3.get("label")) || void 0 === t || t.setPrivate("visible", false));
    }
    this._updateGhost();
  }
  _prepareDataItem(t, e2, i) {
    let s = this.get("renderer"), a = t.get("categoryLocation", 0), o3 = t.get("endCategoryLocation", 1), n2 = t.get("index");
    _(n2) || (n2 = this.categoryToIndex(t.get("category")));
    let r, h = this.indexToPosition(n2, a), g = t.get("endCategory");
    g ? (r = this.categoryToIndex(g), _(r) || (r = n2)) : r = n2;
    let d, c, m = this.indexToPosition(r, o3);
    d = t.get("isRange") ? r : n2 + this._frequency - 1, c = this.indexToPosition(d, o3), s.updateLabel(t.get("label"), h, m, i), s.updateGrid(t.get("grid"), h, m), s.updateTick(t.get("tick"), h, m, i), s.updateFill(t.get("axisFill"), h, c), this._processBullet(t), s.updateBullet(t.get("bullet"), h, m);
    const p = this.get("fillRule");
    p && p(t, e2);
  }
  startIndex() {
    let t = this.dataItems.length;
    return Math.min(Math.max(this.getPrivate("startIndex", 0), 0), t - 1);
  }
  endIndex() {
    let t = this.dataItems.length;
    return Math.max(1, Math.min(this.getPrivate("endIndex", t), t));
  }
  baseValue() {
  }
  basePosition() {
    return 0;
  }
  getX(t) {
    let e2 = this._itemMap[t];
    return e2 ? this._settings.renderer.positionToCoordinate(this.indexToPosition(e2.get("index", 0))) : NaN;
  }
  getY(t) {
    let e2 = this._itemMap[t];
    return e2 ? this._settings.renderer.positionToCoordinate(this.indexToPosition(e2.get("index", 0))) : NaN;
  }
  getDataItemPositionX(t, e2, i, s) {
    const a = t.get(e2), o3 = this._itemMap[a];
    return o3 ? this.indexToPosition(o3.get("index", 0), i) : NaN;
  }
  getDataItemCoordinateX(t, e2, i, s) {
    return this._settings.renderer.positionToCoordinate(this.getDataItemPositionX(t, e2, i, s));
  }
  getDataItemPositionY(t, e2, i, s) {
    const a = t.get(e2), o3 = this._itemMap[a];
    return o3 ? this.indexToPosition(o3.get("index", 0), i) : NaN;
  }
  getDataItemCoordinateY(t, e2, i, s) {
    return this._settings.renderer.positionToCoordinate(this.getDataItemPositionY(t, e2, i, s));
  }
  indexToPosition(t, e2) {
    _(e2) || (e2 = 0.5);
    let i = this.dataItems.length, s = this.get("startLocation", 0);
    i -= s, i -= 1 - this.get("endLocation", 1);
    let a = (t + e2 - s) / i, o3 = this.dataItems[t];
    return o3 && (a += o3.get("deltaPosition", 0)), a;
  }
  categoryToIndex(t) {
    let e2 = this._itemMap[t];
    return e2 ? e2.get("index") : NaN;
  }
  dataItemToPosition(t) {
    return this.indexToPosition(t.get("index"));
  }
  roundAxisPosition(t, e2) {
    return t += (0.5 - e2) / this.dataItems.length, this.indexToPosition(this.axisPositionToIndex(t), e2);
  }
  axisPositionToIndex(t) {
    let e2 = this.dataItems.length;
    return pe(Math.floor(t * e2), 0, e2 - 1);
  }
  getTooltipText(t, e2) {
    const i = this.dataItems[this.axisPositionToIndex(t)];
    if (i) {
      const t2 = i.get("label");
      if (t2) return ei(t2, this.get("tooltipText", ""));
    }
  }
  _updateTooltipText(t, e2) {
    t._setDataItem(this.dataItems[this.axisPositionToIndex(e2)]), t.label.text.markDirtyText();
  }
  getSeriesItem(t, e2) {
    if (this.dataItems.length > 0) {
      let i = this.getPrivate("name") + this.get("renderer").getPrivate("letter"), s = this.axisPositionToIndex(e2), a = t.dataItems[s], o3 = this.dataItems[s], n2 = o3.get("category");
      if (a && o3 && a.get(i) === n2) return a;
      for (let e3 = 0, r = t.dataItems.length; e3 < r; e3++) {
        let s2 = t.dataItems[e3];
        if (s2.get(i) === n2) return s2;
      }
    }
  }
  zoomToIndexes(t, e2, i) {
    let s = this.dataItems.length;
    this.zoom(t / s, e2 / s, i);
  }
  zoomToCategories(t, e2, i) {
    this.zoomToIndexes(this.categoryToIndex(t), this.categoryToIndex(e2) + 1, i);
  }
  getCellWidthPosition() {
    return this._frequency / this.dataItems.length / (this.get("end", 1) - this.get("start", 0));
  }
  nextPosition(t) {
    null == t && (t = 1), "Y" == this.get("renderer").getPrivate("letter") && (t *= -1);
    const e2 = this.getPrivate("tooltipPosition", 0), i = pe(this.axisPositionToIndex(e2) + t, 0, this.dataItems.length - 1);
    return this.toGlobalPosition(this.indexToPosition(i));
  }
};
Object.defineProperty(_2, "className", { enumerable: true, configurable: true, writable: true, value: "CategoryAxis" }), Object.defineProperty(_2, "classNames", { enumerable: true, configurable: true, writable: true, value: ue.classNames.concat([_2.className]) });
var I = class extends x {
  constructor() {
    super(...arguments), Object.defineProperty(this, "columns", { enumerable: true, configurable: true, writable: true, value: this.addDisposer(new ae(Fe.new({}), () => n._new(this._root, { position: "absolute", themeTags: Ut(this.columns.template.get("themeTags", []), ["series", "column"]) }, [this.columns.template]))) });
  }
  makeColumn(t, e2) {
    const i = this.mainContainer.children.push(e2.make());
    return i._setDataItem(t), e2.push(i), i;
  }
  _processAxisRange(t) {
    super._processAxisRange(t), t.columns = new ae(Fe.new({}), () => n._new(this._root, { position: "absolute", themeTags: Ut(t.columns.template.get("themeTags", []), ["series", "column"]) }, [this.columns.template, t.columns.template]));
  }
};
Object.defineProperty(I, "className", { enumerable: true, configurable: true, writable: true, value: "ColumnSeries" }), Object.defineProperty(I, "classNames", { enumerable: true, configurable: true, writable: true, value: x.classNames.concat([I.className]) });
export {
  be as AxisRendererXAm5,
  xe as AxisRendererYAm5,
  _2 as CategoryAxisAm5,
  I as ColumnSeriesAm5,
  fe as LineSeriesAm5,
  ge as ValueAxisAm5,
  ne as XYChartAm5,
  le as XYCursorAm5
};
//# sourceMappingURL=xyChart-IT7MF52Q.js.map
