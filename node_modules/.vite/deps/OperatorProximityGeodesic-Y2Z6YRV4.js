import {
  o
} from "./chunk-XX6OB2JQ.js";
import {
  c,
  g,
  s as s2
} from "./chunk-XHHDLR4G.js";
import "./chunk-NA5F4B4G.js";
import {
  As,
  Ec,
  Es,
  Ga,
  Gu,
  Is,
  Ol,
  Ps,
  Xl,
  Xm2 as Xm,
  bh,
  ce,
  eh,
  fs,
  kh,
  ks,
  nu,
  us,
  vc,
  yc
} from "./chunk-3LTZKV7D.js";
import {
  t
} from "./chunk-FU6NSV57.js";
import "./chunk-AJBMH4BL.js";
import {
  X,
  ee
} from "./chunk-2I7JQ4NT.js";
import "./chunk-BKBHJYAN.js";
import {
  Lt,
  Mt,
  P,
  Rt,
  a,
  b,
  ei,
  h,
  n as n2,
  z
} from "./chunk-ODBTW2MK.js";
import {
  n,
  s
} from "./chunk-SCZRL2WP.js";
import "./chunk-XSCQBFM2.js";

// node_modules/@arcgis/core/chunks/OperatorProximityGeodesic.js
var V = class {
  getOperatorType() {
    return 10501;
  }
  supportsCurves() {
    return true;
  }
  accelerateGeometry(e, t2, n3) {
    return false;
  }
  canAccelerateGeometry(e) {
    return false;
  }
  getNearestCoordinate(e, t2, y, S, N, x) {
    if (t2.isNAN() && P("NAN xy coordinates are not allowed"), e.isEmpty()) return s2();
    if (Number.isNaN(S) && (S = 0), S >= 0 || P("maxDeviationMeters"), 0 === e.getDimension()) return this.getNearestVertex(e, t2, y);
    const A = e.getGeometryType();
    if (A === a.enumGeometryCollection) {
      const n3 = e;
      let s3 = s2();
      for (let e2 = 0, o2 = n3.getGeometryCount(); e2 < o2; ++e2) {
        const o3 = n3.getGeometry(e2), r = this.getNearestCoordinate(o3, t2, y, S, N, x);
        if (!r.isEmpty()) {
          if (0 === r.m_distance) return r;
          r.m_distance >= s3.m_distance || (s3 = r, s3.m_geometryIndex = e2);
        }
      }
      return s3;
    }
    if (A === a.enumEnvelope) {
      const n3 = new eh();
      return n3.addEnvelope(e, false), this.getNearestCoordinate(n3, t2, y, S, N, x);
    }
    h(A) || z("Operation is not implemented for this geometry type");
    const I = e;
    if ((N || x) && A === a.enumPolygon) {
      const n3 = () => {
        const e2 = g(t2, -1, 0);
        return e2.m_bRightSide = true, e2;
      };
      if (y.isPannable()) {
        const s3 = new kh().foldInto360Range(new ee(t2), y);
        if (s3.isEmpty()) return s2();
        const o2 = new kh().foldInto360Range(e, y);
        if (o2.isEmpty()) return s2();
        if (new Xm().execute(o2, s3, y, null) && N) return n3();
      } else {
        const e2 = new kh().clipToSpatialReference(new ee(t2), y, null, 1);
        if (e2.isEmpty()) return s2();
        const s3 = Ol(I, y, 0, null);
        if (s3.isEmpty()) return s2();
        if (new Xm().execute(s3, e2, y, null) && N) return n3();
      }
    }
    const b2 = y !== y.getGCS() ? y.getSRToGCSTransform() : null, v = new ei();
    if (b2 ? new kh().transform(b2, [t2], 1, [v], false) : v.assign(t2), v.isNAN()) return s2();
    const R = O(I, t2, y, 1);
    let U = R.m_distance;
    if (0 === U) return R;
    Number.isNaN(U) ? U = Number.POSITIVE_INFINITY : U *= 1 + 1e-7;
    const q = y.getGCS(), B = q.getUnit().getUnitToBaseFactor(), V2 = v.clone();
    V2.scale(B);
    const Y2 = Gu();
    y.querySpheroidData(Y2);
    const X3 = us(Y2.majorSemiAxis, Y2.e2, V2), L = As(Y2.majorSemiAxis, Y2.e2), z2 = new M(I.querySegmentIterator());
    0 === S && (S = 0.01);
    const Q = Y2.majorSemiAxis * Ga(45), H = new o().executeMany(z2, y, Q, S, 0, null), J = [];
    let K = -1;
    const W = new X(), Z = new nu();
    for (let n3 = H.next(); null !== n3; n3 = H.next()) {
      if (n3.isEmpty()) continue;
      const t3 = H.getGeometryID(), s3 = e.getPathIndexFromPointIndex(t3);
      e.isClosedPath(s3) && I.getPathEnd(s3);
      const o2 = n3;
      for (let e2 = 0, n4 = o2.getPathCount(); e2 < n4; ++e2) {
        J.length = 0;
        for (let t4 = o2.getPathStart(e2), r = o2.getPathEnd(e2); t4 < r; ++t4) J.push(o2.getXY(t4));
        if (b2 && new kh().transform(b2, J, J.length, J, false), J.length > 3) {
          const e3 = k(J, J.length, v, q, 1);
          if (e3.m_distance < U) {
            U = e3.m_distance, W.assign(us(Y2.majorSemiAxis, Y2.e2, e3.m_coordinate.mul(B))), K = t3;
            let n6 = e3.m_vertexIndex;
            n6 + 1 === J.length && (n6 -= 1), o2.getSegmentBuffer(n6, Z, true);
          }
        }
        J.forEach((e3) => e3.mulThis(B));
        const n5 = us(Y2.majorSemiAxis, Y2.e2, J[0]), s4 = new X();
        for (let e3 = 1, r = J.length; e3 < r; ++e3) {
          const r2 = us(Y2.majorSemiAxis, Y2.e2, J[e3]);
          if (Es(L, X3, n5, r2, s4) < U) {
            const { first: s5, second: i } = Is(Y2.majorSemiAxis, Y2.e2, X3, n5, r2, 2, null);
            if (i <= U) {
              const s6 = new X(), { first: i2, second: a2 } = Is(Y2.majorSemiAxis, Y2.e2, X3, n5, r2, 0, s6);
              a2 < U && (U = a2, W.assign(s6), K = t3, o2.getSegmentBuffer(e3 - 1, Z, true));
            }
          }
          n5.assign(r2);
        }
      }
    }
    if (!Number.isFinite(U)) return s2();
    -1 === K && P("");
    const $ = fs(Y2.majorSemiAxis, Y2.e2, W);
    $.divThis(B);
    const ee2 = $.clone(), te = y !== y.getGCS() ? y.getGCSToSRTransform() : null;
    te && new kh().transform(te, [$], 1, [ee2], false), ee2.isNAN() && b("closestPoint2DSr.isNAN()"), n2(!Z.empty());
    const ne = Z.get().getClosestCoordinate(ee2, false);
    ee2.assign(Z.get().getCoord2D(ne)), b2 ? new kh().transform(b2, [ee2], 1, [$], false) : $.assign(ee2), n2(!$.isNAN());
    const se = this.getNearestVertex(new ee($), v, y.getGCS()), oe = g(ee2, K, se.m_distance);
    if (x && e.getDimension() > 0) {
      const n3 = c(e, t2, -1, -1), s3 = 0 !== n3 && 3 !== n3;
      oe.m_bRightSide = s3;
    }
    return oe;
  }
  getNearestVertex(e, t2, o2) {
    if (t2.isNAN() && P("NAN xy coordinates are not allowed"), e.isEmpty()) return s2();
    switch (e.getGeometryType()) {
      case a.enumPoint:
        return Y(e, t2, o2);
      case a.enumMultiPoint:
      case a.enumPolyline:
      case a.enumPolygon:
        return O(e, t2, o2);
      default:
        z("");
    }
  }
  getNearestVertexPoint2D(e, t2, n3, s3) {
    return k(e, t2, n3, s3);
  }
  getNearestVertices(e, t2, o2, i, a2) {
    if (t2.isNAN() && P("NAN xy coordinates are not allowed"), e.isEmpty()) return [];
    switch (e.getGeometryType()) {
      case a.enumPoint: {
        const n3 = Y(e, t2, o2);
        if (!n3.isEmpty() && n3.m_distance < i && a2 >= 1) {
          const e2 = [];
          return e2.push(n3), e2;
        }
        return [];
      }
      case a.enumMultiPoint:
      case a.enumPolyline:
      case a.enumPolygon:
        return X2(e, t2, o2, i, a2);
      default:
        z("");
    }
  }
};
function Y(s3, o2, r) {
  const i = { stack: [], error: void 0, hasError: false };
  try {
    if (0 === r.getCoordinateSystemType() && P("Cannot use local SR"), 1 !== r.getCoordinateSystemType()) {
      const e = new Array(2);
      e[0] = s3.getXY(), e[1] = o2.clone();
      const t3 = Rt(ei, 2);
      2 !== Xl(r, e, 2, t3) && P("Point cannot be projected to GCS");
      const i2 = r.getGCS(), a3 = Y(new ee(t3[0]), t3[1], i2);
      return g(a3.m_coordinate, 0, a3.m_distance);
    }
    const t2 = r.getUnit().getUnitToBaseFactor(), a2 = s3.getXY();
    a2.scale(t2);
    const c2 = o2.clone();
    c2.scale(t2);
    const l = Gu();
    r.querySpheroidData(l);
    const u = n(i, new yc(), false);
    vc.geodeticDistance(l.majorSemiAxis, l.e2, a2.x, a2.y, c2.x, c2.y, u, null, null, Ec.PE_LINETYPE_GEODESIC);
    const g2 = s3.getXY();
    return g(g2, 0, u.val);
  } catch (a2) {
    i.error = a2, i.hasError = true;
  } finally {
    s(i);
  }
}
function O(s3, o2, r, i = 0) {
  0 === r.getCoordinateSystemType() && P("Cannot use local SR");
  let a2 = s3.getAttributeStreamRef(0), c2 = s3.getPointCount();
  const l = r.getGCS(), u = o2.clone();
  if (1 !== r.getCoordinateSystemType()) {
    const e = r.getSRToGCSTransform();
    if (bh(e, [o2], 1, [u], null), u.isNAN()) return g(ei.getNAN(), -1, Number.NaN);
    const t2 = new kh().execute(s3, e, null);
    a2 = t2.getAttributeStreamRef(0), c2 = t2.getPointCount();
  }
  const g2 = l.getUnit().getUnitToBaseFactor(), f = Lt(c2, 0);
  for (let e = 0; e < c2; ++e) f[e] = e;
  ce(c2, f);
  const S = Gu();
  l.querySpheroidData(S);
  const h2 = u.clone();
  h2.scale(g2);
  const A = us(S.majorSemiAxis, S.e2, h2);
  let P2 = -1, w = Number.MAX_VALUE;
  const _ = ks(S.majorSemiAxis, S.e2);
  let T = c2;
  const j = new ei(), G = new ei();
  for (let e = 0; e < T; ) {
    if (a2.queryPoint2D(2 * f[e], j), j.isNAN()) continue;
    G.assign(j), G.scale(g2);
    const t2 = us(S.majorSemiAxis, S.e2, G);
    if (X.distance(t2, A) > w) {
      f[T - 1] = Mt(f[e], f[e] = f[T - 1]), T--;
      continue;
    }
    const n3 = Ps(_, t2, A);
    n3 < w && (P2 = f[e], w = n3), e++;
  }
  if (0 !== S.e2) {
    const n3 = { stack: [], error: void 0, hasError: false };
    try {
      const t2 = n(n3, new yc(), false);
      for (let e = 0; e < T; e++) {
        if (a2.queryPoint2D(2 * f[e], j), j.isNAN()) continue;
        G.assign(j), G.scale(g2);
        const n4 = us(S.majorSemiAxis, S.e2, G);
        X.distance(n4, A) > w || (vc.geodeticDistance(S.majorSemiAxis, S.e2, h2.x, h2.y, G.x, G.y, t2, null, null, i), t2.val < w && (P2 = f[e], w = t2.val));
      }
    } catch (R) {
      n3.error = R, n3.hasError = true;
    } finally {
      s(n3);
    }
  }
  const I = new ei();
  return -1 !== P2 && I.assign(s3.getXY(P2)), g(I, P2, w);
}
function k(s3, o2, r, i, a2 = 0) {
  0 === i.getCoordinateSystemType() && P("Cannot use local SR");
  const l = i.getGCS(), u = r.clone();
  if (1 !== i.getCoordinateSystemType()) {
    const e = i.getSRToGCSTransform();
    if (bh(e, [r], 1, [u], null), u.isNAN()) return s2();
    const t2 = Rt(ei, o2);
    bh(e, s3, o2, t2, null);
    const n3 = k(t2, o2, r, l, a2);
    return n3.isEmpty() ? n3 : g(s3[n3.m_vertexIndex], n3.m_vertexIndex, n3.m_distance);
  }
  const g2 = l.getUnit().getUnitToBaseFactor(), f = Lt(o2, 0);
  for (let e = 0; e < o2; ++e) f[e] = e;
  ce(o2, f);
  const h2 = Gu();
  l.querySpheroidData(h2);
  const A = u.clone();
  A.scale(g2);
  const C = us(h2.majorSemiAxis, h2.e2, A);
  let P2 = -1, w = Number.MAX_VALUE;
  const _ = ks(h2.majorSemiAxis, h2.e2);
  let T = o2;
  for (let e = 0; e < T; ) {
    const t2 = s3[f[e]];
    if (t2.isNAN()) continue;
    const n3 = t2.clone();
    n3.scale(g2);
    const o3 = us(h2.majorSemiAxis, h2.e2, n3);
    if (X.distance(o3, C) > w) {
      f[T - 1] = Mt(f[e], f[e] = f[T - 1]), T--;
      continue;
    }
    const r2 = Ps(_, o3, C);
    r2 < w && (P2 = f[e], w = r2), e++;
  }
  if (0 !== h2.e2) {
    const n3 = { stack: [], error: void 0, hasError: false };
    try {
      const t2 = n(n3, new yc(), false);
      for (let e = 0; e < T; e++) {
        const n4 = s3[f[e]];
        if (n4.isNAN()) continue;
        const o3 = n4.clone();
        o3.scale(g2);
        const r2 = us(h2.majorSemiAxis, h2.e2, o3);
        X.distance(r2, C) > w || (vc.geodeticDistance(h2.majorSemiAxis, h2.e2, A.x, A.y, o3.x, o3.y, t2, null, null, a2), t2.val < w && (P2 = f[e], w = t2.val));
      }
    } catch (G) {
      n3.error = G, n3.hasError = true;
    } finally {
      s(n3);
    }
  }
  const j = new ei();
  return -1 !== P2 && j.assign(s3[P2]), g(j, P2, w);
}
function X2(s3, o2, r, i, a2) {
  const c2 = { stack: [], error: void 0, hasError: false };
  try {
    0 === r.getCoordinateSystemType() && P("Cannot use local SR");
    let t2 = s3.getAttributeStreamRef(0), l = s3.getPointCount();
    const u = r.getGCS(), g2 = o2.clone();
    if (1 !== r.getCoordinateSystemType()) {
      const e = r.getSRToGCSTransform();
      if (bh(e, [o2], 1, [g2], null), g2.isNAN()) return [];
      const n3 = new kh().execute(s3, e, null);
      t2 = n3.getAttributeStreamRef(0), l = n3.getPointCount();
    }
    const f = u.getUnit().getUnitToBaseFactor(), S = Lt(l, 0);
    for (let e = 0; e < l; ++e) S[e] = e;
    ce(l, S);
    const h2 = Gu();
    u.querySpheroidData(h2);
    const A = g2.clone();
    A.scale(f);
    const P2 = us(h2.majorSemiAxis, h2.e2, A), w = [];
    let _ = l;
    const T = new ei(), j = new ei();
    for (let e = 0; e < _; ) {
      if (t2.queryPoint2D(2 * S[e], T), T.isNAN()) continue;
      j.assign(T), j.scale(f);
      const n3 = us(h2.majorSemiAxis, h2.e2, j);
      X.distance(n3, P2) > i ? (S[_ - 1] = Mt(S[e], S[e] = S[_ - 1]), _--) : e++;
    }
    const G = (e, t3) => e.m_distance - t3.m_distance, I = n(c2, new yc(), false);
    for (let e = 0; e < _; e++) t2.queryPoint2D(2 * S[e], T), T.isNAN() || (vc.geodeticDistance(h2.majorSemiAxis, h2.e2, A.x, A.y, T.x * f, T.y * f, I, null, null, Ec.PE_LINETYPE_GEODESIC), I.val < i && w.push(g(T, S[e], I.val)));
    return w.sort(G), w.slice(0, a2);
  } catch (l) {
    c2.error = l, c2.hasError = true;
  } finally {
    s(c2);
  }
}
var M = class extends t {
  constructor(e) {
    super(), this.m_first = true, this.m_segIter = e, this.m_segIter.stripAttributes();
  }
  tock() {
    return true;
  }
  getRank() {
    return 1;
  }
  next() {
    if (this.m_first) {
      if (!this.m_segIter.nextPath()) return null;
      this.m_first = false;
    }
    for (; ; ) {
      if (this.m_segIter.hasNextSegment()) return this.m_segIter.nextSegment().clone();
      if (!this.m_segIter.nextPath()) break;
    }
    return null;
  }
  getGeometryID() {
    return this.m_segIter.getStartPointIndex();
  }
};
export {
  V as OperatorProximityGeodesic
};
//# sourceMappingURL=OperatorProximityGeodesic-Y2Z6YRV4.js.map
