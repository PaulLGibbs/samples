import {
  c,
  p as p3
} from "./chunk-Y6STV4ZG.js";
import {
  p as p2
} from "./chunk-KBZTYWDS.js";
import {
  R,
  f as f2,
  u
} from "./chunk-R5KUHVWX.js";
import {
  t
} from "./chunk-AUSKHKYJ.js";
import {
  f
} from "./chunk-NU6X6ZQC.js";
import {
  K
} from "./chunk-QK2C27HF.js";
import {
  b
} from "./chunk-JTPYHTQV.js";
import {
  M
} from "./chunk-HUFVAHMS.js";
import {
  n as n3
} from "./chunk-3MG5UCTH.js";
import {
  h,
  r as r2
} from "./chunk-BPFNDF4V.js";
import {
  P,
  d,
  p
} from "./chunk-QXWM4LT3.js";
import {
  V
} from "./chunk-RGT625CZ.js";
import {
  i
} from "./chunk-PUAJSZ4Q.js";
import {
  j
} from "./chunk-DJTR6MPM.js";
import {
  s as s2
} from "./chunk-EH4HJRFE.js";
import {
  n as n2
} from "./chunk-3BBSZARH.js";
import {
  r
} from "./chunk-SCZRL2WP.js";
import {
  m
} from "./chunk-HITYQXMG.js";
import {
  a2 as a
} from "./chunk-4XNJLQ7P.js";
import {
  s
} from "./chunk-CAZ5FG6J.js";
import {
  n
} from "./chunk-3TGCPCOH.js";

// node_modules/@arcgis/core/widgets/CoordinateConversion/CoordinateConversionViewModel.js
var S = { default: "default", crosshair: "crosshair" };
var F = new j([0, 0, 500]);
var G = "xy";
var H = ["mgrs", "utm", "usng", "dd", "dms", "ddm"];
var E = "esri__coordinateConversionWidgetState";
var x = { conversions: "conversions", formats: "formats", view: "view", viewChange: "view-change" };
var T = "esri/images/search/search-symbol-32.png";
var W = class extends t(i.EventedAccessor) {
  constructor(t2) {
    super(t2), this._conversionPromise = null, this._locationGraphic = null, this._pointerCount = 0, this.conversions = new V(), this.formats = new V(), this.formatterAvailable = false, this.messages = null, this.filteredFormats = new V(), this.locationSymbol = new n3({ url: n2(T), width: 24, height: 24 }), this.storageEnabled = true, this.storageType = "session", this.view = null, this._saveWidgetState = this._saveWidgetState.bind(this), this._handleFormatChange = this._handleFormatChange.bind(this), this._handleConversionChange = this._handleConversionChange.bind(this), this._handleViewChange = this._handleViewChange.bind(this), this._onClick = this._onClick.bind(this), this._onPointerMove = this._onPointerMove.bind(this), this._onPointerDown = this._onPointerDown.bind(this), this._onPointerUp = this._onPointerUp.bind(this);
  }
  initialize() {
    const t2 = async () => this.messages = await f("esri/widgets/CoordinateConversion/t9n/CoordinateConversion");
    this.formats.addMany(p3()), t2().then(() => {
      if (!this.destroyed && (c(this.messages, this.formats), this.storageEnabled && this._loadWidgetState(), this.formats.forEach((t3) => {
        t3.viewModel = this, this.addHandles(d(() => t3.currentPattern, this._saveWidgetState), t3.name ?? "unnamed-format");
      }), this.addHandles(this.conversions.on("change", this._handleConversionChange), x.conversions), this.addHandles(this.formats.on("change", this._handleFormatChange), x.formats), this.addHandles(h(() => {
        t2().then(() => {
          c(this.messages, this.formats);
        });
      })), K().then(() => {
        this.formatterAvailable = true;
      }).catch((t3) => {
        n.getLogger(this).error(new s("coordinate-conversion:projection-load-failed", "Failed to load the projection module.", { error: t3 })), this.formatterAvailable = false, this._filterFormatsAndConversions();
      }).then(() => this.addHandles(d(() => this.view, this._handleViewChange, P), x.viewChange)), 0 === this.conversions.length)) {
        const t3 = this.formats.find((t4) => t4.name === G) || this.formats.at(0);
        this.conversions.add(new p2({ format: t3 }));
      }
    });
  }
  destroy() {
    this.removeHandles(), this._cleanUpView(this.view), this.view = null;
  }
  castConversions(t2) {
    return this._castToConversions(t2);
  }
  set currentLocation(t2) {
    this._set("currentLocation", t2), this._updateConversions();
  }
  get currentLocation() {
    return this._get("currentLocation") || null;
  }
  set mode(t2) {
    switch (t2) {
      case "capture":
        this.currentLocation = null, this._startCaptureMode(), this._set("mode", t2);
        break;
      case "live":
        this._startLiveMode(), this._set("mode", t2);
    }
  }
  get mode() {
    return this._get("mode") || "live";
  }
  get state() {
    const { messages: t2, view: e } = this, o = e == null ? void 0 : e.ready;
    return t2 ? o ? "ready" : e ? "loading" : "disabled" : "disabled";
  }
  get storage() {
    const { storageType: t2 } = this;
    return "session" === t2 ? sessionStorage : localStorage;
  }
  get waitingForConversions() {
    return null != this._conversionPromise;
  }
  setLocation(t2) {
    var _a, _b;
    if (this._locationGraphic && ((_a = this.view) == null ? void 0 : _a.graphics.remove(this._locationGraphic)), !t2) return;
    const e = t2.clone();
    e.hasZ && (e.z = void 0), this._locationGraphic = new b({ geometry: e, symbol: this.locationSymbol }), (_b = this.view) == null ? void 0 : _b.graphics.add(this._locationGraphic);
  }
  async convert(t2, e) {
    if (!u(e)) throw new s("coordinate-conversion:invalid-point", "Invalid point cannot be converted.", { point: e });
    return Promise.resolve().then(() => t2.convert(e));
  }
  async goToLocation(t2) {
    var _a, _b, _c;
    const { view: e } = this;
    if (!e) throw new s("coordinate-conversion:go-to-failed", "no view");
    const o = "3d" === e.type ? e.clippingArea : null, i2 = (_b = (_a = e.map) == null ? void 0 : _a.basemap) == null ? void 0 : _b.baseLayers;
    if (o || ((i2 == null ? void 0 : i2.length) ?? 0) > 0) {
      const e2 = o ?? ((_c = i2 == null ? void 0 : i2.at(0)) == null ? void 0 : _c.fullExtent);
      if (null != e2 && !e2.contains(t2)) throw new s("coordinate-conversion:go-to-failed", "Point outside basemap extent.", { point: t2 });
    }
    return this.callGoTo({ target: t2 });
  }
  pause() {
    this.currentLocation = null, this.removeHandles(x.view), this.view && (this.view.cursor = S.default, this._locationGraphic && this.view.graphics.remove(this._locationGraphic));
  }
  async previewConversion(t2, e = this.currentLocation || F) {
    return (await A(t2, e)).displayCoordinate;
  }
  resume() {
    "capture" === this.mode ? this._startCaptureMode() : this._startLiveMode();
  }
  reverseConvert(t2, e) {
    return e.reverseConvert(t2);
  }
  async updateConversions(t2, e) {
    if ("point" !== (e == null ? void 0 : e.type)) throw this._clearConversions(this.conversions), new s("coordinate-conversion:invalid-input-point", "Point is invalid, conversions cannot be updated.", { point: e });
    return this._convertMany(t2, e);
  }
  _castToConversions(t2) {
    const e = new V();
    return t2.forEach((t3) => {
      let o = null;
      if (t3 instanceof p2) o = t3;
      else if ("string" == typeof t3) {
        const e2 = this.formats.find((e3) => e3.name === t3);
        e2 && (o = new p2({ format: e2 }));
      }
      o && e.add(o);
    }), e;
  }
  _cleanUpView(t2) {
    t2 && (this._locationGraphic && t2.graphics.remove(this._locationGraphic), this.removeHandles(x.view), t2.cursor = S.default);
  }
  _clearConversions(t2) {
    t2.forEach((t3) => {
      t3.position = { location: null, coordinate: null };
    });
  }
  async _convertMany(t2, e) {
    return Promise.all(t2.map((t3) => A(t3, e)));
  }
  _handleConversionChange(t2) {
    for (const e of t2.added) {
      const { format: t3 } = e;
      t3 && (t3.viewModel = this, this.currentLocation && (this._set("waitingForConversions", true), this.convert(t3, this.currentLocation).then((t4) => {
        e.position = t4, this._set("waitingForConversions", false);
      })));
    }
    this._saveWidgetState();
  }
  _handleFormatChange(t2) {
    t2.added.forEach((t3) => {
      this.addHandles(d(() => t3.currentPattern, this._saveWidgetState), t3.name ?? "unnamed-format"), t3.viewModel = this;
    }), t2.removed.forEach((t3) => {
      t3.viewModel = null;
      const e = this.conversions.filter((e2) => e2.format === t3);
      this.conversions.removeMany(e), t3.name && this.removeHandles(t3.name);
    });
  }
  _loadWidgetState() {
    try {
      const t2 = JSON.parse(this.storage.getItem(E));
      t2 && this._setWidgetState(t2);
    } catch (t2) {
      n.getLogger(this).error(new s("coordinate-conversion:invalid-session-storage-json", "Could not read from storage.", { error: t2 }));
    }
  }
  _startCaptureMode() {
    this.removeHandles(x.view), this.view && (this.view.cursor = S.crosshair, this.currentLocation && this.setLocation(this.currentLocation), this.addHandles(this.view.on("click", this._onClick), x.view));
  }
  _startLiveMode() {
    this._pointerCount = 0, this.removeHandles(x.view), this.view && (this.view.cursor = S.default, this._locationGraphic && this.view.graphics.remove(this._locationGraphic), this.addHandles([this.view.on("pointer-down", this._onPointerDown), this.view.on("pointer-up", this._onPointerUp), this.view.on("pointer-move", this._onPointerMove)], x.view));
  }
  _handleViewChange(t2, e) {
    e && e !== t2 && this._cleanUpView(e), t2 && ("capture" === this.mode ? this._startCaptureMode() : this._startLiveMode(), e && this._filterFormatsAndConversions());
  }
  _onClick(t2) {
    var _a;
    if (0 === t2.button) {
      const e = (_a = this.view) == null ? void 0 : _a.toMap(t2), o = e == null ? void 0 : e.normalize();
      this.setLocation(o), this.currentLocation = o;
    }
  }
  _onPointerDown(t2) {
    var _a;
    const { pointerType: e } = t2;
    if (this._pointerCount++, ("touch" === e || "pen" === e) && 1 === this._pointerCount) {
      const e2 = (_a = this.view) == null ? void 0 : _a.toMap(t2);
      this.currentLocation = e2 == null ? void 0 : e2.normalize();
    }
  }
  _onPointerMove(t2) {
    var _a;
    const { pointerType: e } = t2;
    if ("mouse" === e || 1 === this._pointerCount) {
      const e2 = (_a = this.view) == null ? void 0 : _a.toMap(t2);
      this.currentLocation = e2 == null ? void 0 : e2.normalize();
    }
  }
  _onPointerUp() {
    this._pointerCount--;
  }
  _setWidgetState(t2) {
    try {
      t2.formats.forEach((e) => {
        const o = this.formats.find((t3) => t3.name === e.name);
        o && t2.locale === r2() && e.currentPattern && (o.currentPattern = e.currentPattern), o && e.index >= 0 && this.conversions.add(new p2({ format: o }));
      });
    } catch (e) {
      n.getLogger(this).warn(new s("coordinate-conversion:session-storage-read-error", "Could not get widget state from stored JSON.", { error: e }));
    }
  }
  _saveWidgetState() {
    if (!this.storageEnabled) return;
    const t2 = this._toJSON();
    try {
      this.storage.setItem(E, JSON.stringify({ formats: t2, locale: r2() }));
    } catch (e) {
      n.getLogger(this).error(new s("coordinate-conversion:local-storage-write-error", "Could not write to localStorage.", { error: e }));
    }
  }
  async _updateConversions() {
    try {
      await this.updateConversions(this.conversions.toArray(), this.currentLocation);
    } catch {
    }
  }
  _toJSON() {
    return this.formats.filter((t2) => {
      const e = t2.name;
      return "xy" === e || "basemap" === e || f2(e);
    }).map((t2) => ({ name: t2.name, currentPattern: t2.currentPattern, defaultPattern: t2.defaultPattern, index: this.conversions.findIndex((e) => e.format === t2) })).sort((t2, e) => t2.index - e.index).toArray();
  }
  _filterFormatsAndConversions() {
    const { formatterAvailable: t2, conversions: e, formats: o } = this;
    t2 || this.addHandles(p(() => {
      var _a;
      return (_a = this.view) == null ? void 0 : _a.spatialReference;
    }, (t3) => {
      const i2 = t3.isWebMercator || t3.isWGS84, s3 = o.filter((t4) => {
        const { name: e2 } = t4;
        return !!U(e2) || "xy" === e2 && !i2;
      }), r3 = this.filteredFormats.filter((t4) => "xy" === t4.name && i2 && !this.formats.includes(t4)), n4 = e.filter((t4) => s3.includes(t4.format));
      o.removeMany(s3), e.removeMany(n4), this.filteredFormats.addMany(s3.filter((t4) => !this.filteredFormats.includes(t4))), o.addMany(r3);
    }, { once: true, initial: true }), x.view);
  }
};
async function A(t2, e) {
  var _a;
  try {
    t2.position = await ((_a = t2.format) == null ? void 0 : _a.convert(e));
  } catch (o) {
    t2.position = null;
  }
  return t2;
}
function U(t2) {
  return H.includes(t2);
}
r([m({ type: V.ofType(p2) })], W.prototype, "conversions", void 0), r([s2("conversions")], W.prototype, "castConversions", null), r([m({ type: j })], W.prototype, "currentLocation", null), r([m({ type: V.ofType(R) })], W.prototype, "formats", void 0), r([m()], W.prototype, "messages", void 0), r([m()], W.prototype, "mode", null), r([m()], W.prototype, "filteredFormats", void 0), r([m({ readOnly: true })], W.prototype, "state", null), r([m({ types: M })], W.prototype, "locationSymbol", void 0), r([m({ readOnly: true, dependsOn: ["storageType"] })], W.prototype, "storage", null), r([m()], W.prototype, "storageEnabled", void 0), r([m()], W.prototype, "storageType", void 0), r([m({ readOnly: true })], W.prototype, "waitingForConversions", null), r([m()], W.prototype, "view", void 0), W = r([a("esri.widgets.CoordinateConversion.CoordinateConversionViewModel")], W);
var O = W;

export {
  O
};
//# sourceMappingURL=chunk-EBEEKBAU.js.map
