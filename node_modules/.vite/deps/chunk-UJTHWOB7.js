import {
  i
} from "./chunk-RF72QRKF.js";
import {
  d,
  u
} from "./chunk-SCDCLADK.js";
import {
  e as e2
} from "./chunk-46GTJXDE.js";
import {
  s as s2
} from "./chunk-7FXRLNEH.js";
import {
  o as o2
} from "./chunk-2CY5SLAC.js";
import {
  o as o3
} from "./chunk-3YQXC4G7.js";
import {
  n
} from "./chunk-55SEMSMA.js";
import {
  a
} from "./chunk-BUZ7I3MJ.js";
import {
  e
} from "./chunk-HYT6FDCG.js";
import {
  o
} from "./chunk-7Q3QM466.js";
import {
  s
} from "./chunk-LU33SON4.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/shading/ComputeNormalTexture.glsl.js
function T(T2, u2) {
  const x = T2.fragment;
  u2.hasVertexTangents ? (T2.attributes.add(e.TANGENT, "vec4"), T2.varyings.add("vTangent", "vec4"), u2.doubleSidedMode === i.WindingOrder ? x.code.add(n`mat3 computeTangentSpace(vec3 normal) {
float tangentHeadedness = gl_FrontFacing ? vTangent.w : -vTangent.w;
vec3 tangent = normalize(gl_FrontFacing ? vTangent.xyz : -vTangent.xyz);
vec3 bitangent = cross(normal, tangent) * tangentHeadedness;
return mat3(tangent, bitangent, normal);
}`) : x.code.add(n`mat3 computeTangentSpace(vec3 normal) {
float tangentHeadedness = vTangent.w;
vec3 tangent = normalize(vTangent.xyz);
vec3 bitangent = cross(normal, tangent) * tangentHeadedness;
return mat3(tangent, bitangent, normal);
}`)) : x.code.add(n`mat3 computeTangentSpace(vec3 normal, vec3 pos, vec2 st) {
vec3 Q1 = dFdx(pos);
vec3 Q2 = dFdy(pos);
vec2 stx = dFdx(st);
vec2 sty = dFdy(st);
float det = stx.t * sty.s - sty.t * stx.s;
vec3 T = stx.t * Q2 - sty.t * Q1;
T = T - normal * dot(normal, T);
T *= inversesqrt(max(dot(T,T), 1.e-10));
vec3 B = sign(det) * cross(normal, T);
return mat3(T, B, normal);
}`), u2.textureCoordinateType !== d.None && (T2.include(u, u2), x.uniforms.add(u2.bindType === a.Pass ? new s2("normalTexture", (e3) => e3.textureNormal) : new o2("normalTexture", (e3) => e3.textureNormal)), u2.hasNormalTextureTransform && (x.uniforms.add(new e2("scale", (e3) => e3.scale ?? s)), x.uniforms.add(new o3("normalTextureTransformMatrix", (t) => t.normalTextureTransformMatrix ?? o))), x.code.add(n`vec3 computeTextureNormal(mat3 tangentSpace, vec2 uv) {
vec3 rawNormal = textureLookup(normalTexture, uv).rgb * 2.0 - 1.0;`), u2.hasNormalTextureTransform && x.code.add(n`mat3 normalTextureRotation = mat3(normalTextureTransformMatrix[0][0]/scale[0], normalTextureTransformMatrix[0][1]/scale[1], 0.0,
normalTextureTransformMatrix[1][0]/scale[0], normalTextureTransformMatrix[1][1]/scale[1], 0.0,
0.0, 0.0, 0.0 );
rawNormal.xy = (normalTextureRotation * vec3(rawNormal.x, rawNormal.y, 1.0)).xy;`), x.code.add(n`return tangentSpace * rawNormal;
}`));
}

export {
  T
};
//# sourceMappingURL=chunk-UJTHWOB7.js.map
