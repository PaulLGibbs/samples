import {
  r as r2
} from "./chunk-NAL5X7CU.js";
import {
  E as E2,
  o
} from "./chunk-CZQ5YR7Y.js";
import {
  e
} from "./chunk-J2QYVOHP.js";
import {
  l,
  n as n2,
  r
} from "./chunk-NBFYN6KG.js";
import {
  e as e2
} from "./chunk-VRMHJMLF.js";
import {
  n,
  t
} from "./chunk-SURICA27.js";
import {
  $,
  Z,
  _
} from "./chunk-PONPGREB.js";
import {
  C,
  E,
  F,
  L,
  O
} from "./chunk-QNYMCEB7.js";
import {
  i
} from "./chunk-WTKIQJGU.js";

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrush.js
var t2 = class {
  constructor() {
    this.name = this.constructor.name || "UnnamedBrush", this.brushEffect = null;
  }
  prepareState(t4, r4) {
  }
  draw(t4, r4, s3) {
  }
  drawMany(t4, r4, s3) {
    for (const a of r4) a.visible && this.draw(t4, a, s3);
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLBackground.js
var u = class extends t2 {
  constructor() {
    super(...arguments), this._color = r2(1, 0, 0, 1), this._patternMatrix = e2(), this._programOptions = { id: false, pattern: false };
  }
  dispose() {
    this._vao && (this._vao.dispose(), this._vao = null);
  }
  drawMany(e4, r4) {
    const { context: s3, painter: a, requestRender: f2, allowDelayedRender: m } = e4;
    this._loadWGLResources(e4);
    const u3 = e4.displayLevel, p = e4.styleLayer, _2 = p.backgroundMaterial, d2 = a.vectorTilesMaterialManager, g2 = p.getPaintValue("background-color", u3), h = p.getPaintValue("background-opacity", u3), x = p.getPaintValue("background-pattern", u3), M = void 0 !== x, b = 1 | window.devicePixelRatio, v = e4.spriteMosaic;
    let y, w;
    const j = b > _ ? 2 : 1, A = this._programOptions;
    A.pattern = M;
    const L2 = d2.getMaterialProgram(s3, _2, A);
    if (!m || null == f2 || L2.compiled) {
      if (s3.bindVAO(this._vao), s3.useProgram(L2), M) {
        const t4 = v.getMosaicItemPosition(x, true);
        if (null != t4) {
          const { tl: e5, br: r5, page: i2 } = t4;
          y = r5[0] - e5[0], w = r5[1] - e5[1];
          const a2 = v.getPageSize(i2);
          null != a2 && (v.bind(s3, L.LINEAR, i2, Z), L2.setUniform4f("u_tlbr", e5[0], e5[1], r5[0], r5[1]), L2.setUniform2fv("u_mosaicSize", a2), L2.setUniform1i("u_texture", Z));
        }
        L2.setUniform1f("u_opacity", h);
      } else {
        const t4 = g2[3] * h;
        this._color[0] = t4 * g2[0], this._color[1] = t4 * g2[1], this._color[2] = t4 * g2[2], this._color[3] = t4, L2.setUniform4fv("u_color", this._color);
      }
      L2.setUniform1f("u_depth", p.z || 0);
      for (const e5 of r4) {
        if (L2.setUniform1f("u_coord_range", e5.rangeX), L2.setUniformMatrix3fv("u_dvsMat3", e5.transforms.displayViewScreenMat3), M) {
          const r5 = Math.max(2 ** (Math.round(u3) - e5.key.level), 1), o2 = j * e5.width * r5, i2 = o2 / i(y), s4 = o2 / i(w);
          this._patternMatrix[0] = i2, this._patternMatrix[4] = s4, L2.setUniformMatrix3fv("u_pattern_matrix", this._patternMatrix);
        }
        s3.setStencilFunction(O.EQUAL, 0, 255), s3.drawArrays(E.TRIANGLE_STRIP, 0, 4);
      }
    } else f2();
  }
  _loadWGLResources(t4) {
    if (this._vao) return;
    const { context: e4, styleLayer: r4 } = t4, o2 = r4.backgroundMaterial, i2 = new Int8Array([0, 0, 1, 0, 0, 1, 1, 1]), s3 = E2.createVertex(e4, F.STATIC_DRAW, i2), n5 = new o(e4, o2.getAttributeLocations(), o2.getLayoutInfo(), /* @__PURE__ */ new Map([["geometry", s3]]));
    this._vao = n5;
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLCircle.js
var n3 = class extends t2 {
  constructor() {
    super(...arguments), this._programOptions = { id: false };
  }
  dispose() {
  }
  drawMany(t4, n5) {
    const { context: l2, displayLevel: s3, requiredLevel: o2, state: c2, painter: u3, spriteMosaic: d2, styleLayerUID: f2, requestRender: m, allowDelayedRender: p } = t4;
    if (!n5.some((e4) => {
      var _a;
      return ((_a = e4.layerData.get(f2)) == null ? void 0 : _a.circleIndexCount) ?? false;
    })) return;
    const y = t4.styleLayer, g2 = y.circleMaterial, M = u3.vectorTilesMaterialManager, v = 1.2, E3 = y.getPaintValue("circle-translate", s3), x = y.getPaintValue("circle-translate-anchor", s3), I = this._programOptions, U = M.getMaterialProgram(l2, g2, I);
    if (p && null != m && !U.compiled) return void m();
    l2.useProgram(U), U.setUniformMatrix3fv("u_displayMat3", x === r.VIEWPORT ? c2.displayMat3 : c2.displayViewMat3), U.setUniform2fv("u_circleTranslation", E3), U.setUniform1f("u_depth", y.z), U.setUniform1f("u_antialiasingWidth", v);
    let _2 = -1;
    for (const e4 of n5) {
      if (!e4.layerData.has(f2)) continue;
      e4.key.level !== _2 && (_2 = e4.key.level, g2.setDataUniforms(U, s3, y, _2, d2));
      const t5 = e4.layerData.get(f2);
      if (!t5.circleIndexCount) continue;
      t5.prepareForRendering(l2);
      const n6 = t5.vao;
      null != n6 && (l2.bindVAO(n6), U.setUniformMatrix3fv("u_dvsMat3", e4.transforms.displayViewScreenMat3), o2 !== e4.key.level ? l2.setStencilFunction(O.EQUAL, e4.stencilRef, 255) : l2.setStencilFunction(O.GREATER, 255, 255), l2.drawElements(E.TRIANGLES, t5.circleIndexCount, C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * t5.circleIndexStart), e4.triangleCount += t5.circleIndexCount / 3);
    }
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLFill.js
var s = 1 / 65536;
var f = class extends t2 {
  constructor() {
    super(...arguments), this._fillProgramOptions = { id: false, pattern: false }, this._outlineProgramOptions = { id: false };
  }
  dispose() {
  }
  drawMany(t4, e4) {
    const { displayLevel: i2, renderPass: n5, spriteMosaic: a, styleLayerUID: l2 } = t4;
    let r4 = false;
    for (const _2 of e4) if (_2.layerData.has(l2)) {
      const t5 = _2.layerData.get(l2);
      if (t5.fillIndexCount > 0 || t5.outlineIndexCount > 0) {
        r4 = true;
        break;
      }
    }
    if (!r4) return;
    const o2 = t4.styleLayer, s3 = o2.getPaintProperty("fill-pattern"), f2 = void 0 !== s3, u3 = f2 && s3.isDataDriven;
    let c2;
    if (f2 && !u3) {
      const t5 = s3.getValue(i2);
      c2 = a.getMosaicItemPosition(t5, true);
    }
    const d2 = !f2 && o2.getPaintValue("fill-antialias", i2);
    let p = true, m = 1;
    if (!f2) {
      const t5 = o2.getPaintProperty("fill-color"), e5 = o2.getPaintProperty("fill-opacity");
      if (!(t5 == null ? void 0 : t5.isDataDriven) && !(e5 == null ? void 0 : e5.isDataDriven)) {
        const t6 = o2.getPaintValue("fill-color", i2);
        m = o2.getPaintValue("fill-opacity", i2) * t6[3], m >= 1 && (p = false);
      }
    }
    if (p && "opaque" === n5) return;
    const y = o2.getPaintValue("fill-translate", i2), g2 = o2.getPaintValue("fill-translate-anchor", i2);
    (p || "translucent" !== n5) && this._drawFill(t4, l2, o2, e4, y, g2, f2, c2, u3);
    const M = !o2.hasDataDrivenOutlineColor && o2.outlineUsesFillColor && m < 1;
    d2 && "opaque" !== n5 && !M && this._drawOutline(t4, l2, o2, e4, y, g2);
  }
  _drawFill(n5, f2, u3, c2, d2, p, m, y, g2) {
    if (m && !g2 && null == y) return;
    const { context: M, displayLevel: _2, state: E3, painter: v, pixelRatio: P, spriteMosaic: U, requestRender: I, allowDelayedRender: x } = n5, D = u3.fillMaterial, R = v.vectorTilesMaterialManager, S = P > _ ? 2 : 1, T = this._fillProgramOptions;
    T.pattern = m;
    const h = R.getMaterialProgram(M, D, T);
    if (x && null != I && !h.compiled) return void I();
    if (M.useProgram(h), null != y) {
      const { page: t4 } = y, i2 = U.getPageSize(t4);
      null != i2 && (U.bind(M, L.LINEAR, t4, Z), h.setUniform2fv("u_mosaicSize", i2), h.setUniform1i("u_texture", Z));
    }
    h.setUniformMatrix3fv("u_displayMat3", p === r.VIEWPORT ? E3.displayMat3 : E3.displayViewMat3), h.setUniform2fv("u_fillTranslation", d2), h.setUniform1f("u_depth", u3.z + s);
    let w = -1;
    for (const t4 of c2) {
      if (!t4.layerData.has(f2)) continue;
      t4.key.level !== w && (w = t4.key.level, D.setDataUniforms(h, _2, u3, w, U));
      const i2 = t4.layerData.get(f2);
      if (!i2.fillIndexCount) continue;
      i2.prepareForRendering(M);
      const n6 = i2.fillVAO;
      if (null != n6) {
        if (M.bindVAO(n6), h.setUniformMatrix3fv("u_dvsMat3", t4.transforms.displayViewScreenMat3), M.setStencilFunction(O.EQUAL, t4.stencilRef, 255), m) {
          const e4 = Math.max(2 ** (Math.round(_2) - t4.key.level), 1), i3 = t4.rangeX / (S * t4.width * e4);
          h.setUniform1f("u_patternFactor", i3);
        }
        if (g2) {
          const t5 = i2.patternMap;
          if (!t5) continue;
          for (const [i3, n7] of t5) {
            const t6 = U.getPageSize(i3);
            null != t6 && (U.bind(M, L.LINEAR, i3, Z), h.setUniform2fv("u_mosaicSize", t6), h.setUniform1i("u_texture", Z), M.drawElements(E.TRIANGLES, n7[1], C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * n7[0]));
          }
        } else M.drawElements(E.TRIANGLES, i2.fillIndexCount, C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * i2.fillIndexStart);
        t4.triangleCount += i2.fillIndexCount / 3;
      }
    }
  }
  _drawOutline(e4, i2, n5, a, f2, u3) {
    const { context: c2, displayLevel: d2, state: p, painter: m, pixelRatio: y, spriteMosaic: g2, requestRender: M, allowDelayedRender: _2 } = e4, E3 = n5.outlineMaterial, v = m.vectorTilesMaterialManager, P = 0.75 / y, U = this._outlineProgramOptions, I = v.getMaterialProgram(c2, E3, U);
    if (_2 && null != M && !I.compiled) return void M();
    c2.useProgram(I), I.setUniformMatrix3fv("u_displayMat3", u3 === r.VIEWPORT ? p.displayMat3 : p.displayViewMat3), I.setUniform2fv("u_fillTranslation", f2), I.setUniform1f("u_depth", n5.z + s), I.setUniform1f("u_outline_width", P);
    let x = -1;
    for (const t4 of a) {
      if (!t4.layerData.has(i2)) continue;
      t4.key.level !== x && (x = t4.key.level, E3.setDataUniforms(I, d2, n5, x, g2));
      const e5 = t4.layerData.get(i2);
      if (e5.prepareForRendering(c2), !e5.outlineIndexCount) continue;
      const a2 = e5.outlineVAO;
      null != a2 && (c2.bindVAO(a2), I.setUniformMatrix3fv("u_dvsMat3", t4.transforms.displayViewScreenMat3), c2.setStencilFunction(O.EQUAL, t4.stencilRef, 255), c2.drawElements(E.TRIANGLES, e5.outlineIndexCount, C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * e5.outlineIndexStart), t4.triangleCount += e5.outlineIndexCount / 3);
    }
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLLine.js
var s2 = class extends t2 {
  constructor() {
    super(...arguments), this._programOptions = { id: false, pattern: false, sdf: false };
  }
  dispose() {
  }
  drawMany(i2, s3) {
    const { context: l2, displayLevel: f2, state: u3, painter: c2, pixelRatio: d2, spriteMosaic: m, styleLayerUID: p, requestRender: g2, allowDelayedRender: y } = i2;
    if (!s3.some((e4) => {
      var _a;
      return ((_a = e4.layerData.get(p)) == null ? void 0 : _a.lineIndexCount) ?? false;
    })) return;
    const M = i2.styleLayer, E3 = M.lineMaterial, _2 = c2.vectorTilesMaterialManager, v = M.getPaintValue("line-translate", f2), U = M.getPaintValue("line-translate-anchor", f2), I = M.getPaintProperty("line-pattern"), P = void 0 !== I, x = P && I.isDataDriven;
    let D, S;
    if (P && !x) {
      const e4 = I.getValue(f2);
      D = m.getMosaicItemPosition(e4);
    }
    let L2 = false;
    if (!P) {
      const e4 = M.getPaintProperty("line-dasharray");
      if (S = void 0 !== e4, L2 = S && e4.isDataDriven, S && !L2) {
        const t4 = e4.getValue(f2), i3 = M.getDashKey(t4, M.getLayoutValue("line-cap", f2));
        D = m.getMosaicItemPosition(i3);
      }
    }
    const N = 1 / d2, R = this._programOptions;
    R.pattern = P, R.sdf = S;
    const T = _2.getMaterialProgram(l2, E3, R);
    if (y && null != g2 && !T.compiled) return void g2();
    if (l2.useProgram(T), T.setUniformMatrix3fv("u_displayViewMat3", u3.displayViewMat3), T.setUniformMatrix3fv("u_displayMat3", U === r.VIEWPORT ? u3.displayMat3 : u3.displayViewMat3), T.setUniform2fv("u_lineTranslation", v), T.setUniform1f("u_depth", M.z), T.setUniform1f("u_antialiasing", N), D && null != D) {
      const { page: e4 } = D, i3 = m.getPageSize(e4);
      null != i3 && (m.bind(l2, L.LINEAR, e4, Z), T.setUniform2fv("u_mosaicSize", i3), T.setUniform1i("u_texture", Z));
    }
    let V = -1;
    for (const e4 of s3) {
      if (!e4.layerData.has(p)) continue;
      e4.key.level !== V && (V = e4.key.level, E3.setDataUniforms(T, f2, M, V, m));
      const i3 = 2 ** (f2 - V) / d2;
      T.setUniform1f("u_zoomFactor", i3);
      const s4 = e4.layerData.get(p);
      if (!s4.lineIndexCount) continue;
      s4.prepareForRendering(l2);
      const u4 = s4.vao;
      if (null != u4) {
        if (l2.bindVAO(u4), T.setUniformMatrix3fv("u_dvsMat3", e4.transforms.displayViewScreenMat3), l2.setStencilFunction(O.EQUAL, e4.stencilRef, 255), x || L2) {
          const e5 = s4.patternMap;
          if (!e5) continue;
          for (const [i4, a] of e5) {
            const e6 = m.getPageSize(i4);
            null != e6 && (m.bind(l2, L.LINEAR, i4, Z), T.setUniform2fv("u_mosaicSize", e6), T.setUniform1i("u_texture", Z), l2.drawElements(E.TRIANGLES, a[1], C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * a[0]));
          }
        } else l2.drawElements(E.TRIANGLES, s4.lineIndexCount, C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * s4.lineIndexStart);
        e4.triangleCount += s4.lineIndexCount / 3;
      }
    }
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/GeometryUtils.js
var n4 = 128 / Math.PI;
var t3 = 256 / 360;
var r3 = 1 / Math.LN2;
function u2(n5, t4) {
  return (n5 %= t4) >= 0 ? n5 : n5 + t4;
}
function c(n5) {
  return u2(n5 * t3, 256);
}
function e3(n5) {
  return Math.log(n5) * r3;
}

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLSymbol.js
var g = 1 / 65536;
var d = class extends t2 {
  constructor() {
    super(...arguments), this._iconProgramOptions = { id: false, sdf: false }, this._sdfProgramOptions = { id: false }, this._spritesTextureSize = n();
  }
  dispose() {
  }
  drawMany(e4, t4) {
    const i2 = e4.styleLayer;
    this._drawIcons(e4, i2, t4), this._drawText(e4, i2, t4);
  }
  _drawIcons(e4, t4, o2) {
    const { context: f2, displayLevel: c2, painter: u3, spriteMosaic: p, state: m, styleLayerUID: g2, requestRender: d2, allowDelayedRender: y } = e4, _2 = t4.iconMaterial, M = u3.vectorTilesMaterialManager;
    let h, P = false;
    for (const i2 of o2) if (i2.layerData.has(g2) && (h = i2.layerData.get(g2), h.iconPerPageElementsMap.size > 0)) {
      P = true;
      break;
    }
    if (!P) return;
    const U = t4.getPaintValue("icon-translate", c2), E3 = t4.getPaintValue("icon-translate-anchor", c2);
    let T = t4.getLayoutValue("icon-rotation-alignment", c2);
    T === l.AUTO && (T = t4.getLayoutValue("symbol-placement", c2) === n2.POINT ? l.VIEWPORT : l.MAP);
    const x = T === l.MAP, S = t4.getLayoutValue("icon-keep-upright", c2) && x, v = h.isIconSDF, D = this._iconProgramOptions;
    D.sdf = v;
    const V = M.getMaterialProgram(f2, _2, D);
    if (y && null != d2 && !V.compiled) return void d2();
    f2.useProgram(V), V.setUniformMatrix3fv("u_displayViewMat3", T === l.MAP ? m.displayViewMat3 : m.displayMat3), V.setUniformMatrix3fv("u_displayMat3", E3 === r.VIEWPORT ? m.displayMat3 : m.displayViewMat3), V.setUniform2fv("u_iconTranslation", U), V.setUniform1f("u_depth", t4.z), V.setUniform1f("u_mapRotation", c(m.rotation)), V.setUniform1f("u_keepUpright", S ? 1 : 0), V.setUniform1f("u_level", 10 * c2), V.setUniform1i("u_texture", Z), V.setUniform1f("u_fadeDuration", e / 1e3);
    let R = -1;
    for (const i2 of o2) {
      if (!i2.layerData.has(g2)) continue;
      if (i2.key.level !== R && (R = i2.key.level, _2.setDataUniforms(V, c2, t4, R, p)), h = i2.layerData.get(g2), 0 === h.iconPerPageElementsMap.size) continue;
      h.prepareForRendering(f2), h.updateOpacityInfo();
      const a = h.iconVAO;
      if (null != a) {
        f2.bindVAO(a), V.setUniformMatrix3fv("u_dvsMat3", i2.transforms.displayViewScreenMat3), V.setUniform1f("u_time", (performance.now() - h.lastOpacityUpdate) / 1e3);
        for (const [t5, a2] of h.iconPerPageElementsMap) this._renderIconRange(e4, V, a2, t5, i2);
      }
    }
  }
  _renderIconRange(e4, t4, i2, a, r4) {
    const { context: n5, spriteMosaic: o2 } = e4;
    this._spritesTextureSize[0] = o2.getWidth(a) / 4, this._spritesTextureSize[1] = o2.getHeight(a) / 4, t4.setUniform2fv("u_mosaicSize", this._spritesTextureSize), o2.bind(n5, L.LINEAR, a, Z), this._setStencilState(e4, r4), n5.drawElements(E.TRIANGLES, i2[1], C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * i2[0]), r4.triangleCount += i2[1] / 3;
  }
  _drawText(e4, s3, f2) {
    const { context: c2, displayLevel: u3, glyphMosaic: p, painter: m, pixelRatio: d2, spriteMosaic: y, state: _2, styleLayerUID: M, requestRender: h, allowDelayedRender: P } = e4, U = s3.textMaterial, E3 = m.vectorTilesMaterialManager;
    let T, x = false;
    for (const t4 of f2) if (t4.layerData.has(M) && (T = t4.layerData.get(M), T.glyphPerPageElementsMap.size > 0)) {
      x = true;
      break;
    }
    if (!x) return;
    const S = s3.getPaintProperty("text-opacity");
    if (S && !S.isDataDriven && 0 === S.getValue(u3)) return;
    const v = s3.getPaintProperty("text-color"), D = !v || v.isDataDriven || v.getValue(u3)[3] > 0, V = s3.getPaintProperty("text-halo-width"), R = s3.getPaintProperty("text-halo-color"), I = (!V || V.isDataDriven || V.getValue(u3) > 0) && (!R || R.isDataDriven || R.getValue(u3)[3] > 0);
    if (!D && !I) return;
    const A = 24 / 8;
    let w = s3.getLayoutValue("text-rotation-alignment", u3);
    w === l.AUTO && (w = s3.getLayoutValue("symbol-placement", u3) === n2.POINT ? l.VIEWPORT : l.MAP);
    const L2 = w === l.MAP, O2 = s3.getLayoutValue("text-keep-upright", u3) && L2, N = 0.8 * A / d2;
    this._glyphTextureSize || (this._glyphTextureSize = t(p.width / 4, p.height / 4));
    const z = s3.getPaintValue("text-translate", u3), b = s3.getPaintValue("text-translate-anchor", u3), k = this._sdfProgramOptions, G = E3.getMaterialProgram(c2, U, k);
    if (P && null != h && !G.compiled) return void h();
    c2.useProgram(G), G.setUniformMatrix3fv("u_displayViewMat3", w === l.MAP ? _2.displayViewMat3 : _2.displayMat3), G.setUniformMatrix3fv("u_displayMat3", b === r.VIEWPORT ? _2.displayMat3 : _2.displayViewMat3), G.setUniform2fv("u_textTranslation", z), G.setUniform1f("u_depth", s3.z + g), G.setUniform2fv("u_mosaicSize", this._glyphTextureSize), G.setUniform1f("u_mapRotation", c(_2.rotation)), G.setUniform1f("u_keepUpright", O2 ? 1 : 0), G.setUniform1f("u_level", 10 * u3), G.setUniform1i("u_texture", $), G.setUniform1f("u_antialiasingWidth", N), G.setUniform1f("u_fadeDuration", e / 1e3);
    let W = -1;
    for (const t4 of f2) {
      if (!t4.layerData.has(M)) continue;
      if (t4.key.level !== W && (W = t4.key.level, U.setDataUniforms(G, u3, s3, W, y)), T = t4.layerData.get(M), 0 === T.glyphPerPageElementsMap.size) continue;
      T.prepareForRendering(c2), T.updateOpacityInfo();
      const i2 = T.textVAO;
      if (null == i2) continue;
      c2.bindVAO(i2), G.setUniformMatrix3fv("u_dvsMat3", t4.transforms.displayViewScreenMat3), this._setStencilState(e4, t4);
      const a = (performance.now() - T.lastOpacityUpdate) / 1e3;
      G.setUniform1f("u_time", a), T.glyphPerPageElementsMap.forEach((e5, i3) => {
        this._renderGlyphRange(c2, e5, i3, p, G, I, D, t4);
      });
    }
  }
  _renderGlyphRange(e4, t4, i2, a, r4, n5, s3, l2) {
    a.bind(e4, L.LINEAR, i2, $), n5 && (r4.setUniform1f("u_halo", 1), e4.drawElements(E.TRIANGLES, t4[1], C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * t4[0]), l2.triangleCount += t4[1] / 3), s3 && (r4.setUniform1f("u_halo", 0), e4.drawElements(E.TRIANGLES, t4[1], C.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * t4[0]), l2.triangleCount += t4[1] / 3);
  }
  _setStencilState(e4, t4) {
    const { context: i2, is3D: a, stencilSymbols: r4 } = e4;
    if (i2.setStencilTestEnabled(true), r4) return i2.setStencilWriteMask(255), void i2.setStencilFunction(O.ALWAYS, t4.stencilRef, 255);
    i2.setStencilWriteMask(0), a ? i2.setStencilFunction(O.EQUAL, t4.stencilRef, 255) : i2.setStencilFunction(O.GREATER, 255, 255);
  }
};

export {
  t2 as t,
  u,
  n3 as n,
  f,
  s2 as s,
  e3 as e,
  d
};
//# sourceMappingURL=chunk-SXRTBPL6.js.map
