import {
  q
} from "./chunk-LSTHLY5I.js";
import {
  h
} from "./chunk-74NOLTNG.js";

// node_modules/@arcgis/core/views/support/colorUtils.js
async function r(n) {
  var _a, _b;
  await n.whenReady();
  const r2 = ((_a = n.basemapView) == null ? void 0 : _a.baseLayerViews.map((t) => t.layer).toArray()) ?? [];
  if (!r2.length) {
    const r3 = getComputedStyle(n.container).backgroundColor, o3 = r3 ? new h(r3) : void 0;
    return ("background" in n ? (_b = n.background) == null ? void 0 : _b.color : void 0) || (0 !== (o3 == null ? void 0 : o3.a) ? o3 : null) || null;
  }
  const o2 = (await n.takeScreenshot({ format: "png", layers: r2 })).data.data, a = o2.length;
  let e = 0, l = 0, s = 0, u = 0, c = 0, i = 0;
  for (let t = 0; t < a; t += 4) {
    const n2 = o2[t], r3 = o2[t + 1], a2 = o2[t + 2], d = o2[t + 3], h2 = d / 255;
    e += n2 * n2 * h2, l += r3 * r3 * h2, s += a2 * a2 * h2, c += h2, d && (u += d, i++);
  }
  e = Math.round(Math.sqrt(e / c)), l = Math.round(Math.sqrt(l / c)), s = Math.round(Math.sqrt(s / c));
  return new h([e, l, s, u / i / 255]);
}
async function o(t) {
  if (!t) return null;
  const o2 = await r(t);
  return null != o2 ? q(o2) : null;
}

export {
  r,
  o
};
//# sourceMappingURL=chunk-F26FVSU7.js.map
