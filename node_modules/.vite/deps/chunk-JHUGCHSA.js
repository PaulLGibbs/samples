import {
  AccessorController,
  accessorSupport
} from "./chunk-KWHHQQ3U.js";
import {
  p,
  proxyExports,
  trackPropKey,
  watch
} from "./chunk-RCD36GR6.js";

// node_modules/@arcgis/map-components/dist/chunks/useWidget.js
var P = (d, s) => (i, e) => _(i, d, e);
var y = class extends AccessorController {
  constructor(s, i, e) {
    super(s, i, e), this._watchedViewModelProperties = /* @__PURE__ */ new Map(), this._watchedVisibleElementsProperties = {}, this._isBindingViewModel = { value: true }, Object.defineProperty(this.exports, "viewModel", {
      value: accessorSupport.makeGetterProxy(s, this._watchedViewModelProperties, this._isBindingViewModel)
    }), Object.defineProperty(this.exports, "visibleElements", {
      value: this._makeVisibleElementsBinder()
    });
  }
  hostConnected() {
    super.hostConnected(), this._isBindingViewModel.value = false, this.component.manager.loadedCalled && p(this.component);
  }
  async hostLoad() {
    if (this._watchedProperties.delete("viewModel"), await super.hostLoad(), this.component.manager.destroyed)
      return;
    this._watchMetaProperties(), this.onLifecycle(() => this.reactiveUtils.watch(() => this._instance.viewModel, (e) => {
      this.component.viewModel = e;
    }, { initial: true }));
    const s = this._instance.viewModel;
    typeof s == "object" && (accessorSupport.watchComponentUpdates(this, s, this._watchedViewModelProperties), accessorSupport.watchAccessorUpdates(this, s, this._watchedViewModelProperties));
    const i = Object.entries(this._watchedVisibleElementsProperties);
    i.length > 0 && this.onLifecycle(() => i.map(([e, t]) => {
      let n = this._instance.visibleElements ?? {}, o;
      for (const c of t.slice(0, -1))
        if (typeof n[c] == "object" && n[c] !== null)
          n = n[c];
        else {
          o = !!n[c];
          break;
        }
      o === void 0 && (o = !!(n[t.slice(-1)[0]] ?? true)), e.startsWith("hide");
      const m = e.startsWith("hide") || e.startsWith("show") ? e.startsWith("hide") : o, h = this.component;
      return typeof h[e] == "boolean" ? this._updateWidgetVisibleElement(t, h[e], m) : h[e] = false, watch(this.component, e, (c) => this._updateWidgetVisibleElement(t, c, m));
    }));
  }
  _updateWidgetVisibleElement(s, i, e) {
    let t = this._instance.visibleElements ?? {};
    for (const o of s.slice(0, -1))
      (typeof t[o] != "object" || t[o] === null) && (t[o] = {}), t = t[o];
    const n = !!(e ? !i : i);
    t[s.at(-1)] = n;
  }
  hostLoaded() {
    const { el: s } = this.component;
    s.childElem ?? (s.childElem = document.createElement("div")), this._instance.container = s.childElem;
    let i = false;
    const e = this.component.el.view;
    Object.defineProperty(this.component.el, "view", {
      get: () => {
        var _a;
        return this._instance.view ?? ((_a = this._instance.viewModel) == null ? void 0 : _a.view);
      },
      set: (t) => {
        !("view" in this._instance) && typeof this._instance.viewModel == "object" ? this._instance.viewModel.view = t : this._instance.view = t, !("map" in this._instance) && typeof this._instance.viewModel == "object" ? this._instance.viewModel.map = t.map : this._instance.map = t.map, this._watchViewDestroy(), t && !i && (i = true, this.component.arcgisReady.emit());
      },
      configurable: true,
      enumerable: true
    }), e && (this._instance.view = e), this.component.el.isConnected && (p(this.component), this._watchViewDestroy());
  }
  hostDisconnected() {
    var _a, _b;
    typeof this.component.el.childElem == "object" && ((_b = (_a = this.component.el.view) == null ? void 0 : _a.ui) == null ? void 0 : _b.remove(this.component.el.childElem));
  }
  hostDestroy() {
    var _a;
    super.hostDestroy(), (_a = this._watchViewDestroyHandle) == null ? void 0 : _a.remove();
  }
  _watchMetaProperties() {
    watch(this.component, "position", (s) => {
      this.component.el.childElem != null && typeof s == "string" && p(this.component);
    }), watch(this.component, "referenceElement", () => p(this.component));
  }
  _makeVisibleElementsBinder() {
    return new Proxy({}, {
      has: (s, i) => typeof i == "string" || Reflect.has(s, i),
      get: (s, i) => {
        if (typeof i == "symbol" || i in Promise.prototype)
          return Reflect.get(s, i);
        const e = [i], t = this._makeRecursiveProxy(e);
        return trackPropKey(this.component, (n) => {
          const o = n;
          this.component[o] = false, this._watchedVisibleElementsProperties[o] = e;
        }, t);
      }
    });
  }
  _makeRecursiveProxy(s) {
    const i = new Proxy({}, {
      get: (e, t) => {
        const n = Reflect.get(e, t);
        return typeof t == "symbol" || t in Promise.prototype ? n : (s.push(t), i);
      }
    });
    return i;
  }
  /**
   * Destroy the component if view gets destroyed.
   *
   * When view is destroyed, component disconnect is triggered, which will
   * destroy the component in 1s but that can be too late, because in the
   * meanwhile, a watcher on the widget/viewModel instance may get triggered,
   * and throw an error if it's not able to access this.view.map or etc
   * (happens in VersionManagementViewModel for example).
   *
   * We can't check for this in disconnectedCallback though as, if the entire
   * component tree is removed, the disconnect for the component will be
   * triggered before that of arcgis-map, so the view might not be destroyed
   * yet at that point.
   */
  _watchViewDestroy() {
    var _a;
    (_a = this._watchViewDestroyHandle) == null ? void 0 : _a.remove(), this._watchViewDestroyHandle = this.reactiveUtils.when(() => {
      var _a2;
      return ((_a2 = this.component.el.view) == null ? void 0 : _a2.destroyed) === true;
    }, () => void this.component.destroy().catch(console.error), { sync: true });
  }
};
var _ = proxyExports(y);

export {
  P
};
/*! Bundled license information:

@arcgis/map-components/dist/chunks/useWidget.js:
  (*! All material copyright Esri, All Rights Reserved, unless otherwise specified.
  See https://js.arcgis.com/4.32/esri/copyright.txt for details.
  v4.32.14 *)
*/
//# sourceMappingURL=chunk-JHUGCHSA.js.map
