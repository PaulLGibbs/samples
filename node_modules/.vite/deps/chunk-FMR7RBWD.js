import {
  h as h2,
  i as i2,
  m as m3,
  s as s2,
  x
} from "./chunk-7WON6OJK.js";
import {
  B,
  V,
  ae,
  j as j3,
  me,
  oe,
  re,
  te,
  ue,
  z as z2
} from "./chunk-NZ5HC6WX.js";
import {
  _
} from "./chunk-4SWL27WH.js";
import {
  N,
  a as a3,
  g as g2,
  i
} from "./chunk-ERSI52WX.js";
import {
  p as p2
} from "./chunk-S2MAQLXM.js";
import {
  j as j2
} from "./chunk-3V2MGHEO.js";
import {
  D,
  R,
  U,
  l2 as l,
  u2 as u
} from "./chunk-DDTJ4F3F.js";
import {
  n as n2
} from "./chunk-LBKY47DP.js";
import {
  z
} from "./chunk-4LTIFRBM.js";
import {
  p
} from "./chunk-A3XTXVRC.js";
import {
  a as a2
} from "./chunk-EIJWPXD4.js";
import {
  y as y2
} from "./chunk-LV7Q73OX.js";
import {
  w
} from "./chunk-WLJYVFYC.js";
import {
  j
} from "./chunk-DJTR6MPM.js";
import {
  g
} from "./chunk-LIJLNNZB.js";
import {
  P,
  y
} from "./chunk-D32ASUJF.js";
import {
  S
} from "./chunk-SXNZ7EJ6.js";
import {
  r
} from "./chunk-SCZRL2WP.js";
import {
  m as m2
} from "./chunk-HITYQXMG.js";
import {
  a2 as a
} from "./chunk-4XNJLQ7P.js";
import {
  h
} from "./chunk-F7UUQSMD.js";
import {
  m
} from "./chunk-C4UPF7I2.js";
import {
  s
} from "./chunk-CAZ5FG6J.js";
import {
  n
} from "./chunk-3TGCPCOH.js";

// node_modules/@arcgis/core/layers/support/rasterDatasets/BaseRaster.js
var N2 = 8;
var V2 = 256;
var U2 = 0;
var $ = class extends S {
  constructor() {
    super(...arguments), this._tileFetchQueue = new _({ concurrency: 32, process: (e, t) => this._fetchRawTile(e.pyramidLevel, e.row, e.col, { ...e.options, signal: t }) }), this.datasetName = null, this.datasetFormat = null, this.hasUniqueSourceStorageInfo = true, this.rasterInfo = null, this.ioConfig = { sampling: "closest" };
  }
  normalizeCtorArgs(e) {
    return (e == null ? void 0 : e.ioConfig) && (e = { ...e, ioConfig: { resolution: null, bandIds: null, sampling: "closest", tileInfo: z.create(), ...e.ioConfig } }), e;
  }
  get _isGlobalWrappableSource() {
    const { rasterInfo: e } = this, t = te(e.spatialReference);
    return null != t && e.extent.width >= t / 2;
  }
  get _hasNoneOrGCSShiftTransform() {
    const { transform: e } = this.rasterInfo;
    return null == e || "gcs-shift" === e.type;
  }
  set rasterJobHandler(e) {
    var _a, _b;
    this._set("rasterJobHandler", e), "Function" === this.datasetFormat && ((_b = (_a = this.primaryRasters) == null ? void 0 : _a.rasters) == null ? void 0 : _b.forEach((t) => t.rasterJobHandler = e));
  }
  get rasterId() {
    return this.url || "rasterId-" + U2++;
  }
  set url(e) {
    this._set("url", y(e, n.getLogger(this)));
  }
  async open(e) {
    return this._openPromise ?? (this._openPromise = j3().then(() => this._open(e))), this._openPromise;
  }
  async fetchTile(e, t, i3, o = {}) {
    const r2 = o.tileInfo || this.rasterInfo.storageInfo.tileInfo, n3 = this.getTileExtentFromTileInfo(e, t, i3, r2);
    return o = { noClip: true, ...o }, this.fetchPixels(n3, r2.size[0], r2.size[1], o);
  }
  async identify(e, t = {}) {
    var _a;
    e = h(j, e).clone().normalize();
    const { multidimensionalDefinition: i3, timeExtent: o } = t, { rasterInfo: r2 } = this, { hasMultidimensionalTranspose: n3, multidimensionalInfo: s3 } = r2;
    let { transposedVariableName: l2 } = t;
    const c = null != s3 && n3 && (null != o || g2(i3));
    if (c && !l2) {
      l2 = null != i3 && i3.length > 0 ? i3[0].variableName ?? void 0 : s3.variables[0].name, t = { ...t, transposedVariableName: l2 };
    }
    t = this._getRequestOptionsWithSliceId(t);
    const { spatialReference: u2, extent: h3 } = r2, { datumTransformation: p3 } = t;
    let d = B(e, u2, p3);
    if (!h3.intersects(d)) return { location: d, value: null };
    if (null != r2.transform) {
      const e2 = r2.transform.inverseTransform(d);
      if (!r2.nativeExtent.intersects(e2)) return { location: e2, value: null };
      d = e2;
    }
    let x2 = 0;
    const y3 = null != l2 && null != s3 && r2.hasMultidimensionalTranspose;
    if ("Function" === this.datasetFormat) {
      const e2 = this.primaryRasters.rasters[0];
      if (y3) return e2.identify(d, t);
      const { pixelSize: o2 } = r2, n4 = 3, s4 = o2.x * n4 / 2, l3 = o2.y * n4 / 2, a4 = new w({ xmin: d.x - s4, xmax: d.x + s4, ymin: d.y - l3, ymax: d.y + l3, spatialReference: u2 }), c2 = { interpolation: "nearest", multidimensionalDefinition: i3, sliceId: t.sliceId }, { pixelBlock: m4 } = await e2.fetchPixels(a4, n4, n4, c2), { pixelBlock: h4 } = await this.fetchPixels(a4, n4, n4, c2);
      if (null == m4) return { location: d, value: null };
      const p4 = Math.floor(n4 * n4 * 0.5), x3 = !m4.mask || m4.mask[p4] ? m4.pixels.map((e3) => e3[p4]) : null;
      let g4;
      return null != h4 && (g4 = !h4.mask || h4.mask[p4] ? h4.pixels.map((e3) => e3[p4]) : void 0), { location: d, value: x3, processedValue: g4, pyramidLevel: 0 };
    }
    if (!y3) {
      if (t.srcResolution) {
        x2 = me(t.srcResolution, r2, this.ioConfig.sampling).pyramidLevel;
      } else if (x2 = await this.computeBestPyramidLevelForLocation(e, t), null == x2) return { location: d, value: null };
    }
    const g3 = this.identifyPixelLocation(d, x2, null, y3);
    if (null === g3) return { location: d, value: null };
    const { row: R2, col: w2, rowOffset: S2, colOffset: b, blockWidth: P2 } = g3, B2 = l2 ?? t.sliceId, _2 = i2(this.rasterId, B2), M = `${x2}/${R2}/${w2}`;
    let C = m3(_2, null, M);
    null == C && (C = this.fetchRawTile(x2, R2, w2, t), h2(_2, null, M, C));
    const W = await C;
    if (!((_a = W == null ? void 0 : W.pixels) == null ? void 0 : _a.length)) return { location: d, value: null };
    const j4 = S2 * P2 + b;
    return this._processIdentifyResult(W, { srcLocation: d, position: j4, pyramidLevel: x2, useTransposedTile: !!y3, requestSomeSlices: c, identifyOptions: t });
  }
  async fetchPixels(e, t, i3, o = {}) {
    e = ue(e), o = this._getRequestOptionsWithSliceId(o);
    const { _hasNoneOrGCSShiftTransform: r2 } = this;
    if (o.requestRawData && r2) return this._fetchPixels(e, t, i3, o);
    const n3 = te(e.spatialReference), s3 = oe(e);
    if (null == n3 || 0 === s3 || 1 === s3 && this._isGlobalWrappableSource && r2) return this._fetchPixels(e, t, i3, o);
    if (s3 >= 3) return { extent: e, pixelBlock: null };
    const l2 = [], { xmin: a4, xmax: c } = e, m4 = Math.round(n3 / (c - a4) * t), u2 = m4 - Math.round((n3 / 2 - a4) / (c - a4) * t);
    let h3 = 0;
    const p3 = [];
    for (let g3 = 0; g3 <= s3; g3++) {
      const r3 = new w({ xmin: 0 === g3 ? a4 : -n3 / 2, xmax: g3 === s3 ? c - n3 * g3 : n3 / 2, ymin: e.ymin, ymax: e.ymax, spatialReference: e.spatialReference }), d2 = 0 === g3 ? m4 - u2 : g3 === s3 ? t - h3 : m4;
      h3 += d2, p3.push(d2);
      const x3 = o.disableWrapAround && g3 > 0 ? null : this._fetchPixels(r3, d2, i3, o);
      l2.push(x3);
    }
    const d = (await Promise.all(l2)).map((e2) => e2 == null ? void 0 : e2.pixelBlock);
    let x2 = null;
    const y3 = { width: t, height: i3 };
    if (this.rasterJobHandler) {
      x2 = (await this.rasterJobHandler.mosaicAndTransform({ srcPixelBlocks: d, srcMosaicSize: y3, destDimension: null, coefs: null, sampleSpacing: null, interpolation: "nearest", alignmentInfo: null, blockWidths: p3 }, o)).pixelBlock;
    } else x2 = U(d, y3, { blockWidths: p3 });
    return { extent: e, srcExtent: V(e, this.rasterInfo.spatialReference, o.datumTransformation), pixelBlock: x2 };
  }
  async fetchRawPixels(e, t, i3, o = {}) {
    t = { x: Math.floor(t.x), y: Math.floor(t.y) };
    const r2 = await this._fetchRawTiles(e, t, i3, o), { nativeExtent: n3, nativePixelSize: s3, storageInfo: l2 } = this.rasterInfo, a4 = 2 ** e, c = s3.x * a4, m4 = s3.y * a4, u2 = new w({ xmin: n3.xmin + c * t.x, xmax: n3.xmin + c * (t.x + i3.width - 1), ymin: n3.ymax - m4 * (t.y + i3.height - 1), ymax: n3.ymax - m4 * t.y, spatialReference: n3.spatialReference });
    if (!r2) return { extent: u2, srcExtent: u2, pixelBlock: null };
    const { pixelBlocks: h3, mosaicSize: p3 } = r2;
    if (1 === h3.length && null != h3[0] && h3[0].width === i3.width && h3[0].height === i3.height) return { extent: u2, srcExtent: u2, pixelBlock: r2.pixelBlocks[0] };
    const d = e > 0 ? l2.pyramidBlockWidth : l2.blockWidth, x2 = e > 0 ? l2.pyramidBlockHeight : l2.blockHeight, y3 = { x: t.x % d, y: t.y % x2 };
    let g3;
    if (this.rasterJobHandler) {
      g3 = (await this.rasterJobHandler.mosaicAndTransform({ srcPixelBlocks: h3, srcMosaicSize: p3, destDimension: i3, clipOffset: y3, clipSize: i3, coefs: null, sampleSpacing: null, interpolation: o.interpolation, alignmentInfo: null, blockWidths: null }, o)).pixelBlock;
    } else g3 = U(h3, p3, { clipOffset: y3, clipSize: i3 });
    return { extent: u2, srcExtent: u2, pixelBlock: g3 };
  }
  fetchRawTile(e, t, o, r2) {
    throw new s("BaseRaster:read-not-implemented", "fetchRawTile() is not implemented");
  }
  computeExtent(e) {
    return V(this.rasterInfo.extent, e);
  }
  decodePixelBlock(e, t) {
    return !this.rasterJobHandler || t.useCanvas ? j2(e, t) : this.rasterJobHandler.decode({ data: e, options: t });
  }
  async request(e, i3, o = 0) {
    const { customFetchParameters: r2 } = this.ioConfig, { range: n3, query: s3, headers: l2 } = i3;
    o = o ?? i3.retryCount ?? this.ioConfig.retryCount;
    const a4 = n3 ? { Range: `bytes=${n3.from}-${n3.to}` } : null;
    try {
      return await P(e, { ...i3, query: { ...s3, ...r2 }, headers: { ...l2, ...a4 } });
    } catch (c) {
      if (o > 0) return o--, this.request(e, i3, o);
      throw c;
    }
  }
  getSliceIndex(e) {
    const { multidimensionalInfo: t } = this.rasterInfo;
    return null == t || null == e || 0 === e.length ? null : N(e, t);
  }
  getTileExtentFromTileInfo(e, t, i3, o) {
    const r2 = o.lodAt(e);
    return this.getTileExtent({ x: r2.resolution, y: r2.resolution }, t, i3, o.origin, o.spatialReference, o.size);
  }
  updateTileInfo() {
    const { storageInfo: e, spatialReference: t, extent: i3, pixelSize: o } = this.rasterInfo, { pyramidResolutions: r2 } = e;
    if (!e.tileInfo) {
      const n3 = [], s3 = e.maximumPyramidLevel || 0;
      let l2 = (o.x + o.y) / 2, a4 = 1 / 0.0254 * 96 * l2;
      for (let e2 = 0; e2 <= s3 && (n3.unshift(new p({ level: s3 - e2, resolution: l2, scale: a4 })), e2 !== s3); e2++) if (r2) {
        const t2 = (r2[e2].x + r2[e2].y) / 2;
        a4 *= t2 / l2, l2 = t2;
      } else l2 *= 2, a4 *= 2;
      const c = new j({ x: i3.xmin, y: i3.ymax, spatialReference: t });
      e.tileInfo = new z({ origin: c, size: [e.blockWidth, e.blockHeight], spatialReference: t, lods: n3 }), e.isVirtualTileInfo = true;
    }
  }
  createRemoteDatasetStorageInfo(e, t = 512, i3 = 512, o) {
    const { width: r2, height: n3, nativeExtent: s3, pixelSize: l2, spatialReference: a4 } = e, c = new j({ x: s3.xmin, y: s3.ymax, spatialReference: a4 });
    null == o && (o = Math.max(0, Math.round(Math.log(Math.max(r2, n3)) / Math.LN2 - 8)));
    const f = this.computeBlockBoundary(s3, 512, 512, { x: s3.xmin, y: s3.ymax }, [l2], o);
    e.storageInfo = new n2({ blockWidth: t, blockHeight: i3, pyramidBlockWidth: t, pyramidBlockHeight: i3, origin: c, firstPyramidLevel: 1, maximumPyramidLevel: o, blockBoundary: f });
  }
  async computeBestPyramidLevelForLocation(e, t = {}) {
    return 0;
  }
  computeBlockBoundary(e, t, i3, o, r2, n3 = 0, s3 = 2) {
    if (1 === r2.length && n3 > 0) {
      r2 = [...r2];
      let { x: e2, y: t2 } = r2[0];
      for (let i4 = 0; i4 < n3; i4++) e2 *= s3, t2 *= s3, r2.push({ x: e2, y: t2 });
    }
    const l2 = [], { x: a4, y: c } = o;
    for (let f = 0; f < r2.length; f++) {
      const { x: o2, y: n4 } = r2[f];
      l2.push({ minCol: Math.floor((e.xmin - a4 + 0.1 * o2) / t / o2), maxCol: Math.floor((e.xmax - a4 - 0.1 * o2) / t / o2), minRow: Math.floor((c - e.ymax + 0.1 * n4) / i3 / n4), maxRow: Math.floor((c - e.ymin - 0.1 * n4) / i3 / n4) });
    }
    return l2;
  }
  getPyramidPixelSize(e) {
    const { nativePixelSize: t } = this.rasterInfo, { pyramidResolutions: i3, pyramidScalingFactor: o } = this.rasterInfo.storageInfo;
    if (0 === e) return t;
    if (null != i3 && i3.length) return i3[e - 1];
    const r2 = o ** e;
    return { x: t.x * r2, y: t.y * r2 };
  }
  identifyPixelLocation(e, t, i3, o) {
    const { spatialReference: r2, nativeExtent: n3, storageInfo: s3 } = this.rasterInfo, { maximumPyramidLevel: l2, origin: a4, transposeInfo: c } = s3, f = o && null != c ? c.tileSize[0] : s3.blockWidth, m4 = o && null != c ? c.tileSize[1] : s3.blockHeight, u2 = B(e, r2, i3);
    if (!n3.intersects(u2)) return null;
    if (t < 0 || t > l2) return null;
    const h3 = this.getPyramidPixelSize(t), { x: p3, y: d } = h3, x2 = (a4.y - u2.y) / d / m4, y3 = (u2.x - a4.x) / p3 / f, g3 = Math.min(m4 - 1, Math.floor((x2 - Math.floor(x2)) * m4)), I = Math.min(f - 1, Math.floor((y3 - Math.floor(y3)) * f));
    return { pyramidLevel: t, row: Math.floor(x2), col: Math.floor(y3), rowOffset: g3, colOffset: I, blockWidth: f, srcLocation: u2 };
  }
  getTileExtent(e, t, i3, o, r2, n3) {
    const [s3, l2] = n3, a4 = o.x + i3 * s3 * e.x, c = a4 + s3 * e.x, m4 = o.y - t * l2 * e.y, u2 = m4 - l2 * e.y;
    return new w({ xmin: a4, xmax: c, ymin: u2, ymax: m4, spatialReference: r2 });
  }
  getBlockWidthHeight(e) {
    return { blockWidth: e > 0 ? this.rasterInfo.storageInfo.pyramidBlockWidth : this.rasterInfo.storageInfo.blockWidth, blockHeight: e > 0 ? this.rasterInfo.storageInfo.pyramidBlockHeight : this.rasterInfo.storageInfo.blockHeight };
  }
  isBlockOutside(e, t, i3) {
    const o = this.rasterInfo.storageInfo.blockBoundary[e];
    return !o || o.maxRow < t || o.maxCol < i3 || o.minRow > t || o.minCol > i3;
  }
  updateImageSpaceRasterInfo(e) {
    const { pixelSize: t } = e, { width: i3, height: o } = e, r2 = g.WebMercator;
    e.spatialReference = r2, e.extent = e.nativeExtent = new w({ xmin: -0.5, ymax: 0.5, xmax: i3 - 0.5, ymin: 0.5 - o, spatialReference: r2 }), e.isPseudoSpatialReference = true, e.transform = null, e.pixelSize = new j({ x: 1, y: 1, spatialReference: r2 });
    const { extent: n3, storageInfo: s3 } = e;
    if (s3) {
      s3.origin = new j({ x: n3.xmin, y: n3.ymax, spatialReference: r2 });
      const { pyramidResolutions: i4, tileInfo: o2 } = s3;
      if (i4 && i4.forEach((e2) => {
        e2.x /= t.x, e2.y /= t.y;
      }), o2) {
        o2.origin = s3.origin;
        const t2 = (e.nativePixelSize.x + e.nativePixelSize.y) / 2;
        o2.lods.forEach((e2, i5) => {
          e2.resolution = t2 * 2 ** i5, e2.scale = 96 * e2.resolution / 0.0254;
        });
      }
    }
  }
  async _fetchPixels(e, t, i3, o = {}) {
    let r2 = oe(e);
    if (r2 >= 2) return { extent: e, pixelBlock: null };
    const n3 = this._getSourceDataInfo(e, t, i3, o), { pyramidLevel: s3, srcResolution: l2, srcExtent: a4, srcWidth: c, srcHeight: f, ul: u2 } = n3;
    if (0 === c || 0 === f) return { extent: e, srcExtent: a4, pixelBlock: null };
    const { rasterInfo: h3 } = this, p3 = h3.transform, d = "gcs-shift" === (p3 == null ? void 0 : p3.type), x2 = null != te(e.spatialReference);
    !d && x2 || (r2 = oe(n3.srcExtent, d));
    const y3 = await this._fetchRawTiles(s3, u2, { width: c, height: f, wrapCount: r2 }, o);
    if (!y3) return { extent: e, srcExtent: a4, pixelBlock: null };
    const g3 = h3.storageInfo, I = s3 > 0 ? g3.pyramidBlockWidth : g3.blockWidth, R2 = s3 > 0 ? g3.pyramidBlockHeight : g3.blockHeight;
    let { x: w2, y: S2 } = h3.pixelSize;
    if (s3 > 0) {
      const { pyramidResolutions: e2, pyramidScalingFactor: t2 } = g3;
      if (null != e2 && e2[s3 - 1]) ({ x: w2, y: S2 } = e2[s3 - 1]);
      else {
        const e3 = t2 ** s3;
        w2 *= e3, S2 *= e3;
      }
    }
    const k = h3.spatialReference, T = new j({ x: w2, y: S2, spatialReference: k }), v = I === c && R2 === f && u2.x % I == 0 && u2.y % R2 == 0, b = new j({ x: (e.xmax - e.xmin) / t, y: (e.ymax - e.ymin) / i3, spatialReference: e.spatialReference }), P2 = !e.spatialReference.equals(k), B2 = k.isGeographic ? 1e-9 : 1e-4, { datumTransformation: _2 } = o;
    if (!P2 && v && 1 === y3.pixelBlocks.length && I === t && R2 === i3 && Q(l2, b, B2)) return { extent: e, srcExtent: a4, srcTilePixelSize: T, pixelBlock: y3.pixelBlocks[0] };
    const H = x2 && null != te(a4.spatialReference) && this._hasNoneOrGCSShiftTransform, L = o.requestProjectedLocalDirections && this.rasterInfo.dataType.startsWith("vector");
    L && !this.rasterJobHandler && await j3();
    const E = this.rasterJobHandler ? await this.rasterJobHandler.getProjectionOffsetGrid({ projectedExtent: e, srcBufferExtent: y3.extent, pixelSize: b.toJSON(), datumTransformation: _2, rasterTransform: p3, hasWrapAround: r2 > 0 || H, isAdaptive: false !== this.ioConfig.optimizeProjectionAccuracy, includeGCSGrid: L }, o) : ae({ projectedExtent: e, srcBufferExtent: y3.extent, pixelSize: b, datumTransformation: _2, rasterTransform: p3, hasWrapAround: r2 > 0 || H, isAdaptive: false, includeGCSGrid: L });
    let F;
    const A = !o.requestRawData, G = { rows: E.spacing[0], cols: E.spacing[1] }, J = this._hasNoneOrGCSShiftTransform ? this._getRasterTileAlignmentInfo(s3, y3.extent.xmin) : void 0, { pixelBlocks: N3, mosaicSize: V3, isPartiallyFilled: U3 } = y3;
    let $2 = null;
    if (this.rasterJobHandler) {
      const e2 = await this.rasterJobHandler.mosaicAndTransform({ srcPixelBlocks: N3, srcMosaicSize: V3, destDimension: A ? { width: t, height: i3 } : null, coefs: A ? E.coefficients : null, sampleSpacing: A ? G : null, projectDirections: L, gcsGrid: L ? E.gcsGrid : null, isUV: "vector-uv" === this.rasterInfo.dataType, interpolation: o.interpolation, alignmentInfo: J, blockWidths: null }, o);
      ({ pixelBlock: F, localNorthDirections: $2 } = e2);
    } else {
      const e2 = U(N3, V3, { alignmentInfo: J });
      F = A ? D(e2, { width: t, height: i3 }, E.coefficients, G, o.interpolation) : e2, L && E.gcsGrid && ($2 = R({ width: t, height: i3 }, E.gcsGrid), F = u(F, this.rasterInfo.dataType, $2));
    }
    return o.requestRawData || L ? { extent: e, srcExtent: a4, srcTilePixelSize: T, pixelBlock: F, transformGrid: E, localNorthDirections: $2, isPartiallyFilled: U3 } : { extent: e, srcExtent: a4, srcTilePixelSize: T, pixelBlock: F };
  }
  async _fetchRawTiles(e, t, i3, o) {
    const { origin: r2, blockBoundary: n3 } = this.rasterInfo.storageInfo, { blockWidth: s3, blockHeight: l2 } = this.getBlockWidthHeight(e);
    let { x: a4, y: c } = t, { width: m4, height: u2, wrapCount: h3 } = i3;
    const p3 = this._getRasterTileAlignmentInfo(e, 0);
    o.buffer && (a4 -= o.buffer.cols, c -= o.buffer.rows, m4 += 2 * o.buffer.cols, u2 += 2 * o.buffer.rows);
    let d = 0, x2 = 0, y3 = 0;
    if (h3 && null != p3) {
      ({ worldColumnCountFromOrigin: x2, originColumnOffset: y3, rightPadding: d } = p3);
      x2 * p3.blockWidth - d >= a4 + m4 && (d = 0);
    }
    const g3 = Math.floor(a4 / s3), I = Math.floor(c / l2), R2 = Math.floor((a4 + m4 + d - 1) / s3), w2 = Math.floor((c + u2 + d - 1) / l2), S2 = n3[e];
    if (!S2) return null;
    const { minRow: k, minCol: T, maxCol: v, maxRow: b } = S2;
    if (0 === h3 && (w2 < k || R2 < T || I > b || g3 > v)) return null;
    const P2 = new Array();
    let B2 = false;
    const _2 = null == this.ioConfig.allowPartialFill ? o.allowPartialFill : this.ioConfig.allowPartialFill;
    for (let f = I; f <= w2; f++) for (let t2 = g3; t2 <= R2; t2++) {
      let i4 = t2;
      if (!o.disableWrapAround && h3 && null != p3 && x2 <= t2 && (i4 = t2 - x2 - y3), f >= k && i4 >= T && b >= f && v >= i4) {
        const t3 = this._tileFetchQueue.push({ pyramidLevel: e, row: f, col: i4, options: o }, { signal: o.signal });
        _2 ? P2.push(new Promise((e2) => {
          t3.then((t4) => e2(t4)).catch(() => {
            B2 = true, e2(null);
          });
        })) : P2.push(t3);
      } else P2.push(Promise.resolve(null));
    }
    if (0 === P2.length) return null;
    const M = await Promise.all(P2), C = { height: (w2 - I + 1) * l2, width: (R2 - g3 + 1) * s3 }, { spatialReference: W } = this.rasterInfo, j4 = this.getPyramidPixelSize(e), { x: z3, y: H } = j4;
    return { extent: new w({ xmin: r2.x + g3 * s3 * z3, xmax: r2.x + (R2 + 1) * s3 * z3, ymin: r2.y - (w2 + 1) * l2 * H, ymax: r2.y - I * l2 * H, spatialReference: W }), pixelBlocks: M, mosaicSize: C, isPartiallyFilled: B2 };
  }
  _fetchRawTile(e, t, i3, o) {
    const r2 = this.rasterInfo.storageInfo.blockBoundary[e];
    if (!r2) return Promise.resolve(null);
    const { minRow: s3, minCol: l2, maxCol: a4, maxRow: c } = r2;
    if (t < s3 || i3 < l2 || t > c || i3 > a4) return Promise.resolve(null);
    const f = i2(this.rasterId, o.sliceId), m4 = `${e}/${t}/${i3}`;
    let u2 = m3(f, o.registryId, m4);
    if (null == u2) {
      const r3 = new AbortController();
      u2 = this.fetchRawTile(e, t, i3, { ...o, signal: r3.signal }), h2(f, o.registryId, m4, u2, r3), u2.catch(() => x(f, o.registryId, m4));
    }
    return o.signal && m(o, () => {
      s2(f, o.registryId, m4);
    }), u2;
  }
  _computeMagDirValues(e) {
    var _a;
    const { bandCount: t, dataType: i3 } = this.rasterInfo;
    if (!(2 === t && "vector-magdir" === i3 || "vector-uv" === i3) || 2 !== (e == null ? void 0 : e.length) || !((_a = e[0]) == null ? void 0 : _a.length)) return null;
    const o = e[0].length;
    if ("vector-magdir" === i3) {
      const t2 = e[1].map((e2) => (e2 + 360) % 360);
      return [e[0], t2];
    }
    const [r2, n3] = e, s3 = [], l2 = [];
    for (let a4 = 0; a4 < o; a4++) {
      const [e2, t2] = l([r2[a4], n3[a4]]);
      s3.push(e2), l2.push(t2);
    }
    return [s3, l2];
  }
  _getRasterTileAlignmentInfo(e, t) {
    return null == this._rasterTileAlignmentInfo && (this._rasterTileAlignmentInfo = re(this.rasterInfo)), null == this._rasterTileAlignmentInfo.pyramidsInfo ? null : { startX: t, halfWorldWidth: this._rasterTileAlignmentInfo.halfWorldWidth, hasGCSSShiftTransform: this._rasterTileAlignmentInfo.hasGCSSShiftTransform, ...this._rasterTileAlignmentInfo.pyramidsInfo[e] };
  }
  _getSourceDataInfo(e, t, i3, o = {}) {
    const r2 = { datumTransformation: o.datumTransformation, pyramidLevel: 0, pyramidResolution: null, srcExtent: null, srcHeight: 0, srcResolution: null, srcWidth: 0, ul: { x: 0, y: 0 } };
    o.srcResolution && (r2.srcResolution = o.srcResolution, this._updateSourceDataInfo(e, r2));
    const n3 = this.rasterInfo.storageInfo.maximumPyramidLevel || 0, { srcWidth: s3, srcHeight: l2, pyramidLevel: a4 } = r2, c = s3 / t, f = l2 / i3, u2 = a4 < n3 && c * f >= 16, h3 = a4 === n3 && this._requireTooManySrcTiles(s3, l2, t, i3);
    if (u2 || h3 || (0 === s3 || 0 === l2)) {
      const s4 = new j({ x: (e.xmax - e.xmin) / t, y: (e.ymax - e.ymin) / i3, spatialReference: e.spatialReference });
      let l3 = z2(s4, this.rasterInfo.spatialReference, e, r2.datumTransformation);
      const h4 = !l3 || o.srcResolution && l3.x + l3.y < o.srcResolution.x + o.srcResolution.y;
      if (u2 && o.srcResolution && h4) {
        const e2 = Math.round(Math.log(Math.max(c, f)) / Math.LN2) - 1;
        if (n3 - a4 + 3 >= e2) {
          const t2 = 2 ** e2;
          l3 = { x: o.srcResolution.x * t2, y: o.srcResolution.y * t2 };
        }
      }
      l3 && (r2.srcResolution = l3, this._updateSourceDataInfo(e, r2));
    }
    return this._requireTooManySrcTiles(r2.srcWidth, r2.srcHeight, t, i3) && (r2.srcWidth = 0, r2.srcHeight = 0), r2;
  }
  _requireTooManySrcTiles(e, t, i3, o) {
    const { tileInfo: r2 } = this.rasterInfo.storageInfo, n3 = Math.ceil(e / r2.size[0]) * Math.ceil(t / r2.size[1]), s3 = e / i3, l2 = t / o, a4 = Math.max(1, (i3 + o) / 1024);
    return n3 >= V2 * a4 || s3 > N2 || l2 > N2;
  }
  _updateSourceDataInfo(e, t) {
    t.srcWidth = 0, t.srcHeight = 0;
    const { rasterInfo: i3 } = this, o = i3.spatialReference, { srcResolution: r2, datumTransformation: n3 } = t, { pyramidLevel: s3, pyramidResolution: l2, excessiveReading: a4 } = me(r2, i3, this.ioConfig.sampling);
    if (a4) return;
    let c = t.srcExtent || V(e, o, n3);
    if (null == c) return;
    const f = i3.transform;
    f && (c = f.inverseTransform(c)), t.srcExtent = c;
    const { x: m4, y: u2 } = i3.storageInfo.origin, h3 = Math.floor((c.xmin - m4) / l2.x + 0.1), p3 = Math.floor((u2 - c.ymax) / l2.y + 0.1), d = Math.floor((c.xmax - m4) / l2.x - 0.1), x2 = Math.floor((u2 - c.ymin) / l2.y - 0.1), y3 = c.width < 0.1 * l2.x ? 0 : d - h3 + 1, g3 = c.height < 0.1 * l2.y ? 0 : x2 - p3 + 1;
    t.pyramidLevel = s3, t.pyramidResolution = l2, t.srcWidth = y3, t.srcHeight = g3, t.ul = { x: h3, y: p3 };
  }
  _getRequestOptionsWithSliceId(e) {
    return null != this.rasterInfo.multidimensionalInfo && null == e.sliceId && (e = { ...e, sliceId: this.getSliceIndex(e.multidimensionalDefinition) }), e;
  }
  _processIdentifyResult(e, t) {
    const { srcLocation: i3, position: o, pyramidLevel: r2, useTransposedTile: n3 } = t, s3 = e.pixels[0].length / e.width / e.height;
    if (!(!e.mask || e.mask[o])) return { location: i3, value: null };
    const { multidimensionalInfo: l2 } = this.rasterInfo;
    if (null == l2 || !n3) {
      const t2 = e.pixels.map((e2) => e2[o]), n4 = { location: i3, value: t2, pyramidLevel: r2 }, s4 = this._computeMagDirValues(t2.map((e2) => [e2]));
      return (s4 == null ? void 0 : s4.length) && (n4.magdirValue = s4.map((e2) => e2[0])), n4;
    }
    let a4 = e.pixels.map((e2) => e2.slice(o * s3, o * s3 + s3)), c = this._computeMagDirValues(a4);
    const { requestSomeSlices: f, identifyOptions: m4 } = t;
    let u2 = i(l2, m4.transposedVariableName);
    if (f) {
      const e2 = a3(u2, m4.multidimensionalDefinition, m4.timeExtent);
      a4 = a4.map((t2) => e2.map((e3) => t2[e3])), c = c == null ? void 0 : c.map((t2) => e2.map((e3) => t2[e3])), u2 = e2.map((e3) => u2[e3]);
    }
    const h3 = e.noDataValues || this.rasterInfo.noDataValue, p3 = { pixels: a4, pixelType: e.pixelType };
    let x2;
    null != h3 && (a2(p3, h3), x2 = p3.mask);
    return { location: i3, value: null, dataSeries: u2.map((e2, t2) => {
      const i4 = { value: 0 === (x2 == null ? void 0 : x2[t2]) ? null : a4.map((e3) => e3[t2]), multidimensionalDefinition: e2.multidimensionalDefinition.map((e3) => new p2({ ...e3, isSlice: true })) };
      return (c == null ? void 0 : c.length) && (i4.magdirValue = [c[0][t2], c[1][t2]]), i4;
    }), pyramidLevel: r2 };
  }
};
function Q(e, t, i3) {
  return Math.abs(e.x - t.x) < i3 && Math.abs(e.y - t.y) < i3;
}
r([m2()], $.prototype, "_rasterTileAlignmentInfo", void 0), r([m2()], $.prototype, "_tileFetchQueue", void 0), r([m2({ readOnly: true })], $.prototype, "_isGlobalWrappableSource", null), r([m2({ readOnly: true })], $.prototype, "_hasNoneOrGCSShiftTransform", null), r([m2()], $.prototype, "_openPromise", void 0), r([m2()], $.prototype, "rasterJobHandler", null), r([m2({ readOnly: true })], $.prototype, "rasterId", null), r([m2(y2)], $.prototype, "url", null), r([m2({ type: String, json: { write: true } })], $.prototype, "datasetName", void 0), r([m2({ type: String, json: { write: true } })], $.prototype, "datasetFormat", void 0), r([m2()], $.prototype, "hasUniqueSourceStorageInfo", void 0), r([m2()], $.prototype, "rasterInfo", void 0), r([m2()], $.prototype, "ioConfig", void 0), r([m2()], $.prototype, "sourceJSON", void 0), $ = r([a("esri.layers.support.rasterDatasets.BaseRaster")], $);
var X = $;

export {
  X
};
//# sourceMappingURL=chunk-FMR7RBWD.js.map
