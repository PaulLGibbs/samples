import {
  t as t7
} from "./chunk-UFTVWZ64.js";
import {
  r
} from "./chunk-PNRAKV6Z.js";
import {
  C,
  O,
  o as o8
} from "./chunk-WV76XT7I.js";
import {
  e as e2,
  p as p3,
  t as t5,
  u,
  v
} from "./chunk-RQ3SD5QU.js";
import {
  h,
  x
} from "./chunk-EWM6TZBJ.js";
import {
  n2 as n5
} from "./chunk-6VRSN4D2.js";
import {
  o as o7,
  t as t6
} from "./chunk-CTYKVHGP.js";
import {
  o as o3
} from "./chunk-Z5PB3XRU.js";
import {
  i as i3,
  n as n4,
  t as t3
} from "./chunk-FSHGIYTP.js";
import {
  o as o2
} from "./chunk-LVGIVQEY.js";
import {
  o as o5
} from "./chunk-SCDCLADK.js";
import {
  d as d2
} from "./chunk-4FJV3US7.js";
import {
  p
} from "./chunk-MJJI5AGP.js";
import {
  d,
  f,
  p as p2
} from "./chunk-35USVNJT.js";
import {
  t as t4
} from "./chunk-IBHVRKIT.js";
import {
  o as o4
} from "./chunk-276B3RLG.js";
import {
  s
} from "./chunk-7FXRLNEH.js";
import {
  i as i4
} from "./chunk-XORINZPE.js";
import {
  o as o6
} from "./chunk-DHOEKN2U.js";
import {
  t as t2
} from "./chunk-7PFJGJMU.js";
import {
  n as n3
} from "./chunk-NVPM27JE.js";
import {
  i as i2
} from "./chunk-6QXEI4OT.js";
import {
  n as n2,
  t
} from "./chunk-55SEMSMA.js";
import {
  e
} from "./chunk-HHSHKUKB.js";
import {
  A,
  o
} from "./chunk-W2T7GHZG.js";
import {
  i
} from "./chunk-GIVBDSES.js";
import {
  n
} from "./chunk-4ZDWXD3G.js";

// node_modules/@arcgis/core/views/3d/terrain/TransparencyMode.js
var e3;
!function(e5) {
  e5[e5.Opaque = 0] = "Opaque", e5[e5.Transparent = 1] = "Transparent", e5[e5.InvisibleWithDraped = 2] = "InvisibleWithDraped", e5[e5.Invisible = 3] = "Invisible", e5[e5.COUNT = 4] = "COUNT";
}(e3 || (e3 = {}));

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/attributes/VertexTangent.glsl.js
function e4(e5, t8) {
  e5.varyings.add("tbnTangent", "vec3"), e5.varyings.add("tbnBiTangent", "vec3"), t8.spherical ? e5.vertex.code.add(n2`void forwardVertexTangent(vec3 n) {
tbnTangent = normalize(cross(vec3(0.0, 0.0, 1.0), n));
tbnBiTangent = normalize(cross(n, tbnTangent));
}`) : e5.vertex.code.add(n2`void forwardVertexTangent(vec3 n) {
tbnTangent = vec3(1.0, 0.0, 0.0);
tbnBiTangent = normalize(cross(n, tbnTangent));
}`), e5.fragment.code.add(n2`mat3 getTBNMatrix(vec3 n) {
return mat3(tbnTangent, tbnBiTangent, n);
}`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/terrain/TileBlendInput.js
var o9;
!function(o10) {
  o10[o10.LayerOnly = 0] = "LayerOnly", o10[o10.ColorComposite = 1] = "ColorComposite", o10[o10.GridComposite = 2] = "GridComposite", o10[o10.COUNT = 3] = "COUNT";
}(o9 || (o9 = {}));

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/terrain/TerrainTexture.glsl.js
var s2 = class extends h {
  constructor() {
    super(...arguments), this.overlayOpacity = 1;
  }
};
function d3(e5, n6) {
  const { vertex: s3, fragment: d4, varyings: x2 } = e5;
  x2.add("vtc", "vec2"), s3.uniforms.add(new l("texOffsetAndScale")), d4.uniforms.add(new u2("tex")), d4.uniforms.add(new i5("textureOpacities"));
  const f2 = n6.textureFadingEnabled && !n6.renderOccluded;
  f2 && (s3.uniforms.add(new l("nextTexOffsetAndScale")), x2.add("nvtc", "vec2"), d4.uniforms.add(new u2("texNext")), d4.uniforms.add(new i5("nextTexOpacities")), d4.uniforms.add(new a("fadeFactor")));
  const v2 = n6.tileBlendInput === o9.ColorComposite, p4 = n6.tileBlendInput === o9.GridComposite;
  p4 && d4.include(t7), v2 && d4.uniforms.add(new i5("backgroundColor")), s3.code.add(n2`
  void forwardTextureCoordinatesWithTransform(in vec2 uv) {
    vtc = texOffsetAndScale.xy + uv * texOffsetAndScale.zw;
    ${t(f2, "nvtc = nextTexOffsetAndScale.xy + uv * nextTexOffsetAndScale.zw;")}
  }`), d4.code.add(n2`
    vec4 getColor(vec4 color, vec2 uv, vec3 opacities) {
      ${t(p4 || v2, n2`if (opacities.y <= 0.0) {
           return color * opacities.z * opacities.x;
         }
         vec4 bg = vec4(${v2 ? n2`backgroundColor` : n2`gridColor(uv)`} * opacities.y, opacities.y);
         vec4 layer = color * opacities.z;
         return (bg * (1.0 - layer.a) + layer) * opacities.x;`, "return color;")}
    }`), f2 ? d4.code.add(n2`vec4 getTileColor() {
vec4 color = getColor(texture(tex, vtc), vtc, textureOpacities);
if (fadeFactor >= 1.0) {
return color;
}
vec4 nextColor = getColor(texture(texNext, nvtc), nvtc, nextTexOpacities);
return mix(nextColor, color, fadeFactor);
}`) : d4.code.add(n2`vec4 getTileColor() {
return getColor(texture(tex, vtc), vtc, textureOpacities);
}`);
}
var a = class extends i2 {
  constructor(e5) {
    super(e5, "float");
  }
};
var i5 = class extends i2 {
  constructor(e5) {
    super(e5, "vec3");
  }
};
var l = class extends i2 {
  constructor(e5) {
    super(e5, "vec4");
  }
};
var u2 = class extends i2 {
  constructor(e5) {
    super(e5, "sampler2D");
  }
};

// node_modules/@arcgis/core/chunks/Terrain.glsl.js
var k = class extends s2 {
};
function E(r2) {
  const i6 = new i4(), { vertex: M, fragment: k2, varyings: E2 } = i6, { output: G2, pbrMode: R, overlayMode: J, tileBorders: K, spherical: Q, transparencyMode: X, screenSizePerspective: Y } = r2;
  i6.include(o2), i6.include(t2, r2), i6.include(o5, r2);
  const Z = () => {
    i6.include(r, r2), M.code.add(n2`vec3 getNormal() {
float z = 1.0 - abs(normalCompressed.x) - abs(normalCompressed.y);
vec3 n = vec3(normalCompressed + vec2(normalCompressed.x >= 0.0 ? 1.0 : -1.0,
normalCompressed.y >= 0.0 ? 1.0 : -1.0) * min(z, 0.0), z);
return normalize(n);
}`);
  };
  f(M, r2), i6.include(o3, r2);
  const ee = X === e3.InvisibleWithDraped || X === e3.Invisible, re = J !== C.Disabled, oe = re && ee;
  switch (G2) {
    case n3.ColorEmission:
    case n3.Color:
      {
        i6.include(d3, r2), i6.include(v, r2), re && (r2.pbrMode = R === n5.Simplified ? n5.TerrainWithWater : n5.Water, i6.include(O, r2), r2.pbrMode = R);
        const t8 = J === C.EnabledWithWater;
        t8 && i6.include(e4, r2), E2.add("vnormal", "vec3"), E2.add("vpos", "vec3"), E2.add("vup", "vec3"), Z(), Y && p2(M);
        const n6 = r2.receiveShadows && !r2.renderOccluded;
        n6 && i6.include(n4, r2), Y && (E2.add("screenSizeDistanceToCamera", "float"), E2.add("screenSizeCosAngle", "float")), M.main.add(n2`
          vpos = position;
          vec3 positionWorld = position + localOrigin;
          gl_Position = transformPosition(proj, view, vpos);
          vnormal = getNormal();
          vup = getLocalUp(position, localOrigin);
          ${t(t8, n2`forwardVertexTangent(vnormal);`)}

          vec2 uv = getUV0();
          forwardTextureCoordinatesWithTransform(uv);
          ${t(re, "setOverlayVTC(uv);")}
          ${t(K, "forwardTextureCoordinates();")}
          ${t(Y, n2`vec3 viewPos = (view * vec4(vpos, 1.0)).xyz;
                 screenSizeDistanceToCamera = length(viewPos);
                 vec3 viewSpaceNormal = (viewNormal * vec4(normalize(positionWorld), 1.0)).xyz;
                 screenSizeCosAngle = abs(viewSpaceNormal.z);`)}
          ${t(n6, "forwardLinearDepth();")}`), i6.fragment.include(p, r2), i6.include(v, r2), i6.include(t5, r2), i6.include(x, r2), d(k2, r2), u(k2), p3(k2), k2.uniforms.add(M.uniforms.get("localOrigin"), new o4("viewDirection", ({ camera: e5 }) => A(q, o(q, e5.viewMatrix[12], e5.viewMatrix[13], e5.viewMatrix[14])))), t8 && k2.uniforms.add(new s("ovWaterTex", (e5, r3) => {
          var _a;
          return (_a = r3.overlay) == null ? void 0 : _a.getTexture(o8.WaterNormal);
        }), new t4("view", ({ origin: r3 }, { camera: o10 }) => i(I, o10.viewMatrix, r3)));
        const s3 = 0.2;
        k2.code.add(n2`float lum(vec3 c) {
return (min(min(c.r, c.g), c.b) + max(max(c.r, c.g), c.b)) * 0.5;
}`), t6(k2), o7(k2), k2.main.add(n2`
          vec3 normal = normalize(vnormal);
          float vndl = dot(normal, mainLightDirection);

          float additionalAmbientScale = smoothstep(0.0, 1.0, clamp(vndl*2.5, 0.0, 1.0));
          float shadow = ${n6 ? "max(lightingGlobalFactor * (1.0 - additionalAmbientScale), readShadowMap(vpos, linearDepth))" : Q ? "lightingGlobalFactor * (1.0 - additionalAmbientScale)" : "0.0"};

          float ssao = evaluateAmbientOcclusionInverse();
          vec4 tileColor = getTileColor();

          ${t(re, n2`vec4 overlayColorOpaque = getOverlayColor(ovColorTex, vtcOverlay);
                   vec4 overlayColor = overlayOpacity * overlayColorOpaque;
                   ${t(ee, `if (overlayColor.a < ${n2.float(o6)}) { discard; }`)}
                   vec4 groundColor = tileColor;
                   tileColor = tileColor * (1.0 - overlayColor.a) + overlayColor;`)}

          // If combined alpha is 0 we can discard pixel. The performance impact by having a discard here
          // is neglectable because terrain typically renders first into the framebuffer.
          if(tileColor.a < ${n2.float(o6)}) {
            discard;
          }

          bool sliced = rejectBySlice(vpos);
          if (sliced) {
            tileColor *= ${n2.float(s3)};
          }

          vec3 albedo = tileColor.rgb;

          // heuristic shading function used in the old terrain, now used to add ambient lighting

          vec3 additionalLight = ssao * mainLightIntensity * additionalAmbientScale * ambientBoostFactor * lightingGlobalFactor;

          ${R === n5.Simplified || R === n5.TerrainWithWater ? n2`fragColor = vec4(evaluatePBRSimplifiedLighting(normal, albedo, shadow, 1.0 - ssao, additionalLight, normalize(vpos - cameraPosition), vup), tileColor.a);` : n2`fragColor = vec4(evaluateSceneLighting(normal, albedo, shadow, 1.0 - ssao, additionalLight), tileColor.a);`}
          ${t(t8, n2`vec4 overlayWaterMask = getOverlayColor(ovWaterTex, vtcOverlay);
                   float waterNormalLength = length(overlayWaterMask);
                   if (waterNormalLength > 0.95) {
                     mat3 tbnMatrix = mat3(tbnTangent, tbnBiTangent, vnormal);
                     vec4 waterOverlayColor = vec4(overlayColor.w > 0.0 ? overlayColorOpaque.xyz/overlayColor.w : vec3(1.0), overlayColor.w);
                     vec4 viewPosition = view*vec4(vpos, 1.0);
                     vec4 waterColorLinear = getOverlayWaterColor(overlayWaterMask, waterOverlayColor, -normalize(vpos - cameraPosition), shadow, vnormal, tbnMatrix, viewPosition.xyz,  vpos + localOrigin);
                     vec4 waterColorNonLinear = delinearizeGamma(vec4(waterColorLinear.xyz, 1.0));
                     float opacity = sliced ? ${n2.float(s3)} : 1.0;
                     // un-gamma the ground color to mix in linear space
                     fragColor = mix(groundColor, waterColorNonLinear, waterColorLinear.w) * opacity;
                   }`)}
          ${t(Y, n2`float perspectiveScale = screenSizePerspectiveScaleFloat(1.0, screenSizeCosAngle, screenSizeDistanceToCamera, vec4(0.0));
                   if (perspectiveScale <= 0.25) {
                     fragColor = mix(fragColor, vec4(1.0, 0.0, 0.0, 1.0), perspectiveScale * 4.0);
                   } else if (perspectiveScale <= 0.5) {
                     fragColor = mix(fragColor, vec4(0.0, 0.0, 1.0, 1.0), (perspectiveScale - 0.25) * 4.0);
                   } else if (perspectiveScale >= 0.99) {
                     fragColor = mix(fragColor, vec4(0.0, 1.0, 0.0, 1.0), 0.2);
                   } else {
                     fragColor = mix(fragColor, vec4(1.0, 0.0, 1.0, 1.0), (perspectiveScale - 0.5) * 2.0);
                   }`)}
          ${t(r2.visualizeNormals, Q ? n2`
                  vec3 localUp = normalize(vpos + localOrigin);
                  vec3 right = normalize(cross(vec3(0.0, 0.0, 1.0), localUp));
                  vec3 forward = normalize(cross(localUp, right));
                  mat3 tbn = mat3(right, forward, localUp);
                  vec3 tNormal = normalize(normal * tbn);
                  fragColor = vec4(vec3(0.5) + 0.5 * tNormal, 0.0);` : n2`
                  vec3 tNormal = normalize(normal);
                  fragColor = vec4(vec3(0.5) + 0.5 * tNormal, 0.0);`)}
          ${t(K, n2`vec2 dVuv = fwidth(vuv0);
                 vec2 edgeFactors = smoothstep(vec2(0.0), 1.5 * dVuv, min(vuv0, 1.0 - vuv0));
                 float edgeFactor = 1.0 - min(edgeFactors.x, edgeFactors.y);
                 fragColor = mix(fragColor, vec4(1.0, 0.0, 0.0, 1.0), edgeFactor);`)}
          fragColor = applySlice(fragColor, vpos);`);
      }
      break;
    case n3.Depth:
      oe && i6.include(O, r2), M.main.add(n2`
        ${t(oe, "setOverlayVTC(getUV0());")}
        gl_Position = transformPosition(proj, view, position);`), k2.main.add(`${t(oe, `if (getCombinedOverlayColor().a < ${n2.float(o6)}) discard;`)}`);
      break;
    case n3.Shadow:
    case n3.ShadowHighlight:
    case n3.ShadowExcludeHighlight:
    case n3.ViewshedShadow:
      i6.include(e2, r2), i3(i6), t3(i6), M.main.add(n2`gl_Position = transformPositionWithDepth(proj, view, position, nearFar, linearDepth);`), k2.main.add(n2`outputDepth(linearDepth);`);
      break;
    case n3.Normal:
      oe && i6.include(O, r2), E2.add("vnormal", "vec3"), p2(M), Z(), M.main.add(n2`
        ${t(oe, "setOverlayVTC(getUV0());")}
        gl_Position = transformPosition(proj, view, position);
        vnormal = normalize((viewNormal * vec4(getNormal(), 1.0)).xyz);`), k2.main.add(n2`
        ${t(oe, `if (getCombinedOverlayColor().a < ${n2.float(o6)}) discard;`)}
        vec3 normal = normalize(vnormal);
        if (gl_FrontFacing == false) {
          normal = -normal;
        }
        fragColor = vec4(vec3(0.5) + 0.5 * normal, 1.0);`);
      break;
    case n3.Highlight:
      re && i6.include(O, r2), M.main.add(n2`
        ${t(re, "setOverlayVTC(getUV0());")}
        gl_Position = transformPosition(proj, view, position);`), i6.include(d2, r2), k2.main.add(n2`
        ${t(re, n2`
           vec2 overlayHighlightTexel = getAllOverlayHighlightValuesEncoded();
           calculateOcclusionAndOutputHighlightOverlay(overlayHighlightTexel);`, "calculateOcclusionAndOutputHighlight();")}
      `);
  }
  if (G2 === n3.ObjectAndLayerIdColor) if (re) r2.pbrMode = n5.Disabled, i6.include(O, r2), r2.pbrMode = R, M.main.add(n2`gl_Position = transformPosition(proj, view, position);
setOverlayVTC(getUV0());`), k2.main.add(n2`fragColor = getOverlayColorTexel();`);
  else {
    const e5 = X === e3.Opaque;
    M.main.add(n2`${t(e5, "gl_Position = transformPosition(proj, view, position);")}`), k2.main.add(n2`fragColor = vec4(0.0);`);
  }
  return i6;
}
var I = e();
var q = n();
var G = Object.freeze(Object.defineProperty({ __proto__: null, TerrainPassParameters: k, build: E }, Symbol.toStringTag, { value: "Module" }));

export {
  e3 as e,
  o9 as o,
  k,
  E,
  G
};
//# sourceMappingURL=chunk-WTD7P5IZ.js.map
