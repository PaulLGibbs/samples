import {
  l
} from "./chunk-4LSJIQF3.js";
import {
  S,
  b,
  d,
  m
} from "./chunk-REYN6SOX.js";
import {
  i
} from "./chunk-22K37LMJ.js";
import {
  c
} from "./chunk-S6MPVQGR.js";
import {
  s
} from "./chunk-CAZ5FG6J.js";

// node_modules/@arcgis/core/smartMapping/heuristics/scaleRange.js
var l2 = 500;
var c2 = 1e8;
var p = 1280;
var f = 12;
var m2 = 30;
var u = 15;
async function g(a) {
  const { view: t, sampleSize: c3 } = a;
  if (!a || !a.layer) throw new s("scale-range:missing-parameters", "'layer' parameter is required");
  if (a.snapToLOD && !t) throw new s("scale-range:missing-parameters", "'view' parameter is required when 'snapToLOD' is true");
  a.forBinning && i(a, "scale-range");
  const { layer: p2, ...f2 } = a, m3 = a.forBinning ? d : S, u2 = m(p2, m3, a.forBinning);
  if (!u2) throw new s("scale-range:invalid-parameters", "'layer' must be one of these types: " + b(m3).join(", "));
  const g2 = { layerAdapter: u2, ...f2 };
  g2.sampleSize = c3 || l2, await (t == null ? void 0 : t.when());
  const y2 = null != g2.signal ? { signal: g2.signal } : null;
  return await u2.load(y2), g2;
}
function y(e, a) {
  const t = f, n = p / 4, i2 = m2, s2 = p / 4, r = u, o = p / 2;
  let l3 = 0, c3 = 0, g2 = 0, y2 = 0;
  switch (e) {
    case "point":
    case "multipoint": {
      const e2 = a;
      l3 = e2.avgMinDistance ?? 0, c3 = t, g2 = e2.minDistance ?? 0, y2 = n;
      break;
    }
    case "polyline": {
      const e2 = a;
      l3 = e2.avgLength ?? 0, c3 = i2, g2 = e2.minLength ?? 0, y2 = s2;
      break;
    }
    case "polygon": {
      const e2 = a;
      l3 = e2.avgSize ?? 0, c3 = r, g2 = e2.minSize ?? 0, y2 = o;
      break;
    }
  }
  return { resolutionForMinScale: l3 > 0 ? l3 / c3 : null, resolutionForMaxScale: g2 > 0 ? g2 / y2 : null };
}
function w(e, t, n) {
  const i2 = y(e.geometryType, t), s2 = (n == null ? void 0 : n.spatialReference) || e.layer.spatialReference;
  return { minScale: c(i2.resolutionForMinScale ?? 0, s2), maxScale: c(i2.resolutionForMaxScale ?? 0, s2) };
}
function S2(e, a, t = true) {
  if (e.constraints && "effectiveLODs" in e.constraints) {
    const n = e.constraints.effectiveLODs, i2 = t ? n : n.slice().reverse();
    let s2 = null;
    for (const e2 of i2) if (!(t ? e2.scale > a : e2.scale < a)) {
      s2 = e2;
      break;
    }
    return s2;
  }
}
function h(a, t, n, i2) {
  const { view: s2, snapToLOD: r, layerAdapter: o } = a;
  if (r && s2) {
    const e = S2(s2, t), a2 = S2(s2, n, false);
    t = e ? e.scale : t, n = a2 ? a2.scale : n;
  }
  if (t < n) throw new s("scale-range:invalid", "calculated minScale is less than maxScale.");
  return n > t / 2 && (n = Math.floor(n / 2)), t > c2 && (t = 0), "polygon" !== o.geometryType && (n = 0), { minScale: Math.ceil(t), maxScale: Math.floor(n), spatialStatistics: i2 };
}
async function v(a) {
  const n = await g(a), { view: i2, sampleSize: s2, layerAdapter: r, signal: o, filter: l3 } = n, c3 = await r.getSampleFeatures({ view: i2, sampleSize: s2, returnGeometry: true, filter: l3, signal: o }, "json");
  if (!(c3 == null ? void 0 : c3.length)) throw new s("scale-range:insufficient-info", "No features are available to calculate statistics");
  const p2 = await l({ features: c3, geometryType: r.geometryType }), { minScale: f2, maxScale: m3 } = w(r, p2, i2);
  return h(n, f2, m3, p2);
}

export {
  v
};
//# sourceMappingURL=chunk-Z3DCM22I.js.map
