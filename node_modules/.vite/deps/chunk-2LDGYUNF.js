import {
  a as a4,
  m as m2
} from "./chunk-3DJDZ3OB.js";
import {
  l as l3
} from "./chunk-MQPF3LCI.js";
import {
  a as a3
} from "./chunk-OABA2CUT.js";
import {
  d as d3
} from "./chunk-LKUCAGRM.js";
import {
  c
} from "./chunk-QE62RBLB.js";
import {
  a as a2,
  d as d2,
  f,
  l as l2,
  o,
  u
} from "./chunk-BHUYP444.js";
import {
  A as A2,
  P,
  d,
  p,
  v
} from "./chunk-QXWM4LT3.js";
import {
  r
} from "./chunk-SCZRL2WP.js";
import {
  g
} from "./chunk-YZLMZUTQ.js";
import {
  m
} from "./chunk-HITYQXMG.js";
import {
  a2 as a
} from "./chunk-4XNJLQ7P.js";
import {
  A
} from "./chunk-OZPZC6AN.js";
import {
  l
} from "./chunk-C4UPF7I2.js";
import {
  e
} from "./chunk-E52FM5Z3.js";
import {
  n
} from "./chunk-3TGCPCOH.js";

// node_modules/@arcgis/core/widgets/Daylight/support/daylightUtils.js
var e2;
!function(t) {
  t[t.NORTHERN = 0] = "NORTHERN", t[t.SOUTHERN = 1] = "SOUTHERN";
}(e2 || (e2 = {}));
var i = { spring: { dayOfMonth: 20, month: 2 }, summer: { dayOfMonth: 21, month: 5 }, fall: { dayOfMonth: 23, month: 8 }, winter: { dayOfMonth: 21, month: 11 } };
var r2 = ["spring", "summer", "fall", "winter"];
function o2(t, n2, e3) {
  const r3 = f2(n2, e3), { dayOfMonth: o3, month: m4 } = i[r3], u3 = new Date(t.getTime());
  return u3.setMonth(m4, o3), u3;
}
function m3(t, n2) {
  return f2(u2(t), n2);
}
function u2(t) {
  const n2 = t.getTime(), i2 = o2(t, "spring", e2.NORTHERN).getTime(), r3 = o2(t, "summer", e2.NORTHERN).getTime(), m4 = o2(t, "fall", e2.NORTHERN).getTime(), u3 = o2(t, "winter", e2.NORTHERN).getTime();
  return n2 >= i2 && n2 < r3 ? "spring" : n2 >= r3 && n2 < m4 ? "summer" : n2 >= m4 && n2 < u3 ? "fall" : "winter";
}
function f2(t, n2) {
  return n2 === e2.NORTHERN ? t : s(t);
}
function s(t) {
  const n2 = r2.indexOf(t);
  return r2[(n2 + 2) % 4];
}
function N(n2, e3) {
  return u(n2, e3, "minutes");
}
function T(t) {
  return a2(t, "minutes");
}
function c2(t, n2, e3) {
  const i2 = 12e5, r3 = i2 / 4, o3 = 18e5, m4 = r3 / 3 * 2, u3 = 72e5;
  if (null == t || null == n2 || isNaN(t.getTime()) && isNaN(n2.getTime())) return r3;
  const f3 = e3.getTime(), s3 = t.getTime(), N2 = n2.getTime(), T2 = s3 - u3 / 2, c3 = s3 + u3 / 2, R = N2 - u3 / 3 * 2, g2 = N2 + u3 / 3;
  let O = r3;
  return f3 >= T2 && f3 <= c3 ? O = f3 - T2 <= i2 ? o3 - (f3 - T2) / i2 * (o3 - m4) : c3 - f3 <= i2 / 2 ? r3 - (c3 - f3) / i2 * 2 * (r3 - m4) : m4 : f3 >= R && f3 <= g2 ? O = f3 - R <= i2 / 2 ? r3 - (f3 - R) / i2 * 2 * (r3 - m4) : g2 - f3 <= i2 ? o3 - (g2 - f3) / i2 * (o3 - m4) : m4 : (f3 < s3 || f3 > N2) && (O = o3), O;
}

// node_modules/@arcgis/core/widgets/Daylight/support/SliderWithDropdownViewModel.js
var s2 = class extends l3 {
  constructor() {
    super(...arguments), this.utcOffset = null, this.timezonePickerOpen = false;
  }
};
r([m()], s2.prototype, "utcOffset", void 0), r([m()], s2.prototype, "timezonePickerOpen", void 0), s2 = r([a("esri.widgets.Daylight.support.SliderWithDropdownViewModel")], s2);

// node_modules/@arcgis/core/widgets/Daylight/DaylightViewModel.js
var E = class extends g {
  constructor(t) {
    super(t), this.view = null, this.timeSliderViewModel = new s2({ min: 0, max: 1439, values: [0], labelFormatFunction: m2, inputFormatFunction: m2 }), this.lightingUpdateInterval = 200, this._oldLighting = null, this.playSpeedMultiplier = 1, this._lastTime = null, this._sunrise = null, this._sunset = null, this._cachedLightingDateUTC = /* @__PURE__ */ new Date(0), this._cachedDisplayUTCOffset = 0, this._firstInteraction = true, this._lastLightingUpdate = 0, this._lightingUpdateHandle = null;
  }
  initialize() {
    this.addHandles([p(() => this.view, (t) => t.when(() => this._updateLighting()), P), d(() => {
      const t = this._lighting;
      return "sun" === (t == null ? void 0 : t.type) ? this._ensureValidDate(t) : null;
    }, (t) => this._scheduleLightingUpdate(t)), v(() => this._lighting, "timezone-will-change", (t) => this._timezoneWillChange(t), { onListenerAdd: () => this._timezoneWillChange(null) }), d(() => {
      var _a;
      return true === ((_a = this.view) == null ? void 0 : _a.stationary);
    }, () => {
      (this.dayPlaying || this.yearPlaying) && this._updateSunriseAndSunset();
    }, P), d(() => {
      const t = this.timeSliderViewModel;
      return { vm: t, state: t.state, sliderPosition: this.timeSliderPosition };
    }, ({ vm: t, state: e3, sliderPosition: i2 }) => {
      "ready" === e3 && t.setValue(0, i2);
    }), d(() => {
      var _a;
      return (_a = this.timeSliderViewModel) == null ? void 0 : _a.utcOffset;
    }, (t) => {
      null != t && (this.utcOffset = t);
    }), d(() => ({ utcOffset: this.utcOffset, sliderViewModel: this.timeSliderViewModel }), ({ utcOffset: t, sliderViewModel: e3 }) => {
      e3 && (e3.utcOffset = t);
    }, A2), d(() => this.timeSliderViewModel.timezonePickerOpen, () => this.stopPlaying()), d(() => this.timeSliderViewModel.values, (t) => this._setTimeSliderPosition((t == null ? void 0 : t[0]) ?? 0, { forceLightingUpdate: false }))]);
  }
  destroy() {
    this._cancelLightingUpdate(), this.view = null;
  }
  get isSupported() {
    return null == this.view || "3d" === this.view.type;
  }
  get utcOffset() {
    return this._cachedDisplayUTCOffset;
  }
  set utcOffset(t) {
    t !== this.utcOffset && null != this._lighting && "virtual" !== this._lighting.type && (this._lighting.displayUTCOffset = t, this._updateLighting());
  }
  get localDate() {
    return d2(this._lightingDateDisplay);
  }
  set localDate(t) {
    var _a;
    ((_a = this.view) == null ? void 0 : _a.ready) ? f(t) && t.getTime() !== this.localDate.getTime() && (this._lightingDateDisplay = l2(this._lightingDateDisplay, t)) : n.getLogger(this).error("The localDate property cannot be set before the view and the widget are ready.");
  }
  get timeSliderPosition() {
    return T(this._lightingDateDisplay);
  }
  set timeSliderPosition(t) {
    var _a, _b;
    ((_a = this.view) == null ? void 0 : _a.ready) && "ready" === ((_b = this.timeSliderViewModel) == null ? void 0 : _b.state) ? this._setTimeSliderPosition(t, { forceLightingUpdate: true }) : n.getLogger(this).error("The timeSliderPosition property cannot be set before the view and the widget are ready.");
  }
  _setTimeSliderPosition(t, e3) {
    Math.abs(t - this.timeSliderPosition) <= 1 / 60 || (this.stopPlaying(), this._enableDirectShadowsIfFirstInteraction(), e3.forceLightingUpdate && (this._cancelLightingUpdate(), this._updateLighting()), this._lightingDateDisplay = N(this._lightingDateDisplay, t));
  }
  _timezoneFromCamera(t, e3) {
    if (null == e3 || !t.cameraTrackingEnabled) return 0;
    const i2 = c([e3.longitude, e3.latitude]);
    return null == i2 ? 0 : Math.round(i2.hours + i2.minutes / 60 + i2.seconds / 3600);
  }
  get directShadowsEnabled() {
    var _a;
    return ((_a = this._lighting) == null ? void 0 : _a.directShadowsEnabled) ?? false;
  }
  set directShadowsEnabled(t) {
    const e3 = this._lighting;
    e3 && (e3.directShadowsEnabled = t);
  }
  get sunLightingEnabled() {
    return "sun" === this._lightingType;
  }
  set sunLightingEnabled(t) {
    const e3 = this._environment;
    if (t === this._get("sunLightingEnabled") || null == e3) return;
    const i2 = e3.lighting, n2 = this._oldLighting;
    this._oldLighting = i2;
    const s3 = { directShadowsEnabled: i2.directShadowsEnabled, cameraTrackingEnabled: i2.cameraTrackingEnabled }, a5 = t ? "sun" : "virtual";
    let l4;
    l4 = null != n2 && n2.type === a5 ? n2 : t ? new d3() : new a3(), l4.set(s3), e3.lighting = l4, t || (this.stopPlaying(), this.timeSliderViewModel.timezonePickerOpen = false);
  }
  set playingState(t) {
    this.playingState !== t && (this._set("playingState", t), "none" !== t && this.sunLightingEnabled && (this._updateSunriseAndSunset(), this._lastTime = Date.now(), this._play(), this._enableDirectShadowsIfFirstInteraction()));
  }
  get dayPlaying() {
    return "day" === this.playingState;
  }
  set dayPlaying(t) {
    t ? this.playingState = "day" : this.dayPlaying && (this.playingState = "none");
  }
  get yearPlaying() {
    return "year" === this.playingState;
  }
  set yearPlaying(t) {
    t ? this.playingState = "year" : this.yearPlaying && (this.playingState = "none");
  }
  get currentSeason() {
    return m3(this.localDate, this._currentHemisphere);
  }
  set currentSeason(t) {
    this.stopPlaying();
    const e3 = f2(t, this._currentHemisphere);
    this.localDate = o2(this.localDate, e3, e2.NORTHERN);
  }
  get _currentHemisphere() {
    var _a, _b, _c;
    const t = (_c = (_b = (_a = this.view) == null ? void 0 : _a.camera) == null ? void 0 : _b.position) == null ? void 0 : _c.latitude;
    return null == t || t >= 0 ? e2.NORTHERN : e2.SOUTHERN;
  }
  get _environment() {
    var _a;
    return (_a = this.view) == null ? void 0 : _a.environment;
  }
  get _lighting() {
    var _a;
    return (_a = this._environment) == null ? void 0 : _a.lighting;
  }
  get _lightingType() {
    var _a;
    return (_a = this._lighting) == null ? void 0 : _a.type;
  }
  get _lightingDateDisplay() {
    return o(this._cachedLightingDateUTC, this._cachedDisplayUTCOffset, "hours");
  }
  set _lightingDateDisplay(t) {
    const e3 = this._lighting;
    if (null == e3 || !this.sunLightingEnabled || "virtual" === e3.type || !f(t)) return;
    const i2 = this._ensureValidDate(e3), n2 = o(t, -this._cachedDisplayUTCOffset, "hours");
    n2.getTime() !== i2.getTime() && (e3.date = n2, this._updateLighting());
  }
  stopPlaying() {
    this.playingState = "none";
  }
  toggleDayPlaying() {
    this.dayPlaying = !this.dayPlaying;
  }
  toggleYearPlaying() {
    this.yearPlaying = !this.yearPlaying;
  }
  toggleSunLightingEnabled() {
    this.stopPlaying(), this.sunLightingEnabled = !this.sunLightingEnabled;
  }
  toggleDirectShadowsEnabled() {
    this.stopPlaying(), this.directShadowsEnabled = !this.directShadowsEnabled;
  }
  onDateChange(t) {
    this.dayPlaying = false, this.localDate = t;
  }
  _enableDirectShadowsIfFirstInteraction() {
    this._firstInteraction && (this._firstInteraction = false, this.directShadowsEnabled = true);
  }
  _updateLighting(t) {
    var _a;
    const e3 = Date.now();
    this._lastLightingUpdate = e3;
    const { view: i2 } = this, n2 = this._lighting;
    if (null == i2 || null == n2 || "virtual" === n2.type) return;
    t ?? (t = this._ensureValidDate(n2));
    const s3 = n2.displayUTCOffset, a5 = null != s3 ? s3 : this._timezoneFromCamera(n2, (_a = i2.camera) == null ? void 0 : _a.position);
    this._cachedLightingDateUTC.getTime() !== t.getTime() && (this._cachedLightingDateUTC = new Date(t.getTime())), this._cachedDisplayUTCOffset !== a5 && (this._cachedDisplayUTCOffset = a5);
  }
  _timezoneWillChange(t) {
    const e3 = this._lighting;
    if (null == e3 || "virtual" === e3.type || !e3.cameraTrackingEnabled) return;
    let i2;
    if (t) i2 = t.timezoneOffset;
    else {
      if (null != e3.displayUTCOffset) return;
      i2 = d3.calculateTimezoneOffset(e3.positionTimezoneInfo);
    }
    e3.displayUTCOffset = i2, this._scheduleLightingUpdate();
  }
  _scheduleLightingUpdate(t) {
    if (t && (this._lightingUpdateHandle = l(this._lightingUpdateHandle), !f(t))) return;
    if (this._lightingUpdateHandle) return;
    const e3 = Date.now() - this._lastLightingUpdate, n2 = this.lightingUpdateInterval - e3;
    let a5 = null;
    const l4 = () => {
      this._updateLighting(t), this._lightingUpdateHandle === a5 && (this._lightingUpdateHandle = null);
    };
    if (n2 <= 0) this._lightingUpdateHandle = a5 = A(l4);
    else {
      const t2 = setTimeout(l4, n2);
      this._lightingUpdateHandle = a5 = e(() => clearTimeout(t2));
    }
  }
  _cancelLightingUpdate() {
    this._lightingUpdateHandle = l(this._lightingUpdateHandle);
  }
  _play() {
    const t = this._lighting;
    if (null == t || !this.sunLightingEnabled || "virtual" === t.type) return;
    const e3 = this._ensureValidDate(t);
    if (this.dayPlaying || this.yearPlaying) {
      const i2 = Date.now() - (this._lastTime ?? 0);
      if (this.dayPlaying) {
        this._lastTime = Date.now();
        const n2 = c2(this._sunrise, this._sunset, e3) * this.playSpeedMultiplier / 100 * i2;
        if (n2 > 0) {
          let i3 = new Date(e3.getTime() + n2);
          const s3 = t.displayUTCOffset ?? 0;
          if (((i3.getUTCHours() + s3) % 24 + 24) % 24 < ((e3.getUTCHours() + s3) % 24 + 24) % 24) {
            const t2 = 864e5;
            i3 = new Date(e3.getTime() + n2 - t2);
          }
          t.date = i3;
        }
      } else {
        if (i2 > 1e3) {
          this._lastTime = Date.now();
          const i3 = (e3.getUTCMonth() + 1) % 12, n2 = new Date(e3.getTime());
          n2.setUTCMonth(i3), t.date = n2;
        }
      }
      requestAnimationFrame(() => this._play());
    }
  }
  _updateSunriseAndSunset() {
    var _a, _b;
    const t = this._lighting;
    if (null == t || "virtual" === t.type || !this.sunLightingEnabled) return;
    const e3 = (_b = (_a = this.view) == null ? void 0 : _a.camera) == null ? void 0 : _b.position;
    if (null == e3) return;
    const { latitude: i2, longitude: n2 } = e3, { date: s3, displayUTCOffset: a5 } = t, l4 = a4(s3, i2, n2, a5 ?? 0);
    l4 && (this._sunrise = new Date(l4.sunrise), this._sunset = new Date(l4.sunset));
  }
  _ensureValidDate(t) {
    return f(t.date) || (n.getLogger(this).warn("Invalid date. Reverting to the current date/time."), t.date = /* @__PURE__ */ new Date()), t.date;
  }
};
r([m()], E.prototype, "view", void 0), r([m({ type: s2, nonNullable: true })], E.prototype, "timeSliderViewModel", void 0), r([m()], E.prototype, "isSupported", null), r([m()], E.prototype, "lightingUpdateInterval", void 0), r([m()], E.prototype, "utcOffset", null), r([m({ type: Date })], E.prototype, "localDate", null), r([m()], E.prototype, "timeSliderPosition", null), r([m()], E.prototype, "directShadowsEnabled", null), r([m()], E.prototype, "sunLightingEnabled", null), r([m({ type: ["none", "day", "year"], value: "none" })], E.prototype, "playingState", null), r([m()], E.prototype, "dayPlaying", null), r([m()], E.prototype, "yearPlaying", null), r([m()], E.prototype, "playSpeedMultiplier", void 0), r([m()], E.prototype, "currentSeason", null), r([m()], E.prototype, "_lastTime", void 0), r([m()], E.prototype, "_sunrise", void 0), r([m()], E.prototype, "_sunset", void 0), r([m()], E.prototype, "_cachedLightingDateUTC", void 0), r([m()], E.prototype, "_cachedDisplayUTCOffset", void 0), r([m()], E.prototype, "_firstInteraction", void 0), r([m()], E.prototype, "_currentHemisphere", null), r([m()], E.prototype, "_environment", null), r([m()], E.prototype, "_lighting", null), r([m()], E.prototype, "_lightingType", null), r([m()], E.prototype, "_lightingDateDisplay", null), E = r([a("esri.widgets.Daylight.DaylightViewModel")], E);
var H = E;

export {
  r2 as r,
  s2 as s,
  H
};
//# sourceMappingURL=chunk-2LDGYUNF.js.map
