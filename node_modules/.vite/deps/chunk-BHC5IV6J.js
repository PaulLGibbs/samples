import {
  a as a3,
  e,
  t,
  u as u2
} from "./chunk-TQSG64Z5.js";
import {
  M,
  a as a4,
  g as g2,
  r as r2,
  u as u3
} from "./chunk-WWUOH62P.js";
import {
  a as a2,
  f,
  h,
  o,
  u
} from "./chunk-DDTJ4F3F.js";
import {
  B,
  D
} from "./chunk-YEYIQPNT.js";
import {
  d
} from "./chunk-LBKY47DP.js";
import {
  g,
  l
} from "./chunk-IT4SSBCW.js";
import {
  S
} from "./chunk-SXNZ7EJ6.js";
import {
  r
} from "./chunk-SCZRL2WP.js";
import {
  m
} from "./chunk-HITYQXMG.js";
import {
  a2 as a
} from "./chunk-4XNJLQ7P.js";
import {
  n
} from "./chunk-3TGCPCOH.js";

// node_modules/@arcgis/core/layers/support/rasterFunctions/surfaceUtils.js
var s = 1;
var n2 = 1 / 111e3;
function a5(t2) {
  let { altitude: e2, azimuth: i } = t2;
  const { hillshadeType: s2, pixelSizePower: a6 = 1, pixelSizeFactor: o3 = 1, scalingType: l3, isGCS: r4, resolution: c2 } = t2, h3 = "multi-directional" === s2 ? 2 * t2.zFactor : t2.zFactor, { x: u5, y: f3 } = c2;
  let d3 = h3 / (8 * u5), p = h3 / (8 * f3);
  if (r4 && h3 > 1e-3 && (d3 /= n2, p /= n2), "adjusted" === l3) if (r4) {
    const t3 = u5 * n2, e3 = f3 * n2;
    d3 = (h3 + t3 ** a6 * o3) / (8 * t3), p = (h3 + e3 ** a6 * o3) / (8 * e3);
  } else d3 = (h3 + u5 ** a6 * o3) / (8 * u5), p = (h3 + f3 ** a6 * o3) / (8 * f3);
  let x = (90 - e2) * Math.PI / 180, w = Math.cos(x), y = (360 - i + 90) * Math.PI / 180, m2 = Math.sin(x) * Math.cos(y), M2 = Math.sin(x) * Math.sin(y);
  const z = [315, 270, 225, 360, 180, 0], A = [60, 60, 60, 60, 60, 90], g3 = new Float32Array([3, 5, 3, 2, 1, 4]), F = g3.reduce((t3, e3) => t3 + e3), k = g3.map((t3) => t3 / F), P = "multi-directional" === s2 ? z.length : 1, S2 = new Float32Array(6), C = new Float32Array(6), T2 = new Float32Array(6);
  if ("multi-directional" === s2) for (let n3 = 0; n3 < P; n3++) e2 = A[n3], i = z[n3], x = (90 - e2) * Math.PI / 180, w = Math.cos(x), y = (360 - i + 90) * Math.PI / 180, m2 = Math.sin(x) * Math.cos(y), M2 = Math.sin(x) * Math.sin(y), S2[n3] = w, C[n3] = m2, T2[n3] = M2;
  else S2.fill(w), C.fill(m2), T2.fill(M2);
  return { resolution: c2, factor: [d3, p], sinZcosA: m2, sinZsinA: M2, cosZ: w, sinZcosAs: [...C], sinZsinAs: [...T2], cosZs: [...S2], weights: [...k], hillshadeType: ["traditional", "multi-directional"].indexOf(s2) };
}
function o2(t2, e2) {
  const { width: i, height: n3 } = t2, a6 = i * n3, o3 = new Uint8Array(a6), l3 = null != t2.mask;
  let r4;
  if (l3) {
    r4 = new Uint8Array(t2.mask);
    for (let t3 = 0; t3 < r4.length; t3++) r4[t3] && (o3[t3] = 1, r4[t3] = 1);
  }
  const c2 = t2.pixels[0], h3 = new Float32Array(a6), u5 = new Float32Array(a6);
  let f3, d3 = 0;
  if (e2) {
    const { resolution: t3 } = e2, i2 = t3 ? (t3.x + t3.y) / 2 : 1;
    d3 = 200 * e2.zFactor / (i2 * i2), f3 = new Float32Array(a6);
  }
  const p = e2 == null ? void 0 : e2.curvatureType;
  let x, w, y, m2, M2, z, A, g3;
  for (let F = s; F < n3 - s; F++) {
    const t3 = F * i;
    for (let e3 = s; e3 < i - s; e3++) {
      const s2 = t3 + e3;
      if (!r4 || 0 !== r4[s2]) {
        if (r4) {
          if (r4[s2 - i - 1] + r4[s2 - i] + r4[s2 - i + 1] + r4[s2 - 1] + r4[s2 + 1] + r4[s2 + i - 1] + r4[s2 + i] + r4[s2 + i + 1] < 7) {
            o3[s2] = 0;
            continue;
          }
          x = r4[s2 - i - 1] ? c2[s2 - i - 1] : c2[s2], w = r4[s2 - i] ? c2[s2 - i] : c2[s2], y = r4[s2 - i + 1] ? c2[s2 - i + 1] : c2[s2], m2 = r4[s2 - 1] ? c2[s2 - 1] : c2[s2], M2 = r4[s2 + 1] ? c2[s2 + 1] : c2[s2], z = r4[s2 + i - 1] ? c2[s2 + i - 1] : c2[s2], A = r4[s2 + i] ? c2[s2 + i] : c2[s2], g3 = r4[s2 + i + 1] ? c2[s2 + i + 1] : c2[s2];
        } else x = c2[s2 - i - 1], w = c2[s2 - i], y = c2[s2 - i + 1], m2 = c2[s2 - 1], M2 = c2[s2 + 1], z = c2[s2 + i - 1], A = c2[s2 + i], g3 = c2[s2 + i + 1];
        if (h3[s2] = y + M2 + M2 + g3 - (x + m2 + m2 + z), u5[s2] = z + A + A + g3 - (x + w + w + y), null != f3) {
          const t4 = c2[s2], e4 = 0.5 * (m2 + M2) - t4, i2 = 0.5 * (w + A) - t4;
          if ("standard" === p) f3[s2] = -d3 * (e4 + i2);
          else {
            const t5 = (-x + y + z - g3) / 4, n4 = (-m2 + M2) / 2, a7 = (w - A) / 2, o4 = n4 * n4, l4 = a7 * a7, r5 = o4 + l4;
            r5 && (f3[s2] = "profile" === p ? d3 * (e4 * o4 + i2 * l4 + t5 * n4 * a7) / r5 : -d3 * (e4 * l4 + i2 * o4 - t5 * n4 * a7) / r5);
          }
        }
      }
    }
  }
  return { outMask: l3 ? o3 : null, dzxs: h3, dzys: u5, curvatures: f3 };
}
function l2(t2, e2, i) {
  for (let s2 = 0; s2 < i; s2++) t2[s2 * e2] = t2[s2 * e2 + 1], t2[(s2 + 1) * e2 - 1] = t2[(s2 + 1) * e2 - 2];
  for (let s2 = 1; s2 < e2 - 1; s2++) t2[s2] = t2[s2 + e2], t2[s2 + (i - 1) * e2] = t2[s2 + (i - 2) * e2];
}
function r3(n3, r4) {
  if (!o(n3)) return n3;
  const { factor: c2, sinZcosA: h3, sinZsinA: u5, cosZ: f3, sinZcosAs: d3, sinZsinAs: p, cosZs: x, weights: w } = a5(r4), [y, m2] = c2, M2 = "traditional" === r4.hillshadeType, { width: z, height: A } = n3, g3 = new Uint8Array(z * A), { dzxs: F, dzys: k, outMask: P } = o2(n3);
  for (let t2 = s; t2 < A - s; t2++) {
    const e2 = t2 * z;
    for (let t3 = s; t3 < z - s; t3++) {
      const i = e2 + t3;
      if (!P || P[i]) {
        const t4 = F[i] * y, e3 = k[i] * m2, s2 = Math.sqrt(1 + t4 * t4 + e3 * e3);
        let n4 = 0;
        if (M2) {
          let i2 = 255 * (f3 + u5 * e3 - h3 * t4) / s2;
          i2 < 0 && (i2 = 0), n4 = i2;
        } else {
          const i2 = p.length;
          for (let a6 = 0; a6 < i2; a6++) {
            let i3 = 255 * (x[a6] + p[a6] * e3 - d3[a6] * t4) / s2;
            i3 < 0 && (i3 = 0), n4 += i3 * w[a6];
          }
        }
        g3[i] = 255 & n4;
      }
    }
  }
  l2(g3, z, A);
  return new g({ width: z, height: A, pixels: [g3], mask: P, pixelType: "u8", validPixelCount: n3.validPixelCount, statistics: [new l(0, 255)] });
}
function c(t2, e2, s2, n3) {
  if (!o(t2) || !o(e2)) return;
  const { min: a6, max: o3 } = n3, l3 = t2.pixels[0], { pixels: r4, mask: c2 } = e2, h3 = r4[0], u5 = 255.00001 / (o3 - a6), f3 = new Uint8ClampedArray(h3.length), d3 = new Uint8ClampedArray(h3.length), p = new Uint8ClampedArray(h3.length), x = s2.length - 1;
  for (let i = 0; i < h3.length; i++) {
    if (c2 && 0 === c2[i]) continue;
    const t3 = Math.floor((h3[i] - a6) * u5), [e3, n4] = s2[t3 < 0 ? 0 : t3 > x ? x : t3], o4 = l3[i], r5 = o4 * n4, w = r5 * (1 - Math.abs(e3 % 2 - 1)), y = o4 - r5;
    switch (Math.floor(e3)) {
      case 0:
        f3[i] = r5 + y, d3[i] = w + y, p[i] = y;
        break;
      case 1:
        f3[i] = w + y, d3[i] = r5 + y, p[i] = y;
        break;
      case 2:
        f3[i] = y, d3[i] = r5 + y, p[i] = w + y;
        break;
      case 3:
        f3[i] = y, d3[i] = w + y, p[i] = r5 + y;
        break;
      case 4:
        f3[i] = w + y, d3[i] = y, p[i] = r5 + y;
        break;
      case 5:
      case 6:
        f3[i] = r5 + y, d3[i] = y, p[i] = w + y;
    }
  }
  t2.pixels = [f3, d3, p], t2.updateStatistics();
}
function h2(e2, a6) {
  if (!o(e2)) return e2;
  const r4 = a6.zFactor, c2 = a6.pixelSizePower ?? 1, h3 = a6.pixelSizeFactor ?? 1, u5 = a6.slopeType, f3 = a6.isGCS, { x: d3, y: p } = a6.resolution;
  let x = r4 / (8 * d3), w = r4 / (8 * p);
  f3 && Math.abs(r4 - 1) < 1e-4 && (x /= n2, w /= n2), "adjusted" === u5 && (x = (r4 + d3 ** c2 * h3) / (8 * d3), w = (r4 + p ** c2 * h3) / (8 * p));
  const { dzxs: y, dzys: m2, outMask: M2 } = o2(e2), { width: z, height: A } = e2, g3 = new Float32Array(z * A);
  for (let t2 = s; t2 < A - s; t2++) {
    const e3 = t2 * z;
    for (let t3 = s; t3 < z - s; t3++) {
      const i = e3 + t3;
      if (!M2 || M2[i]) {
        const s2 = y[i] * x, n3 = m2[i] * w, a7 = Math.sqrt(s2 * s2 + n3 * n3);
        g3[e3 + t3] = "percent-rise" === u5 ? 100 * a7 : 57.2957795 * Math.atan(a7);
      }
    }
  }
  l2(g3, z, A);
  const F = new g({ width: z, height: A, pixels: [g3], mask: M2, pixelType: "f32", validPixelCount: e2.validPixelCount });
  return F.updateStatistics(), F;
}
function u4(n3, a6 = {}) {
  if (!o(n3)) return n3;
  const { resolution: r4 } = a6, c2 = r4 ? 1 / r4.x : 1, h3 = r4 ? 1 / r4.y : 1, { dzxs: u5, dzys: f3, outMask: d3 } = o2(n3), { width: p, height: x } = n3, w = new Float32Array(p * x);
  for (let t2 = s; t2 < x - s; t2++) {
    const e2 = t2 * p;
    for (let t3 = s; t3 < p - s; t3++) {
      const i = e2 + t3;
      if (!d3 || d3[i]) {
        const s2 = u5[i] * c2, n4 = f3[i] * h3;
        let a7 = -1;
        0 === s2 && 0 === n4 || (a7 = 90 - 57.29578 * Math.atan2(n4, -s2), a7 < 0 && (a7 += 360), 360 === a7 ? a7 = 0 : a7 > 360 && (a7 %= 360)), w[e2 + t3] = a7;
      }
    }
  }
  l2(w, p, x);
  return new g({ width: p, height: x, pixels: [w], mask: d3, pixelType: "f32", validPixelCount: n3.validPixelCount, statistics: [new l(-1, 360)] });
}
function f2(e2, s2) {
  if (!o(e2)) return e2;
  const { curvatures: n3, outMask: a6 } = o2(e2, s2), { width: r4, height: c2 } = e2;
  l2(n3, r4, c2);
  const h3 = new g({ width: r4, height: c2, pixels: [n3], mask: a6, pixelType: "f32", validPixelCount: e2.validPixelCount });
  return h3.updateStatistics(), h3;
}
function d2(t2, e2, i) {
  const { hillshadeType: s2, altitude: n3, azimuth: a6, zFactor: o3, pixelSizeFactor: l3, pixelSizePower: r4, slopeType: c2 } = t2;
  return { hillshadeType: s2, altitude: n3, azimuth: a6, zFactor: o3, pixelSizePower: r4, pixelSizeFactor: l3, scalingType: "scaled" === c2 ? "adjusted" : "none", resolution: e2, isGCS: i };
}

// node_modules/@arcgis/core/renderers/support/RasterSymbolizer.js
var L = class extends S {
  constructor(e2) {
    super(e2);
  }
  bind() {
    const { rendererJSON: e2 } = this;
    if (!e2) return { success: false };
    let t2;
    switch (this.lookup = { rendererJSON: {} }, e2.type) {
      case "uniqueValue":
        t2 = this._updateUVRenderer(e2);
        break;
      case "rasterColormap":
        t2 = this._updateColormapRenderer(e2);
        break;
      case "rasterStretch":
        t2 = this._updateStretchRenderer(e2);
        break;
      case "classBreaks":
        t2 = this._updateClassBreaksRenderer(e2);
        break;
      case "rasterShadedRelief":
        t2 = this._updateShadedReliefRenderer(e2);
        break;
      case "vectorField":
        t2 = this._updateVectorFieldRenderer();
        break;
      case "flowRenderer":
        t2 = this._updateFlowRenderer();
    }
    return t2;
  }
  symbolize(e2) {
    let t2 = e2 == null ? void 0 : e2.pixelBlock;
    if (!v(t2)) return t2;
    if (e2.simpleStretchParams && "rasterStretch" === this.rendererJSON.type) return this.simpleStretch(t2, e2.simpleStretchParams);
    try {
      let r4;
      switch (t2.pixels.length > 3 && (t2 = t2.extractBands(e2.bandIds ?? [0, 1, 2])), this.rendererJSON.type) {
        case "uniqueValue":
        case "rasterColormap":
          r4 = this._symbolizeColormap(t2);
          break;
        case "classBreaks":
          r4 = this._symbolizeClassBreaks(t2);
          break;
        case "rasterStretch":
          r4 = this._symbolizeStretch(t2, e2.bandIds);
          break;
        case "rasterShadedRelief": {
          const s2 = e2.extent, a6 = s2.spatialReference.isGeographic, o3 = { x: (s2.xmax - s2.xmin) / t2.width, y: (s2.ymax - s2.ymin) / t2.height };
          r4 = this._symbolizeShadedRelief(t2, { isGCS: a6, resolution: o3 });
          break;
        }
      }
      return r4;
    } catch (s2) {
      return n.getLogger(this).error("symbolize", s2.message), t2;
    }
  }
  simpleStretch(e2, t2) {
    if (!v(e2)) return e2;
    try {
      return e2.pixels.length > 3 && (e2 = e2.extractBands([0, 1, 2])), M(e2, { ...t2, isRenderer: true });
    } catch (s2) {
      return n.getLogger(this).error("symbolize", s2.message), e2;
    }
  }
  generateWebGLParameters(e2) {
    if (["uniqueValue", "rasterColormap", "classBreaks"].includes(this.rendererJSON.type)) {
      const { indexedColormap: e3, offset: t3 } = this.lookup.colormapLut || {};
      return { colormap: e3, colormapOffset: t3, isClassBreaks: "classBreaks" === this.rendererJSON.type, type: "lut" };
    }
    const { pixelBlock: t2, isGCS: r4, resolution: s2, bandIds: a6 } = e2, { rendererJSON: o3 } = this;
    return "rasterStretch" === o3.type ? this._generateStretchWebGLParams(t2, o3, a6) : "rasterShadedRelief" === o3.type ? this._generateShadedReliefWebGLParams(o3, r4, s2 ?? void 0) : "vectorField" === o3.type ? this._generateVectorFieldWebGLParams(o3) : null;
  }
  _isLUTChanged(e2) {
    if (!this.lookup || !this.lookup.rendererJSON) return true;
    if ("colorRamp" in this.rendererJSON) {
      const t2 = this.rendererJSON.colorRamp;
      return e2 ? JSON.stringify(t2) !== JSON.stringify(this.lookup.rendererJSON.colorRamp) : (this.rendererJSON, this.lookup.rendererJSON, JSON.stringify(this.rendererJSON) !== JSON.stringify(this.lookup.rendererJSON));
    }
    return JSON.stringify(this.rendererJSON) !== JSON.stringify(this.lookup.rendererJSON);
  }
  _symbolizeColormap(e2) {
    if (this._isLUTChanged()) {
      if (!this.bind().success) return e2;
    }
    return h(e2, this.lookup.colormapLut);
  }
  _symbolizeClassBreaks(e2) {
    const { canUseIndexedLUT: t2 } = this._analyzeClassBreaks(this.rendererJSON);
    if (this._isLUTChanged()) {
      if (!this.bind().success) return e2;
    }
    return t2 ? h(e2, this.lookup.colormapLut) : u(e2, this.lookup.remapLut ?? []);
  }
  _symbolizeStretch(e2, t2) {
    var _a, _b;
    const { rasterInfo: r4 } = this, { pixelType: s2, bandCount: a6 } = r4, o3 = this.rendererJSON, i = ["u8", "u16", "s8", "s16"].includes(s2);
    let u5, c2;
    const { dra: m2 } = o3, { gamma: f3 } = this.lookup;
    if ("histogramEqualization" === o3.stretchType) {
      const s3 = m2 ? null : (_a = this.lookup) == null ? void 0 : _a.histogramLut, a7 = g2(o3, { rasterInfo: r4, pixelBlock: e2, bandIds: t2, returnHistogramLut: !s3 }), n3 = M(e2, { ...a7, gamma: f3, isRenderer: true });
      c2 = f(n3, { lut: m2 ? a7.histogramLut : s3, offset: 0 });
    } else if (i) {
      if (m2) {
        const a7 = g2(o3, { rasterInfo: r4, pixelBlock: e2, bandIds: t2 });
        u5 = a4({ pixelType: s2, ...a7, gamma: f3, rounding: "floor" });
      } else if (this._isLUTChanged()) {
        if (!this.bind().success) return e2;
        u5 = this.lookup ? this.lookup.stretchLut : null;
      } else u5 = this.lookup ? this.lookup.stretchLut : null;
      if (!u5) return e2;
      a6 > 1 && null != t2 && t2.length === (e2 == null ? void 0 : e2.pixels.length) && (u5 == null ? void 0 : u5.lut.length) === a6 && (u5 = { lut: t2.map((e3) => u5.lut[e3]), offset: u5.offset }), c2 = f(e2, u5);
    } else {
      const s3 = g2(o3, { rasterInfo: r4, pixelBlock: e2, bandIds: t2 });
      c2 = M(e2, { ...s3, gamma: f3, isRenderer: true });
    }
    if (o3.colorRamp) {
      if (this._isLUTChanged(true)) {
        if (!this.bind().success) return e2;
      }
      c2 = h(c2, (_b = this.lookup) == null ? void 0 : _b.colormapLut);
    }
    return c2;
  }
  _symbolizeShadedRelief(e2, t2) {
    var _a;
    const r4 = this.rendererJSON, s2 = { ...r4, ...t2 }, a6 = r3(e2, s2);
    if (!r4.colorRamp) return a6;
    let o3;
    if (this._isLUTChanged(true)) {
      if (!this.bind().success) return a6;
      o3 = this.lookup ? this.lookup.hsvMap : null;
    } else o3 = this.lookup ? this.lookup.hsvMap : null;
    if (!o3) return a6;
    const n3 = ((_a = this.rasterInfo.statistics) == null ? void 0 : _a[0]) ?? { min: 0, max: 8e3 };
    return c(a6, e2, o3, n3), a6;
  }
  _isVectorFieldData() {
    const { bandCount: e2, dataType: t2 } = this.rasterInfo;
    return 2 === e2 && ("vector-magdir" === t2 || "vector-uv" === t2);
  }
  _updateVectorFieldRenderer() {
    return this._isVectorFieldData() ? { success: true } : { success: false, error: `Unsupported data type "${this.rasterInfo.dataType}"; VectorFieldRenderer only supports "vector-magdir" and "vector-uv".` };
  }
  _updateFlowRenderer() {
    return this._isVectorFieldData() ? { success: true } : { success: false, error: `Unsupported data type "${this.rasterInfo.dataType}"; FlowRenderer only supports "vector-magdir" and "vector-uv".` };
  }
  _updateUVRenderer(e2) {
    var _a;
    const { bandCount: t2, attributeTable: r4, pixelType: s2 } = this.rasterInfo, a6 = e2.field1;
    if (!a6) return { success: false, error: "Unsupported renderer; missing UniqueValueRenderer.field." };
    const o3 = e2.defaultSymbol, n3 = 1 === t2 && ["u8", "s8"].includes(s2);
    if (!t(this.rasterInfo, a6) && !n3) return { success: false, error: "Unsupported data; UniqueValueRenderer is only supported on single band data with a valid raster attribute table." };
    const i = [];
    if (null != r4) {
      const t3 = r4.fields.find((e3) => "value" === e3.name.toLowerCase());
      if (!t3) return { success: false, error: "Unsupported data; the data's raster attribute table does not have a value field." };
      r4.features.forEach((r5) => {
        var _a2, _b;
        const s3 = (_a2 = e2.uniqueValueInfos) == null ? void 0 : _a2.find((e3) => String(e3.value) === String(r5.attributes[a6])), n4 = (_b = s3 == null ? void 0 : s3.symbol) == null ? void 0 : _b.color;
        n4 ? i.push([r5.attributes[t3.name]].concat(n4)) : o3 && i.push([r5.attributes[t3.name]].concat(o3.color));
      });
    } else {
      if ("value" !== a6.toLowerCase()) return { success: false, error: 'Unsupported renderer; UniqueValueRenderer.field must be "Value" when raster attribute table is not available.' };
      (_a = e2.uniqueValueInfos) == null ? void 0 : _a.forEach((e3) => {
        var _a2;
        const t3 = (_a2 = e3 == null ? void 0 : e3.symbol) == null ? void 0 : _a2.color;
        t3 ? i.push([parseInt("" + e3.value, 10)].concat(t3)) : o3 && i.push([parseInt("" + e3.value, 10)].concat(o3 == null ? void 0 : o3.color));
      });
    }
    if (0 === i.length) return { success: false, error: "Invalid UniqueValueRenderer. Cannot find matching records in the raster attribute table." };
    const l3 = a2({ colormap: i });
    return this.lookup = { rendererJSON: e2, colormapLut: l3 }, this.canRenderInWebGL = a3(l3 == null ? void 0 : l3.indexedColormap), { success: true };
  }
  _updateColormapRenderer(e2) {
    if (!u2(this.rasterInfo)) return { success: false, error: "Unsupported data; the data source does not have a colormap." };
    const t2 = e2.colormapInfos.map((e3) => [e3.value].concat(e3.color)).sort((e3, t3) => e3[0] - t3[0]);
    if (!t2 || 0 === t2.length) return { success: false, error: "Unsupported renderer; ColormapRenderer must have meaningful colormapInfos." };
    const r4 = a2({ colormap: t2 });
    return this.lookup = { rendererJSON: e2, colormapLut: r4 }, this.canRenderInWebGL = a3(r4 == null ? void 0 : r4.indexedColormap), { success: true };
  }
  _updateShadedReliefRenderer(e2) {
    if (!e(this.rasterInfo)) return { success: false, error: `Unsupported data type "${this.rasterInfo.dataType}"; ShadedReliefRenderer only supports "elevation", or single band float/s16 data.` };
    if (e2.colorRamp) {
      const t2 = B(e2.colorRamp, { interpolateAlpha: true }), r4 = a2({ colormap: t2 }), s2 = D(r4.indexedColormap);
      this.lookup = { rendererJSON: e2, colormapLut: r4, hsvMap: s2 };
    } else this.lookup = null;
    return this.canRenderInWebGL = true, { success: true };
  }
  _analyzeClassBreaks(e2) {
    const { attributeTable: t2, pixelType: r4 } = this.rasterInfo, s2 = null != t2 ? t2.fields.find((e3) => "value" === e3.name.toLowerCase()) : null, a6 = null != t2 ? t2.fields.find((t3) => t3.name.toLowerCase() === e2.field.toLowerCase()) : null, o3 = null != s2 && null !== a6;
    return { canUseIndexedLUT: ["u8", "u16", "s8", "s16"].includes(r4) || o3, tableValueField: s2, tableBreakField: a6 };
  }
  _updateClassBreaksRenderer(e2) {
    const { attributeTable: t2 } = this.rasterInfo, { canUseIndexedLUT: r4, tableValueField: s2, tableBreakField: a6 } = this._analyzeClassBreaks(e2), o3 = e2.classBreakInfos;
    if (!(o3 == null ? void 0 : o3.length)) return { success: false, error: "Unsupported renderer; missing or invalid ClassBreaksRenderer.classBreakInfos." };
    const n3 = o3.sort((e3, t3) => e3.classMaxValue - t3.classMaxValue), i = n3[n3.length - 1];
    let l3 = e2.minValue;
    if (!r4) {
      const t3 = [];
      for (let e3 = 0; e3 < n3.length; e3++) t3.push({ value: n3[e3].classMinValue ?? l3, mappedColor: n3[e3].symbol.color }), l3 = n3[e3].classMaxValue;
      return t3.push({ value: i.classMaxValue, mappedColor: i.symbol.color }), this.lookup = { rendererJSON: e2, remapLut: t3 }, this.canRenderInWebGL = false, { success: true };
    }
    const c2 = [];
    if (null != t2 && null != s2 && null !== a6 && s2 !== a6) {
      const r5 = s2.name, o4 = a6.name, i2 = n3[n3.length - 1], { classMaxValue: u5 } = i2;
      l3 = e2.minValue;
      for (const e3 of t2.features) {
        const t3 = e3.attributes[r5], s3 = e3.attributes[o4], a7 = s3 === u5 ? i2 : s3 < l3 ? null : n3.find(({ classMaxValue: e4 }) => e4 > s3);
        a7 && c2.push([t3].concat(a7.symbol.color));
      }
    } else {
      l3 = Math.floor(e2.minValue);
      for (let e3 = 0; e3 < n3.length; e3++) {
        const t3 = n3[e3];
        for (let e4 = l3; e4 < t3.classMaxValue; e4++) c2.push([e4].concat(t3.symbol.color));
        l3 = Math.ceil(t3.classMaxValue);
      }
      i.classMaxValue === l3 && c2.push([i.classMaxValue].concat(i.symbol.color));
    }
    const p = a2({ colormap: c2, fillUnspecified: false });
    return this.lookup = { rendererJSON: e2, colormapLut: p }, this.canRenderInWebGL = a3(p == null ? void 0 : p.indexedColormap), { success: true };
  }
  _updateStretchRenderer(e2) {
    var _a, _b;
    let { stretchType: t2, dra: r4 } = e2;
    if (!("none" === t2 || ((_a = e2.statistics) == null ? void 0 : _a.length) || V(this.rasterInfo.statistics) || r4)) return { success: false, error: "Unsupported renderer; StretchRenderer.statistics is required when dynamic range adjustment is not used." };
    const s2 = e2.histograms || this.rasterInfo.histograms;
    !O(e2.stretchType) || (s2 == null ? void 0 : s2.length) || r4 || (t2 = "minMax");
    const { computeGamma: a6, useGamma: o3, colorRamp: n3 } = e2;
    let { gamma: i } = e2;
    if (o3 && a6 && !(i == null ? void 0 : i.length)) {
      const t3 = ((_b = e2.statistics) == null ? void 0 : _b.length) ? e2.statistics : this.rasterInfo.statistics;
      i = u3(this.rasterInfo.pixelType, t3);
    }
    const l3 = this.rasterInfo.pixelType, c2 = !r4 && ["u8", "u16", "s8", "s16"].includes(l3);
    if ("histogramEqualization" === t2) {
      const t3 = s2.map((e3) => r2(e3));
      this.lookup = { rendererJSON: e2, histogramLut: t3 };
    } else if (c2) {
      const t3 = g2(e2, { rasterInfo: this.rasterInfo }), r5 = a4({ pixelType: l3, ...t3, gamma: o3 ? i : null, rounding: "floor" });
      this.lookup = { rendererJSON: e2, stretchLut: r5 };
    }
    if (n3 && !T(n3)) {
      const t3 = B(n3, { interpolateAlpha: true });
      this.lookup || (this.lookup = { rendererJSON: e2 }), this.lookup.colormapLut = a2({ colormap: t3 }), this.lookup.rendererJSON = e2;
    }
    return this.lookup.gamma = o3 && (i == null ? void 0 : i.length) ? i : null, this.canRenderInWebGL = true, { success: true };
  }
  _generateStretchWebGLParams(e2, t2, r4) {
    var _a;
    let s2 = null, a6 = null;
    const o3 = (_a = this.lookup) == null ? void 0 : _a.colormapLut;
    t2.colorRamp && o3 && (s2 = o3.indexedColormap, a6 = o3.offset), "histogramEqualization" === t2.stretchType && (t2 = { ...t2, stretchType: "minMax" });
    const { gamma: n3 } = this.lookup, i = !(!t2.useGamma || !(n3 == null ? void 0 : n3.some((e3) => 1 !== e3))), { minCutOff: l3, maxCutOff: u5, minOutput: c2, maxOutput: p } = g2(t2, { rasterInfo: this.rasterInfo, pixelBlock: e2, bandIds: r4 });
    let h3 = 0;
    null != e2 && (h3 = e2.getPlaneCount(), 2 === h3 && ((e2 = e2.clone()).statistics = [e2.statistics[0]], e2.pixels = [e2.pixels[0]]));
    const { bandCount: m2 } = this.rasterInfo, f3 = Math.min(3, (r4 == null ? void 0 : r4.length) || h3 || m2, m2), y = s2 || i ? 1 : 255, b = new Float32Array(f3);
    if (i && n3) for (let d3 = 0; d3 < f3; d3++) n3[d3] > 1 ? n3[d3] > 2 ? b[d3] = 6.5 + (n3[d3] - 2) ** 2.5 : b[d3] = 6.5 + 100 * (2 - n3[d3]) ** 4 : b[d3] = 1;
    1 === l3.length && (l3[2] = l3[1] = l3[0]), 1 === u5.length && (u5[2] = u5[1] = u5[0]);
    const g3 = i && n3 ? [n3[0], n3[1] ?? n3[0], n3[2] ?? n3[0]] : [1, 1, 1], S2 = i ? [b[0], b[1] ?? b[0], b[2] ?? b[0]] : [1, 1, 1], k = u5.map((e3, t3) => u5[t3] === l3[t3] ? 0 : (p - c2) / (u5[t3] - l3[t3]) / y);
    return { bandCount: f3, minOutput: c2 / y, maxOutput: p / y, minCutOff: l3, maxCutOff: u5, factor: k, useGamma: i, gamma: g3, gammaCorrection: S2, colormap: s2, colormapOffset: a6, stretchType: t2.stretchType, type: "stretch" };
  }
  _generateShadedReliefWebGLParams(e2, t2 = false, r4 = { x: 0, y: 0 }) {
    var _a, _b;
    let s2 = null, a6 = null;
    const o3 = (_a = this.lookup) == null ? void 0 : _a.colormapLut;
    e2.colorRamp && o3 && (s2 = o3.indexedColormap, a6 = o3.offset);
    const n3 = { ...e2, isGCS: t2, resolution: r4 }, i = a5(n3), l3 = (_b = this.rasterInfo.statistics) == null ? void 0 : _b[0];
    return { ...i, minValue: (l3 == null ? void 0 : l3.min) ?? 0, maxValue: (l3 == null ? void 0 : l3.max) ?? 8e3, hillshadeType: "traditional" === e2.hillshadeType ? 0 : 1, type: "hillshade", colormap: s2, colormapOffset: a6 };
  }
  _generateVectorFieldWebGLParams(e2) {
    var _a, _b, _c;
    const { style: t2, inputUnit: r4, outputUnit: s2, visualVariables: a6, symbolTileSize: o3, flowRepresentation: n3 } = e2;
    let i;
    const l3 = ((_a = this.rasterInfo.statistics) == null ? void 0 : _a[0].min) ?? 0, u5 = ((_b = this.rasterInfo.statistics) == null ? void 0 : _b[0].max) ?? 50, c2 = (a6 == null ? void 0 : a6.find((e3) => "sizeInfo" === e3.type)) ?? { type: "sizeInfo", field: "Magnitude", maxDataValue: u5, maxSize: 0.8 * o3, minDataValue: l3, minSize: 0.2 * o3 }, p = c2.minDataValue ?? l3, d3 = c2.maxDataValue ?? u5, h3 = null != c2.maxSize && null != c2.minSize ? [c2.minSize / o3, c2.maxSize / o3] : [0.2, 0.8];
    if ("wind_speed" === t2) {
      const e3 = (h3[0] + h3[1]) / 2;
      h3[0] = h3[1] = e3;
    }
    const m2 = null != p && null != d3 ? [p, d3] : null;
    if ("classified_arrow" === t2) if (null != p && null != d3 && null != c2) {
      i = [];
      const e3 = (c2.maxDataValue - c2.minDataValue) / 5;
      for (let t3 = 0; t3 < 6; t3++) i.push(c2.minDataValue + e3 * t3);
    } else i = [0, 1e-6, 3.5, 7, 10.5, 14];
    const f3 = "flow_to" === n3 === ("ocean_current_kn" === t2 || "ocean_current_m" === t2) ? 0 : Math.PI, y = a6 == null ? void 0 : a6.find((e3) => "rotationInfo" === e3.type);
    return { breakValues: i, dataRange: m2, inputUnit: r4, outputUnit: s2, symbolTileSize: o3, symbolPercentRange: h3, style: t2 || "single_arrow", rotation: f3, rotationType: ((_c = this.rasterInfo.storageInfo) == null ? void 0 : _c.tileInfo) && "vector-uv" === this.rasterInfo.dataType ? "geographic" : (y == null ? void 0 : y.rotationType) || e2.rotationType, type: "vectorField" };
  }
};
r([m({ json: { write: true } })], L.prototype, "rendererJSON", void 0), r([m({ type: d, json: { write: true } })], L.prototype, "rasterInfo", void 0), r([m({ json: { write: true } })], L.prototype, "lookup", void 0), r([m()], L.prototype, "canRenderInWebGL", void 0), L = r([a("esri.renderers.support.RasterSymbolizer")], L);
var _ = L;
function O(e2) {
  return "percentClip" === e2 || "histogramEqualization" === e2;
}
function V(e2) {
  return null != e2 && e2.length > 0 && null != e2[0].min && null != e2[0].max;
}
function v(e2) {
  return o(e2) && 0 !== e2.validPixelCount;
}
function T(e2) {
  return "algorithmic" === e2.type && ["0,0,0,255", "0,0,0"].includes(e2.fromColor.join(",")) && ["255,255,255,255", "255,255,255"].includes(e2.toColor.join(","));
}

export {
  n2 as n,
  a5 as a,
  r3 as r,
  c,
  h2 as h,
  u4 as u,
  f2 as f,
  d2 as d,
  _
};
//# sourceMappingURL=chunk-BHC5IV6J.js.map
