import {
  v
} from "./chunk-WCORXEFT.js";
import {
  l
} from "./chunk-ZTGBQTGN.js";
import {
  b
} from "./chunk-OLLZQVIP.js";
import {
  h
} from "./chunk-74NOLTNG.js";
import {
  E
} from "./chunk-5U3RZOMJ.js";

// node_modules/@arcgis/core/widgets/smartMapping/support/utils.js
function r(t) {
  return E(new Date(t), v);
}
function s(t) {
  const n = 2, o = Math.floor(Math.log10(Math.abs(t))) + 1, e = o < 4 || o > 6 ? 4 : o, r2 = 1e6, s2 = Math.abs(t) >= r2 ? "compact" : "standard";
  return l(t, { notation: s2, minimumSignificantDigits: n, maximumSignificantDigits: e });
}
function a(t, n, i) {
  if (null == n || null == t) return [];
  const o = [];
  for (let e = -1 * i; e <= i; e++) 0 !== e && o.push(n + e * t);
  return o;
}
function u(t) {
  const { max: n, min: i, padding: o, pathHeight: e, pathWidth: r2, stops: s2 } = t, a2 = n - i;
  let u2, h3 = `M0 0 L${r2} 0 `;
  const m2 = 3 === s2.length ? [s2[0], s2[1], s2[2]] : [s2[0], s2[2], s2[4]], f2 = Math.min.apply(Math, m2.map((t2) => t2.size)), l3 = Math.max(Math.abs(m2[0].size - m2[1].size), Math.abs(m2[2].size - m2[1].size));
  return m2.reverse(), m2.forEach(({ size: t2, value: n2 }, s3) => {
    const m3 = Math.round((t2 - f2) / l3 * 100) / 100, c = Math.round(e - (n2 - i) / a2 * e);
    u2 = m3 * r2, 0 === u2 && (u2 = o * r2), 0 === s3 && (h3 += `L${u2} 0 `), h3 += `L${u2} ${c} `;
  }), h3 += `L${u2} ${e} L0 ${e} L0 0 Z`, h3;
}
function h2(t) {
  const { bottomValue: n, bottomWidth: i, max: o, min: e, pathHeight: r2, pathWidth: s2, topValue: a2, topWidth: u2 } = t, h3 = u2 * s2, m2 = i * s2, f2 = o - e, l3 = Math.round(r2 - (n - e) / f2 * r2);
  return `M${h3} 0 L${h3} ${Math.round(r2 - (a2 - e) / f2 * r2)} L${m2} ${l3} L${m2} ${r2} L0 ${r2} L0 0 Z`;
}
function m(t) {
  let n = t.maxSize, i = t.minSize;
  return n instanceof b && (n = n.stops[0].size), i instanceof b && (i = i.stops[0].size), [n, i];
}
function f(t, n, i) {
  const o = i.length - 1;
  if (o < 0) return [];
  const e = i[0], r2 = i[o] - e, s2 = n - t, a2 = [];
  for (let u2 = 1; u2 < o; u2++) {
    const n2 = (i[u2] - e) / r2 * s2 + t;
    a2.push({ index: u2, value: n2 });
  }
  return a2.unshift({ index: 0, value: t }), a2.push({ index: o, value: n }), a2;
}
function l2(n) {
  return n instanceof h ? n.toCss(true) : h.fromString(n ?? "").toCss(true);
}

export {
  r,
  s,
  a,
  u,
  h2 as h,
  m,
  f,
  l2 as l
};
//# sourceMappingURL=chunk-WZSGCEUK.js.map
